{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      // Bu paket şu an yalnızca tip denetimi yapıyor (tsconfig'de noEmit: true).
      // Gerçek paketleme Vite ile Faz 1.8'de gelecek; o zaman outputs
      // ["dist/**"] olarak geri açılır. Boş bırakılmasının sebebi turbo'nun
      // "no output files found" uyarısını susturmak değil, çıktının gerçekten
      // olmaması — cache anlamı bozulmasın.
      "outputs": []
    }
  }
}
