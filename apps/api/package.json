{
  "name": "@fms/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "NestJS HTTP + SSE",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
