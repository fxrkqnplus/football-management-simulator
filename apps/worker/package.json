{
  "name": "@fms/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BullMQ tüketicileri (tur simülasyonu)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
