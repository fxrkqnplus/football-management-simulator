## 🟢 TAMAM 4.11 — Faz 4'ün kapanışı (ER diyagramı · `world.md` · BORÇ-008 · faz kaydı · PR)

**Ne yapıldı:** Faz 4'ün son kabul kriteri kapandı (**6 / 6**), BORÇ-008 ödenip 17/17 mutasyonla kanıtlandı, tutarlılık kontrolü bir betiğe çevrilip gerçek bir uyuşmazlık buldu, faz kaydı yazıldı ve ANLIK DURUM sözleşmesine çekildi.

**Kapılar:** typecheck [x] · lint [x] · format [x]\* · test [x] · test:db [x] · build [x] SOĞUK · arch [x] · gaps:check [x] · D5 [x]
**Commit:** `9e59e89` (kod) · `3f8b902` (belge) · `<kayıt commit'i>` · push ⏳ · ANLIK DURUM güncellendi [x]
\* `format` denetlenen küme **TypeScript** — `.prettierignore` `*.md` taşıyor (SAPMA-024); bu alt görevin çıktısı ağırlıklı olarak Markdown.

**Kanıtlar:**

- **BORÇ-008** → kopya sayısı **bugün yeniden sayıldı**: **dokuz** (kütük tuttu). Kaba tarama **onuncu** aday gösterdi (`kit-templates.ts`), ölçüm **ayırdı**: `KIT_COLOR_SLOTS` sayı dizisi, çıktı tırnaksız `2, 3` → bağlansa **şema değişirdi**. → `drizzle-kit generate` = **"No schema changes"**; **mutasyon 17/17**
- **Kriter 6** → mermaid bloğu `er-diagram.itest.ts` **3/3 geçti**; prose **DÖRT yerde bayat** bulundu ve düzeltildi; render **yeniden ölçüldü** (22 varlık, `.mmd` ↔ `.svg` **4/4**)
- **Tutarlılık kontrolü** → `pnpm gaps:check`: **20 satır · 3 atlandı · 17 tarandı · 1 ✗ (G-13 → Faz 5)**, düzeltildikten sonra **0 ✗**. Negatif testle kanıtlandı
- **CI** → dalda **14 push koşusu: 11 yeşil · 1 kırmızı (`33419337117`) · 2 iptal**, liste sorgusundan türetildi
- **§0.5** → **4,562 gün** (4a **2,641** + 4b **1,673**); **kural değiştirilmedi**

**KARARIN GEREKİYOR:** YOK

**Yeni kayıt:** **SAPMA-035** — `SESSION-TEMPLATE` §15.1 Faz 5'i **Bölüm 13 = DAĞITIM**'a yönlendiriyordu ve i18n'in bir spec bölümü **yok**. · **BORÇ-008 KAPATILDI.**

**Sıradaki:** **FAZ 5 — i18n Altyapısı ve Terim Sözlüğü.** PR açıldıktan sonra **dur** — merge kullanıcının işi.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. KABUL KRİTERLERİ — 6 / 6

| # | Kriter | Alt görev | Nasıl doğrulandı |
|---|---|---|---|
| 1 | 5.000 sahte oyuncu seed → şema tutarlı | 4.9 | üç katman + D5 54/54 |
| 2 | Üç ileri FK + `ON DELETE` tanımlı | 4.4 | üç farklı davranış, **davranış** gerçek PG'ye karşı ölçüldü |
| 3 | *"20–24 yaş, sağ bek, CA>120"* < 50 ms | 4.10 | medyan **0,425 ms**, istatistiğin varlığı `last_analyze` ile denetlendi |
| 4 | Nitelik aralıkları CHECK **ALMAZ** | 4.5 | **negatif iddia** (`pg_constraint` boş liste) |
| 5 | İlişki değişmezleri CHECK **ALIR** | 4.5 | negatif test + sınır dahil karşı örnek |
| **6** | **Şema dokümanı güncellendi** | **4.11** | **aşağıda** |

## 2. KRİTER 6 — İKİ TARAF AYRI ÖLÇÜLDÜ, YALNIZCA BİRİ KORUNUYORDU

### ① Mermaid bloğu — KOŞTURULDU, varsayılmadı

`er-diagram.itest.ts` **3/3 geçti**: blok canlı katalogdan üretilen metnin
**birebir** aynısı, belge metninden sayılan tablo/ilişki sayısı katalogla ve
bugünün değerleriyle aynı (**22 / 32**), ve kontrol deneyi (blok bozulursa
karşılaştırma kırılır) çalışıyor.

*"4.8–4.10 tablo eklemedi, herhâlde günceldir"* **denmedi.**

### ② Prose — DÖRT YERDE BAYAT

| Yer | Neydi | Bugün ne | Ne zamandan beri bayat |
|---|---|---|---|
| Başlık (satır 3–5) | *"TAMAMLANDI (Faz 3.10) … indeksler **3.7'de** eklendi"* | **altı** indeks (3.7 → dört · 4.8 → iki) | **4.8** |
| `## Tablolar (15)` | 15 satır, **`player_hidden_attributes`te duruyor** | **22** — 4.6/4.7'nin **yedi tablosu** eksikti | **4.6** |
| Yabancı anahtarlar | *"**On altı** FK … **16/16**"* | **32/32** | **4.3** |
| Migration zinciri | **dokuz** dosya sayıyordu | **on iki** | **4.6** |

⚠️ **4.7'nin commit mesajı *"mermaid dışı prose sayıları da güncellendi"* diyordu
ve DOĞRUYDU** — dokunduğu sayılar (22/32) güncellendi. Dokunmadığı bölümler dört
alt görev boyunca bayat kaldı. **F1'in belge tarafındaki biçimi.**

**Ders belgenin KENDİ BAŞLIĞINA yazıldı:** nöbetçi bloğu koruyor, prose'u
**hiçbir şey** korumuyor — *"test geçti, belge günceldir"* geçersiz bir çıkarım.

### ③ Render — belgenin kendi kuralı 3.10'dan beri ateşlememişti

Paragraf *"şema değişince render tek seferlik yeniden ölçülür"* diyordu ve şema
**11 → 22 varlığa** çıkmıştı. `mermaid-cli 11`, tek seferlik, **repoya bağımlılık
eklenmedi** (`pnpm-lock.yaml` diff **boş**, ölçüldü):

| | 3.10 (11 varlık) | **4.11 (22 varlık)** |
|---|---|---|
| SVG | 403 KB | **954.908 bayt** |
| Hata kutusu | yok | **yok** |
| Varlık adı birebir bir kez | 11/11 | **22/22** |
| İşaretler | 9 `PK` · 2 `PK,FK` · 10 `FK` · 8 `UK` | **15 · 8 · 23 · 9** |

İşaretler **iki kaynaktan** sayıldı (üretilmiş `.mmd` ↔ render `.svg`) → **4/4**.

⚠️ **ÖLÇÜM ARACI İKİ KEZ YANLIŞ CEVAP ÜRETTİ (D2), ikisi de aynı koşuda:**

1. Hata kutusu dedektörü `error-icon` arıyordu → **yanlış pozitif**. O dize
   mermaid'in **varsayılan CSS'inde** duruyor (`.error-icon{fill:#552222;}`);
   gerçek imzalar (`aria-roledescription="error"`, görünür `Syntax error`)
   ikisi de **0**.
2. `.mmd` işaret sayacı işareti **satır sonunda** arıyordu, oysa yorumlu
   nitelikler tırnaklı metinle bitiyor (`integer club_id FK "null"`) → `FK`
   **12** sayıldı, gerçek **23**.

**İkisini de gösteren şey, sayımın iki kaynaklı olmasıydı.** Tek kaynaktan
sayılsaydı ikisi de sessiz kalırdı.

## 3. BORÇ-008 — ÖDENDİ

### Bugün ölçülen kopya sayısı: DOKUZ (dosya) / ON İKİ (çağrı yeri)

| Biçim | Dosya |
|---|---|
| Adlandırılmış yerel yardımcı (5) | `people` · `staff` · `managers` (`literals`) · `player-positions` (`inList`) · `data-pack-columns` (yerel `const`) |
| Satır içi (4) | `club-kits` · `competitions` · `countries` · `players` |

⚠️ **ONUNCU ADAY ÖLÇÜMLE ELENDİ.** Kaba tarama (`.join(', ')`)
`kit-templates.ts`i de gösteriyordu. Ama `KIT_COLOR_SLOTS = [2, 3] as const` bir
**sayı** dizisi; `.join(', ')` çıktısı tırnaksız `2, 3`. Buraya bağlanması
üretilen SQL'i `'2', '3'` yapardı — **şema değişirdi**. Ortaklaştırmanın sınırı
*"aynı görünen"* değil **"aynı metni üreten"** ifadedir.

### Doğrulama `typecheck` DEĞİL — ve bu yapısal

İfade bir **SQL metni** üretiyor ve o metin hiçbir tipe girmiyor. `typecheck`
bu refactor'ı sessizce geçirirdi (nitekim geçirdi).

| Kanıt | Sonuç |
|---|---|
| `drizzle-kit generate` | **"No schema changes, nothing to migrate"** · `drizzle/` git-temiz, snapshot dokunulmadı |
| **Mutasyon** (modül körlendi) | **17 / 17** — üretilen migration 17 CHECK kısıtının **17'sini birden** değiştirdi |

Mutasyonun kırdığı 17 kısıt adıyla listelendi: altı `*_source_check`
(`sourceCheck` üzerinden) + `club_kits_kit_type` + `competitions_type` +
`countries_work_permit_rule_key` + `players_primary_position` + `people_gender` +
`people_person_type` + `player_positions_position` + `player_positions_level` +
`staff_role` + `managers_coaching_badge` + `managers_experience_level`.

**Bağlanmayan tek bir çağrı yeri olsaydı sayı 16 olurdu.** Mutasyon uygulandıktan
hemen sonra geri alındı ve `generate` yeniden koşuldu (kural #42).

### Nöbetçi hatanın olacağı YERDE yaşıyor

`sql-literals.test.ts` (10 test) **iki ayrı iddia** taşıyor: fonksiyonun
sözleşmesi, ve **onuncu kopyanın yazılamaması** (`src/schema/` taranıyor).
Borç 7'den 9'a tam olarak bu nöbetçi olmadığı için çıkmıştı.

Negatif iddia **üç karşı kontrolle** körlükten çıkarıldı: tarayıcı sahibinin
gövdesinde deseni **buluyor** · taranan dosya sayısı ayrıca iddia ediliyor
(boş küme üzerinde koşan bir tarayıcı da boş liste döndürür) ·
`kit-templates.ts`in listede **olmadığı** ayrıca iddia ediliyor.

## 4. TUTARLILIK KONTROLÜ — TALİMATIN KENDİSİ BAYATTI

ROADMAP'in 4.11 maddesi şunu yazıyordu:

> *"her G-satırı için … doğrulanır (**bugün G-01…G-16**)"*
> *"kapatılmış satırları da (**G-03, G-08**) listeler ama atlar"*

**İkisi de bayat — ölçüldü:** kütükte **G-20**'ye kadar satır var (dört satır
menzil dışında kalırdı) ve kapalı satırlardan **G-18** atlama listesinde yoktu.
**Talimat olduğu gibi uygulansaydı kontrol yeşil verir ve dört satıra hiç
bakmazdı** — D3'ün en saf biçimi, üstelik D3'ü yakalamak için yazılmış bir adımda.

**Çare bir sayı güncellemesi değildi** (o da bayatlardı). `pnpm gaps:check`
(`scripts/check-gap-coverage.mjs`) satırları **kütükten sayıyor**, açık/kapalı
ayrımını **satırın kendi durum sütunundan** okuyor, hedef fazı *"Hangi faza ait
olmalı"* sütunundaki **kalın** `**Faz N**` atamalarından çıkarıyor (düz metinle
anılan fazlar bir atama değil bir **bağlam atfı** — ayraç dosyanın ölçülmüş
yazım deseni) ve ROADMAP'i `## FAZ N` başlıklarından dilimleyip satırın
**adıyla** geçtiğini doğruluyor. **Talimat artık hiçbir sayı taşımıyor.**

### Sonuç

```
kütükteki G satırı : 20
atlanan (kapalı)   : 3 — G-03, G-08, G-18
taranan (açık)     : 17
```

| Bulgu | |
|---|---|
| ✗ **G-13 → Faz 5** | Satır **iki** faza atanmış (*"**Faz 5** çeviri kaynağını belirler; **Faz 17** arama mekanizmasını seçer"*); 4.0 yalnızca Faz 17'ye işlemişti. **Faz 5 kapsamına bir kabul kriteriyle yazıldı.** |
| ✓ kalan 16 | hepsi hedef fazının kapsamında adıyla geçiyor |
| yeni boşluk | **YOK** — 4.11 hiçbir boşluk açmadı |

ℹ️ **G-13 bayat menzilin İÇİNDEYDİ (13 < 16)** — onu kaçıran şey menzil değil
**kontrolün hiç koşmamış olması**ydı. SAPMA-033'ün aynı biçimi.

### Betik kendi hatasını da gürültülü buldu

İlk koşuda kırıldı: G-06'nın hücresi **kaçışlı bir boru işareti** taşıyor
(`` `Sentry \| 5.000 olay/ay \| 4.000` ``) ve düz bir `split('|')` satırı
kaydırıyordu — *"Hangi faza ait olmalı"* sütunu bir başkasının içeriğini
gösteriyordu. **Sessizce geçmedi**: atama çıkaramayınca patladı (tasarım gereği).

**Negatif test:** ROADMAP'teki bir G atıfı geçici olarak bozuldu → **exit 1** ve
satır adıyla raporlandı; dosya birebir geri yüklendi (**md5 aynı**).

`pnpm gaps:check` **faz kapanış ritüeline eklendi** (`SESSION-TEMPLATE` adım 21).

### Aynı sınıfın BORÇ tarafı da ölçüldü

**BORÇ-003 ve BORÇ-005** kütükte *"ödenmesi gereken faz: 5"* yazıyor, ama
ROADMAP'te **yalnızca Faz 2 bölümünde** geçiyorlardı — yani borcun **açıldığı**
yerde, **ödeneceği** yerde değil. İkisi de Faz 5 kapsamına **ve bir kabul
kriterine** yazıldı. Borç bir boşluk değil, ama ayraç aynı: **kapsam taşıması
kütüğe kayıtla bitmez.**

## 5. YENİ SAPMA — 035

Faz kaydının §11'i *"sıradaki oturumun okuması gereken spec"* satırını
doldururken dosya adı **kontrol edildi** ve bulunamadı:

- `SESSION-TEMPLATE` §15.1: **Faz 5 → Bölüm 13**
- Bölüm 13 = **DAĞITIM** (`docs/spec/10-deployment.md` — Oracle, Caddy, yedekleme)
- `MASTER-SPEC`'in **on yedi bölümünün hiçbiri i18n değil**; `docs/spec/` altında
  **13 numaralı dosya yok** (ikisi de sayıldı)
- `CLAUDE.md` belge haritasında **Faz 5'i gösteren hiçbir satır yok**

**Kontrol edilmeseydi Faz 5 oturumu dağıtım spesifikasyonunu okuyarak açılacaktı.**

Sınıfı SAPMA-033'ün akrabası ama farkı önemli: orada *"kuralı kontrol eden adım
yoktu"*, burada **adım var ve yanlış yeri gösteriyor**. `spec/11` §12.3'ün kendi
cümlesi bunu zaten söylüyordu (bir **veri** alanı için): *"eksik bir alan okuyanı
ölçmeye gönderir, yanlış bir alan okuyanı **yanlış yere** gönderir."*

✅ Tablo satırı gerçeğe çevrildi. ⚠️ **Bir i18n spec'i YAZILMADI** (K12 — kapsam
Faz 5'in kendisi). Faz 48 de aynı tabloda Bölüm 13'e bakıyor ve o da şüpheli;
**ölçülmedi**, sahibi Faz 48.

## 6. FAZ KAYDI — 11 BAŞLIĞIN TAMAMI

| § | Başlık | Yazıldı |
|---|---|---|
| 1 | Fazın Konusu | ✅ |
| 2 | Yapılması Planlananlar | ✅ — **bir madde `[ ]` bırakıldı**: `player_stats_history` bölümleme değerlendirmesi |
| 3 | Gerçekte Yapılanlar | ✅ |
| 4 | Plandan Sapmalar | ✅ yedi satır |
| 5 | Karşılaşılan ve Giderilen Hatalar | ✅ günlük **#1…#38** desen bazlı işlendi |
| 6 | Kontroller ve Sonuçları | ✅ + **CI tam sayımı** + mutasyon serisi |
| 7 | Performans Ölçümleri | ✅ |
| 8 | Kabul Kriterleri Doğrulaması | ✅ **6/6** |
| 9 | Oluşturulan / Değişen Dosyalar | ✅ |
| 10 | Yeni Sorun / Borç / Sapma | ✅ |
| 11 | Sonraki Faz İçin Devir Teslim | ✅ |

⚠️ **§2'de bir madde `[ ]` KALDI ve gerekçesi yazıldı.** `player_stats_history`
yıla göre bölümleme **değerlendirilmedi**, çünkü değerlendirilecek bir şey yok:
tablo **boş** (satır yazan hat Faz 9/10'da doğuyor) ve bölümleme kararı
**hacimle** alınır. Bugün bölümlemek ölçülmemiş bir tahmini şemaya çakmak
olurdu. **Yeni bir G-satırı AÇILMADI** çünkü sahip zaten var: tabloyu dolduran
ve büyüten fazlar **Faz 12** ve **Faz 46**. *"Yapılmayan «yapıldı» yazılmaz."*

### Çalışma günlüğü

**38 satır** (#1…#38) faz kaydının **§5** tablosuna işlendi, tablo **boşaltıldı**,
**başlık kaldı** (`spec/11` §12.2). Silinen satır sayısı **programatik** sayıldı.

🆕 **Faz 4 yeni bir D/F harfi AÇMADI ve bu bir bulgu olarak yazıldı:** dört
fazdır aynı sözlük yetiyor. Bu fazın ürettiği yeni şey *desen* değil, var olan
desenlerin **daha dar ve ölçülmüş biçimleri** oldu — örn. D7'nin üçüncü biçimi
(*"doğru yapılmış bir ölçüm de yazıldığı andan sonra yanlışa döner"*). **D4 bu
fazda da hiç geçmedi.**

## 7. ANLIK DURUM — SÖZLEŞMESİNE ÇEKİLDİ

**Ölçüldü: 74 satır / 67 tablo satırı → 33 / 13.**

⚠️ **SIRA BAĞLAYICIYDI: önce faz kaydı, sonra silme.** Tersinde bilgi bir süre
**hiçbir yerde** olmazdı.

Silinen her satır için soruldu: *"bu bilgi faz kaydında ya da
`docs/reports/faz-04/` altında var mı?"*

| Silinen | Nereye taşındı |
|---|---|
| `4.4`…`4.7 (önceki/arşiv)` satırları | faz kaydı §3 · §5 + **13 rapor dosyası** |
| `CI (arşiv)` ×2 | faz kaydı §6'nın **CI alt bölümü** (tam sayım) |
| `kapı (arşiv)` ×2 · `kapı kapsamı` ×3 | faz kaydı §6 + blockquote |
| `D5 (arşiv)` ×3 | faz kaydı §6 |
| `kapsam (arşiv)` ×2 | faz kaydı §6 |
| `install (arşiv)` ×3 | faz kaydı §6'ya **yeni satır** (bağımlılık diff'i boş) |
| `G-17 (arşiv)` ×4 · `4.5'in ikinci dersi` | kütük (G-17) + faz kaydı §10 |
| `Şema durumu (arşiv)` ×2 · `Veri durumu (arşiv)` | faz kaydı §11 + yeni bloğun iki satırı |
| `kriterler (arşiv)` ×3 · `ilk gün (arşiv)` | faz kaydı §8 · §4 |

ℹ️ **13 satır hâlâ sözleşmenin *"~10"*undan fazla ve bu saklanmadı:** blok bir
**faz sınırında** duruyor, yani Faz 5'in başlangıç durumunun tamamını taşımak
zorunda. Faz 5'in ilk alt görevinde tekrar daralır.

## 8. CI — TAM SAYIM (liste sorgusundan türetildi, kopyalanmadı)

`gh run list --branch feature/faz-04-schema-ii` ile **her koşunun** `event` ·
`status` · `conclusion` alanları tek tek okundu.

| | |
|---|---|
| Toplam | **14** (hepsi `push`, hepsi `completed`) |
| ✅ `success` | **11** |
| ❌ `failure` | **1** |
| ⏹️ `cancelled` | **2** |

⚠️ **KIRMIZI KOŞU ADIYLA YAZILDI:** **`33419337117`** (`0459fa5`, 4.7,
2026-08-31). Sebebi kod dışıydı (İmaj/amd64, Docker Hub auth zaman aşımı) **ama
bir iptal değil bir BAŞARISIZLIK** ve `1c93890`'dan beri süren **27 ardışık
yeşil** seriyi kırdı. **Yeniden denenmedi** — dışa dönük bir eylem, kullanıcının
kararı. Bir faz kaydı kalıcıdır ve *"o gün bir kırmızı vardı"* bilgisi silinmez.

Kırmızıdan sonra **3 ardışık yeşil** (`33450312143` · `33559303179` ·
`33643171325`). ⏳ 4.11'in koşuları **yazım anında bilinmiyor**.

## 9. §0.5 — SÜRE ÖLÇÜLDÜ, KURAL DEĞİŞTİRİLMEDİ

Kaynak `git show -s`. Faz 4'ün ilk commit'i **`0682c5f`** (2026-08-29 05:00:19 +0300).

| | Süre | Commit | Eşik (3 gün) |
|---|---|---|---|
| **4a** (`0682c5f` → `0459fa5`) | **2,641 gün** | 23 | ✅ altında |
| **4b** (`9d848fa` → `3f8b902`) | **1,673 gün** | 8 | ✅ altında |
| **TOPLAM** | **4,562 gün** | 31 | aşıldı |

**Toplamın 3'ü aşması bölünmenin BEKLENEN sonucudur** — kural tam da bunun için
var ve 4.7'de **ateşledi, uygulandı**. Faz 3 karşılaştırması keskin: **4 gün,
bölünme yok, istisna kaydedilmedi**, çünkü o gün süreyi **ölçen bir adım yoktu**
(SAPMA-033). Adım 4.1'de eklendi ve bu faz onun ilk gerçek sonucu.

⚠️ **Kural bugün DEĞİŞTİRİLMEDİ** — `docs/ROADMAP.md`:181 kullanıcının açık
kararı; 4.11 yalnızca **ölçtü ve kaydetti**.

## 10. FAZ 5'E DEVİR — KONTROL EDİLDİ, VARSAYILMADI

| İş | Kütükte | ROADMAP Faz 5 kapsamında | Sonuç |
|---|---|---|---|
| **BORÇ-003** | ✅ *"Faz 5"* | ❌ **yoktu** (yalnızca Faz 2 bölümünde) | ✅ yazıldı + kabul kriteri |
| **BORÇ-005** | ✅ *"Faz 5"* | ❌ **yoktu** | ✅ yazıldı + aynı kriter |
| **G-13** | ✅ *"Faz 5 + Faz 17"* | ❌ yalnızca **Faz 17** | ✅ yazıldı + kabul kriteri |

**Üçü de hâlâ AÇIK** (ölçüldü) ve **üçü de kapsamda görünmüyordu.** *"Kapsam
taşıması kütüğe kayıtla bitmez"* kuralı bu turda **üç kez** ihlal edilmiş
bulundu — ve üçünü de yakalayan şey, devri **yazarken doğrulamak** oldu.

## 11. KAPILAR VE ÖLÇÜMLER

| Kapı | Sonuç | Bu alt görevin dosyalarına baktı mı? |
|---|---|---|
| `pnpm typecheck` | ✅ 10/10 | **Evet ama hiçbir şey söylemedi** — SQL metni hiçbir tipe girmiyor |
| `pnpm lint` | ✅ 0 (ilk koşuda **1 hata**: import sıralaması) | Evet |
| `pnpm format:check` | ✅ 0 | ⚠️ **Kısmen** — `.md` kapsam dışı (SAPMA-024) |
| `pnpm arch:check` | ✅ **9 kural** (325 ms) | Evet |
| `pnpm build` | ✅ **8/8 SOĞUK**, `Cached: 0`, **6,99 s** | Evet (`pwd` önce denetlendi) |
| `pnpm test` | ✅ **977 / 67** (+10 / +1) | **Evet — yeni modülün nöbetçisi** |
| `pnpm test:db` | ✅ **301 / 10** (değişmedi) | Evet — ve **değişmemesi doğru** |
| `pnpm gaps:check` | ✅ 20/3/17/**0 ✗** | 🆕 bu turda yazıldı |
| `drizzle-kit generate` | ✅ **"No schema changes"** | **ASIL DOĞRULAYICI** |
| **D5** | ✅ **21 / 21** | **ASIL DOĞRULAYICI** |
| Kapsam | fonksiyon **%80,31 (355/442)** | ⚠️ aşağıya bak |

### Kapsam — Faz 4'te İLK YÜKSELİŞ, ve sebebi yapısal

**%78,80 (357/453) → %80,31 (355/442).** Pay **ve** payda birlikte düştü:
refactor **11 fonksiyon sildi** (dört yerel yardımcı + dört satır içi `map`
geri çağrısı + …), **dokuzu kapsanmayan** ok fonksiyonuydu.

⚠️ **BU, FAZ 3'ÜN DÜRÜSTLÜK NOTUNUN CANLI KANITI:** `packages/db` kapsamı bir
**kanıt değil**. Fonksiyon kapsamı **1,5 puan yükseldi** ve tek bir yeni iddia
kanıtlanmadı — yalnızca sayılan şey azaldı.

Marj **yeniden hesaplandı** (kopyalanmadı): `355 / 0,70 = 507` → payda en fazla
**507**, bugün **442** → boşluk **65 fonksiyon**. **Eşik %70 düşürülmedi, dosya
dışlanmadı, import testi yazılmadı.**

### D5 — 21 / 21, ve DÖRT uyuşmazlık (dördü de benim beklentimde)

Derlenmiş `dist` + düz `node` + gerçek PostgreSQL **18.6** (`fms_d5_411`,
yaratıldı ve düşürüldü). Vitest yok, testcontainers yok, TS tipleri yok.

| # | Uyuşmazlık | Kim haklıydı |
|---|---|---|
| a | CHECK sayısı **17** sanıldı | **Katalog: 20.** Üçü `sqlLiterals`tan gelmiyor (`ca_le_pa` · `pa_range` · `kit_templates_color_slots`). İddia bir **karşı ölçüme** çevrildi: 17 + 3 = 20 ve üçü de adıyla iddia ediliyor |
| b | Negatif test **yanlış kısıtla** reddedildi (`portrait_seed NOT NULL`) | **#13'ün birebir sınıfı.** Bir ret, **hangi** kısıtın reddettiği sorulmadan bir kanıt değildir. Sütun kümesi `information_schema`'dan okundu (#32'nin reçetesi) |
| c | Karşı örnek **FK'dan** patladı | `countries` **boş** — D5 seed koşturmuyor |
| d | `countries` sütunları **hafızadan** yazıldı | **#30/#32'nin ÜÇÜNCÜ tekrarı.** `name` diye bir sütun yok (`name_key`), `uefa_coefficient` unutulmuştu |

**D5'in değeri ve bedeli aynı yerden geliyor:** fixture kullanmıyor, ham SQL
yazıyor. Dördü de **gürültülü** patladı.

### Mutasyon serisi — 29 / 301

Pay **ve** payda ikisi de sabit; **serinin ilk tam durağan satırı**, ve sebebi
ölçüldü: 4.11 şema **dosyalarına** dokundu ama **şemaya** dokunmadı, yeni olgu
türü yok, 10 yeni test **birim** testi (yani `test:db` paydası kımıldamadı).
`spec/09` §11.5'e işlendi.

## 12. NE YAPILMADI

- **Faz 5'e geçilmedi.** PR açıldı, **merge EDİLMEDİ** — merge kullanıcının işi.
- **4.7'nin kırmızı CI koşusu yeniden denenmedi** — dışa dönük eylem.
- **Bir i18n spec'i yazılmadı** (SAPMA-035) — kapsam Faz 5'in kendisi (K12).
- **`player_stats_history` bölümleme değerlendirmesi yapılmadı** — tablo boş,
  karar hacimle alınır; sahip Faz 12/46 (§2'de `[ ]` bırakıldı, gerekçesiyle).
- **`people_birth_date_idx` kaldırılmadı** — 4.10'da kullanılmadığı ölçüldü ama
  kaldırmak bir şema değişikliği olurdu; yeniden değerlendirme Faz 32'de.
- **`mermaid` repoya bağımlılık olarak eklenmedi** — render kalıcı bir kapı
  değil, tek seferlik ölçüm (K12; `pnpm-lock.yaml` diff **boş**).
