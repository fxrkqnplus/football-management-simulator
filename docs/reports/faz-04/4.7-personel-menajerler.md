> **Alt görev:** 4.7 — `staff` + `staff_attributes` + `managers` + `manager_attributes` (`0010`) + `migrateDownPastRefereeCheck` sarmalayıcısı
> **Tarih:** 2026-08-31 · **Dal:** feature/faz-04-schema-ii
> **Sarmalayıcı commit'i:** `420115f` · **İçerik commit'i:** `0f9557e`
> **CI koşusu / push:** yazım anında bilinmiyor — güncel durum ANLIK DURUM'da
> **Bağlam yüzdesi:** ölçülemedi — `/context` çıktısı bu turda mevcut değil
>
> ℹ️ Bu alt görevin ADIM 0 ve ANALİZ ölçümleri ayrı bir dosyada:
> `docs/reports/faz-04/4.7-adim-0-ve-analiz.md` (önceki oturumda yazıldı, Docker
> motoru kapalı olduğu için o gün commit edilemedi; `420115f` ile arşive girdi).

## 🟡 KARAR 4.7 — personel ve menajerler, ve §0.5 kontrol noktasının ilk gerçek sonucu

**Ne yapıldı:** Dört tablo (`0010`) yazıldı, Faz 4'ün master envanteri kapandı (22 tablo / 32 FK), ve kod işinden **önce** ayrı bir commit'le `0008`in sınırı bir sarmalayıcıya taşındı; §0.5 kontrol noktası kapanışta eşiği aştı ve Faz 4 ikiye bölündü.

**Kapılar:** typecheck [x] · lint [x] · format [x] · test [x] · build [x] · arch [x] · test:db [x] · D5 [x]
**Commit:** `420115f` (sarmalayıcı) + `0f9557e` (içerik) + kayıt · push [ ] · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- typecheck → `pnpm typecheck` → **10/10**. ⚠️ **Yine asıl doğrulayıcı DEĞİLDİ (beşinci tekrar):** 48 yeni sütunu ve altı FK'yı hiçbir şey söylemeden geçirdi.
- lint → `pnpm lint` → 0. İlk koşuda **2 hata** (`simple-import-sort`), `pnpm lint:fix`.
- format → `pnpm format:check` → temiz. **Bu commit'te `.ts`/`.sql` dosyaları var, yani kapı gerçekten baktı** (SAPMA-024 kontrolü yapıldı).
- build → `.turbo/cache` **silindi** → **8/8**, `Cached: 0` **doğrulandı**, 11,16 s. ⚠️ İlk deneme **yanlış kapsamda** koştu (2/2) — çalışma dizini `packages/db/drizzle`de kalmıştı; zemin doğrulanıp yeniden koşuldu (günlük #31).
- arch → `pnpm arch:check` → 9 kural temiz, 277 ms.
- test → `pnpm test` → **868 / 63 dosya** (822/59'dan +46/+4).
- test:db → **241 / 8 dosya** (230'dan +11). ⚠️ **İlk koşuda 23 test kırıldı** ve hepsi bayat envanterdi — hiçbir statik kapı göremezdi.
- **FK kuralı KOŞTURULDU** (hafızadan uygulanmadı) → **6/6**, üretilen SQL'in `ON DELETE` satırlarıyla tek tek karşılaştırıldı.
- **D5** → derlenmiş `dist`, düz `node`, ayrı veritabanı `fms_d5_47` → **25 / 25**.
- **Mutasyon** → `spec/09` §11.5'in tam metniyle → **27 / 241**; **pay SABİT ve sebebi ÖLÇÜLDÜ** (aşağıda).
- **Sarmalayıcının negatif kanıtı** → daraltma çıkarılınca round-trip'te **15 test** `people_person_type_check … is violated by some row` ile kırılıyor.
- **§0.5** → `git show -s` ile iki ölçüm: başta **1,941 gün**, kapanışta **2,633 gün**.

**KARARIN GEREKİYOR:** 1 madde

1. **§0.5 kontrol noktası eşiği aştı ve BÖLÜNMEYİ UYGULADIM — onaylıyor musun?**
   Ölçüm: 4.7'nin başında **1,941 gün** (aşılmadı), kapanışında **2,633 gün** (**aşıldı**).
   ROADMAP:1919 *"iki günü aştıysa 4.8–4.11 **Faz 4b** olarak ayrılır ve ROADMAP'e yazılır"* diyor; kural açık, çizgi hazırdı ve **yazdım**: **4a = 4.0→4.7 (kapandı) · 4b = 4.8→4.11**.
   Önerim: **böyle kalsın.** Gerekçe: kuralı ölçüp uygulamamak SAPMA-033'ün ta kendisi olurdu — Faz 3'te tam olarak bu oldu (4 gün, sınır 3, hiçbir şey olmadı). Kapsam, sıra ve kabul kriterleri **değişmedi**; tek PR ve tek faz kaydı yine 4.11'de.
   Cevap vermezsen: bölünme kalır ve 4.8'e (Faz 4b'nin ilki) geçerim.

**Yeni kayıt:** **BORÇ-008** (CHECK literal ifadesi dokuz şema dosyasında tekrarlanıyor, vadesi **4.11**) · günlük **#26–#31** · SAPMA yok · boşluk yok

**Sıradaki:** **4.8** — transfer arama indeksleri (`0011`); kapsam `spec/01`'in indeks satırından **değil** kriter 3'ün sorgusundan türetilir.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. İLK ADIM — `0008`in sınırı sarmalayıcıya taşındı (`420115f`)

### 1.1 Devir notunun sayıları BAYATTI — kaynaktan sayıldı (D7)

| Ölçüm | 4.6'nın raporu / prompt | **4.7'de sayıldı** |
|---|---|---|
| `narrow…` çağrı yeri | 15 | **17** |
| `seedAllTables()` çağrı yeri | 16 | **18** |
| Seed edip **bilerek** daraltmayan test | 1 | **1** ✅ |

Yapısal iddia doğruydu ve hâlâ doğru; bozulan yalnızca sayılardı. Sebep ölçüldü:
4.6 sayıyı alt görevin **ortasında** almış, sonra `0009`un iki testi eklenmişti.

⚠️ **D7'nin yeni bir biçimi.** Bugüne kadar *"kendi planımız kaynak değildir"*
diye okunuyordu; bu vaka **doğru yapılmış bir ölçümün de** yazıldığı andan sonra
yanlışa dönebileceğini gösteriyor.

### 1.2 `seedAllTables()` içine koymak — ölçüldü ve REDDEDİLDİ

`seedAllTables` yalnızca `round-trip.itest.ts`e ait (satır 414, dışa aktarılmıyor)
ve 18 çağıranının 18'i de sonrasında `migrateDown` yapıyor — yani *"bugün
çalışırdı"*. İki ölçülmüş sebeple reddedildi:

1. **Sınırın kendi testini kırardı.** *"0008 DOWN'u referee taşıyan bir satır
   varken GÜRÜLTÜLÜ patlıyor"* testi seed ediyor **ve bilerek daraltmıyor**.
   Daraltma seed'in içine girseydi o testin bir **opt-out bayrağına** ihtiyacı
   olurdu — kancanın bilerek reddettiği kaçış yolu deseni, tek bir test için.
2. **Ad yalan söylerdi.** `seedAllTables()` iki hakem kişisi yazıyor ve
   `SEEDED_ROWS.people` bunu sayıyor. *"Tabloları doldur"* adlı bir fonksiyon iki
   satırın `person_type`ını `['referee']` → `['staff']` yapardı; bugün buna bakan
   bir iddia yok, **yarın yazılacak ilk dağılım testi sessizce yanlış olurdu**.

### 1.3 Uygulanan biçim ve kapsamı

```ts
async function migrateDownPastRefereeCheck(options: DownOptions): Promise<DownResult> {
  await narrowRefereePersonTypesForDown();
  return migrateDown({ executor, source, logger }, options);
}
```

| Çağrı yeri sınıfı | Sayı | Ne oldu |
|---|---|---|
| Gerçek zincir, sınırın kendi testi dışında | **22** | → sarmalayıcı; 17 `narrow…` satırı ve 15 yorum satırı silindi |
| Gerçek zincir, **sınırın kendi testi** | **1** | **ham** `migrateDown` — kaçış yolu açık ve adlı |
| Fixture zinciri (`source: chain` / `broken`) | **9** | ham `migrateDown` — sarmalayıcı ilgisiz |

`{ executor, source, logger }` **içeride bağlı**: sarmalayıcı bir fixture
zincirine **yapısal olarak** uygulanamıyor (orada `people` yok, `UPDATE` patlardı).

**Dönüşüm satır numarası izin listesiyle yapıldı** ve her hedef **dokunulmadan
önce iddia edildi** — bütün dosyaya uygulanan bir regex, günlük #37'nin uyardığı
sınıf olurdu. Betik `21 + 1` yeniden yazım, `17` silme, `15` yorum silme raporladı.

### 1.4 Sarmalayıcının yük taşıdığı NEGATİF olarak kanıtlandı

Geçen bir test, çağrının yapıldığını **göstermez** (D3). Daraltma sarmalayıcıdan
geçici olarak çıkarıldı → `round-trip` **15 test** `people_person_type_check …
is violated by some row` ile kırıldı → geri alındı. Sarmalayıcı dekoratif değil.

### 1.5 🆕 Sınır İKİ dosyaya uzanıyor — 4.6 bunu görmemişti

`runner.itest.ts` de gerçek zinciri kullanıyor
(`createFileMigrationSource(DRIZZLE_DIR)`, `DRIZZLE_DIR = ../drizzle`) ve **beş**
`migrateDown` çağırıyor, `narrow…` **sıfır**. Bugün çalışıyor çünkü `people`'a
hiç hakem yazmıyor.

Sarmalayıcı oraya **taşınmadı** (K12 — `executor`/`source`/`logger`ı iki
entegrasyon dosyası arasında paylaşmak demekti). Bunun yerine **o dosyanın kendi
başlığına** uyarı yazıldı: *"buraya `'referee'` taşıyan bir satır yazan bir test
eklenirse `migrateDown` patlar; o gün yapılacak şey sarmalayıcının kardeşini
buraya yazmaktır."*

⚠️ **Nöbetçi, hatanın olacağı YERDE yaşar.** O dosyaya test ekleyecek kişi bir
alt görev raporunu değil, dosyanın başlığını okuyor.

## 2. Dört tablo — envanterler `spec/01`'den SAYILDI

⚠️ **Promptun ② notu (*"`spec/02`'den say"*) YANLIŞTI ve ölçümle çürüdü:**
`docs/spec/02-attributes.md` içinde `staff` / `manager` → **0 eşleşme** (208
satırın tamamı). Envanterlerin tek kaynağı `spec/01` §3.1 (satır 686-737).

Bu, 4.6'nın `trait_code` vakasının **tersi**: küme sayılabilir, ama beklenen
belgede değil. *"Kaynaktan say"* yetmiyor — **hangi kaynak** sorusu da ölçülüyor.

| Envanter | `spec/01`'den sayıldı | **Üretilen SQL'den karşı-ölçüm** |
|---|---|---|
| `staff.role` | 12 | `staff_role_check` içinde **12** literal |
| `staff_attributes` | 16 | `staff_attributes` **19 sütun** (1+16+2), **16** `smallint NOT NULL` |
| `manager_attributes` | 8 | `manager_attributes` **11 sütun** (1+8+2), **8** `smallint NOT NULL` |
| `managers.coaching_badge` | 5 | `managers_coaching_badge_check` içinde **5** |
| `managers.experience_level` | 5 | `managers_experience_level_check` içinde **5** |

`staff` **6 sütun**, `managers` **12 sütun** (drizzle-kit çıktısından ve D5'te
katalogdan ayrıca doğrulandı).

### 2.1 §3.1.2 ②'nin ayracı BEŞ kez koştu, İKİ cevap verdi

| Sütun | `spec/01` nasıl yazmış | Ayraç | Karar |
|---|---|---|---|
| `staff.role` | 12 değer, **kapalı** | sözleşme | **CHECK** |
| `managers.coaching_badge` | 5 değer, **kapalı** | sözleşme | **CHECK** |
| `managers.experience_level` | 5 değer, **kapalı** | sözleşme | **CHECK** |
| `managers.philosophy` | `'attacking'\|'control'\|'balanced'\|`**`...`** | **açık uçlu** | **CHECK YOK** |
| `managers.reputation` | `// 0-200` **aralık** | kalibrasyon | **CHECK YOK** |

⚠️ **`philosophy`nin üç noktası kararı tek başına veriyor.** Kümenin sonu
yazılmamış → sayılamıyor → kapalı iddia edilemez. 4.6'nın `trait_code`
gerekçesinin aynısı; tek fark orada kümenin **hiç**, burada **eksik** tanımlı
olması.

CHECK yokluğu **iki yönlü** iddia ediliyor: katalogda `managers`ın **yalnızca
iki** CHECK'i var (ikisi de `philosophy` hakkında değil) **ve** uydurma bir
felsefe (`bu_felsefe_hicbir_listede_yok`) gerçekten kabul ediliyor.

### 2.2 `staff.person_id` UNIQUE DEĞİL — ölçülmüş bir fark

`spec/01` `players`ı `personId: FK UNIQUE`, `staff`ı yalnızca `personId FK` diye
yazıyor. Aynı kişi bir kulüpte kondisyon antrenörü, başkasında gözlemci olabilir
(`personType` zaten bir dizi).

Fark **kullanılıyor**: seed `kisi-5`e iki rol yazıyor ve entegrasyon testi
üçüncüsünü ekleyip **3** satır sayıyor. Yazılmasaydı bir gün *"tutarlılık"*
gerekçesiyle UNIQUE eklenebilir ve hiçbir şey ötmezdi.

Bunun bir bedeli var ve `fixtures.ts`e yazıldı: `staffIdOfPerson` **iki
bileşen** (kişi + rol) alıyor, `playerIdOfPerson` bir tane. Rol olmadan alt
sorgu ya *"birden çok satır"* ile patlar ya da **yanlış personeli** seçer — G-17
bir kademe daha derinleşti.

### 2.3 `managers.user_id` YAZILMADI — ve yokluğu KOŞAN bir iddia

Sütun eklenmedi (SAPMA-032 / G-16, Faz 13) ve üç test bunu sabitliyor:
① tabloda `userId` yok ② `spec/01` sütunu **hâlâ istiyor** (yani bu bir eksiklik
değil **erteleme**) ③ `is_user_manager` onun yerini tutmuyor — biri *"bir insan
mı oynuyor"*, diğeri *"hangi insan"*.

**Faz 13 sütunu eklediğinde ① kırılacak** ve kararın bilinçli olduğu görünecek.

### 2.4 `staff_roles` AÇILMADI — 4.1'in gerekçesi DENETLENDİ

Karar kopyalanmadan önce dört gerekçesi tek tek denetlendi ve dördü de geçerli
çıktı; ikisi ayrıca **koşan bir testle** sabitlendi (`spec/01` rolleri hâlâ satır
içi yazıyor · `staff_roles: {` diye bir blok yok). *"Bir kararı kopyalamadan
önce gerekçesinin hâlâ geçerli olduğu sorulur"* — ve bu soru bir temenni değil,
koşan bir adım olmalı.

## 3. FK kuralı KOŞTURULDU → 6/6, ve bir KARŞI-ÖLÇÜM

Derlenmiş `packages/db/dist/schema/fk-policy.js` gerçek olgularla beslendi,
**tahmin SQL'den önce yazıldı**, sonra üretilen migration'ın `ON DELETE`
satırlarıyla karşılaştırıldı.

| FK | Kuralın cevabı | Üretilen SQL | |
|---|---|---|---|
| `staff.person_id` → `people` | CASCADE | `ON DELETE cascade` | ✅ |
| `staff.club_id` → `clubs` | SET NULL | `ON DELETE set null` | ✅ |
| `staff_attributes.staff_id` → `staff` | CASCADE | `ON DELETE cascade` | ✅ |
| `managers.person_id` → `people` | CASCADE | `ON DELETE cascade` | ✅ |
| `managers.club_id` → `clubs` | SET NULL | `ON DELETE set null` | ✅ |
| `manager_attributes.manager_id` → `managers` | CASCADE | `ON DELETE cascade` | ✅ |

⚠️ Hedef `people`/`clubs` **independent** olmasına rağmen cevap CASCADE — kural
②'den sonra **kaynağın** sınıfına bakıyor. 4.6'nın `competition_id` dersinin
tekrarı.

### 3.1 KARŞI-ÖLÇÜM — `key` kararı 6 FK'nın 4'ünü çeviriyor

`spec/01` §3.1.0'ın iki listesi dört tabloyu da **hiç saymıyordu**. Karar
uydurulmadı:

```
staff/managers `key` TAŞIMAZSA (karar):   CASCADE ×4 + SET NULL ×2
staff/managers `key` TAŞISAYDI:           RESTRICT ×4 + CASCADE ×2
```

En keskin sonuç `club_id`: `key` taşınsaydı bir kulüp silindiğinde personeli ve
menajeri serbest bırakmak yerine silme **reddedilirdi**. `spec/01` `clubId`i
açıkça *nullable* yazıyor (işsiz personel/menajer geçerli durumlar), yani doğru
cevap `SET NULL` ve o **yalnızca** `key` taşınmadığında çıkıyor.

Mekanizma §3.1.0'ın `people` ↔ `players` ölçümünün (20/20 ↔ 17/20) aynısı.
**Dört tablo §3.1.0'ın "TAŞIMAYAN" listesine eklendi**, gerekçesiyle.

## 4. 1:1 ayracı KOŞTURULDU — kardeş tablodan kopyalanmadı

| Arama (`spec/01` tamamı) | Eşleşme |
|---|---|
| `staff_attributes` / `staffAttributes` | **1** — yalnızca kendi tanımı |
| `staffAttributeId` / `staff_attribute_id` | **0** |
| `manager_attributes` / `managerAttributes` | **1** — yalnızca kendi tanımı |
| `managerAttributeId` / `manager_attribute_id` | **0** |

→ gelen FK **0** → **PK = FK** (3.5 deseni). `spec/01`'in `staffId PK FK` yazımı
bir **iddia**, gelen FK sayısı bir **ölçüm** (D7).

ℹ️ Ayrıca ölçüldü: `managers`a bakan **dört** FK var — biri `manager_attributes`
(master, bu fazda), üçü §3.2 save katmanında (`saves` · `manager_career` ·
`board_confidence`, Faz 12/13). Ayraç **uydunun** sorusudur, sahibinin değil.

## 5. #16 DÖRDÜNCÜ kez ateşledi — üç testin sınıfı değişti

`stepsBackTo('0009…')` `0010` eklenince **iki** adım oldu:

| Test | 4.6'da | **4.7'de** |
|---|---|---|
| *"yalnızca 0009 geri alınınca"* | 3 tablo | **7 tablo** (başlık da değişti) |
| *"0006'ya kadar — KARIŞIK kayıp"* | 5 tablo | **9 tablo** |
| *"SÜTUN kaybı — 0003+0002+0001"* | 17 tablo | **21 tablo** |
| *"0008'in KENDİ katkısı sıfır"* | 3 tablo | **7 tablo** |

Türetilmiş sayı doğru kaldı, **iddia ve başlık** onunla birlikte kaymadı — o
yüzden ikisi de elle güncellendi. **Kural artık üçlü:** *"hangi testler
kırıldı?"* · *"hangi testlerin SINIFI değişti?"* · *"hangi sınıfın son örneği
kayboldu?"*

## 6. `0010`un `down`u — sıra bir tercih DEĞİL

`0009`un üç tablosu birbirine bakmıyordu ve o dosyanın başlığı sırayı
*"okunabilirlik tercihi"* diye yazıyordu. `0010` **iki katmanlı iki zincir**
getiriyor:

```
staff_attributes  → staff
manager_attributes → managers
```

CASCADE'siz `DROP TABLE` kendisine bakan bir FK varken **reddedilir**, yani
uydular önce düşmek **zorunda**. `CASCADE` yazılmadı: bu migration'ın
yaratmadığı nesneleri de sessizce götürürdü (3.2b'nin *"fazla giden down"*
sınıfı). `staff` ve `managers` birbirine bakmıyor, yani iki zincirin göreli
sırası serbest — bu da başlıkta yazılı.

## 7. Envanter sayıları — hepsi ÖLÇÜLDÜ

| Sabit | Önce | **Sonra** | Nasıl |
|---|---|---|---|
| `ALL_TABLES` / `MASTER_TABLE_COUNT` | 18 | **22** | liste + sayı **ayrı ayrı** iddia ediliyor |
| `EXPECTED_TABLE_COUNT` | 18 | **22** | ER nöbetçisi |
| `EXPECTED_FOREIGN_KEY_COUNT` | 26 | **32** | ER nöbetçisi |
| `COMPARED_FACTS_FLOOR` | 3.570 | **4.205** | sınır `9_999_999`a kondu, **testin reddettiği çıktıdan** okundu |
| FK envanteri (adıyla) | 21 | **32** | `pg_constraint` |
| Nullable FK | 8 | **9** | `staff_club_id` + `managers_club_id` |
| `SET NULL` alan FK | 3 | **5** | dalın 4. ve 5. vakaları |
| Sequence | 12 | **14** | dört tablodan **ikisi** `serial id` aldı |
| `SEEDED_ROWS` | 15 tablo | **19 tablo** | `people` 6 → 9 |
| Zincir uzunluğu | 10 | **11** | `world-seed.itest.ts` |
| Seed hacmi zemini | 18 tablo | **22 tablo** | `seed-query-performance.itest.ts` |

⚠️ **ER bloğu elle düzenlenmedi** — testin ürettiği metin programatik olarak
yerine kondu (419 satır, `mermaid` çiti içinde). **Mermaid dışı prose sayıları
da** güncellendi (başlık, iddia tablosu, izleme notu) — 4.4'ün bulduğu, 4.6'da
tekrarlanan sınıf.

## 8. Mutasyon — pay SABİT (27), ve sebebi ÖLÇÜLDÜ

| Alt görev | Şema | Kırılan / toplam | Oran |
|---|---|---|---|
| 4.5 | 15 tablo | 26 / 216 | %12,0 |
| 4.6 | 18 tablo | **27 / 230** | %11,7 |
| **4.7** | **22 tablo** | **27 / 241** | **%11,2** |

**Alarm *"sabit pay"* değil, *"AÇIKLANAMAYAN sabit pay"*.** Sebep sayılarak
gösterildi: `0010`un getirdiği **yedi SQL tipinin yedisi de** zincirde zaten
vardı (`serial` 12 · `integer` 63 · `text` 46 · `smallint` 83 · `boolean` **2** ·
`text[]` **1** · `timestamptz` 36 önceki eşleşme), **0 bileşik PK**, **0 indeks**.
Üç CHECK `constraint.definition` olgusunu kullanıyor ve onun negatif testi
4.5'te yazılmıştı.

Yani karşılaştırıcının okuduğu alan listesinin genişlemesi **gerekmedi** ve
yazılacak yeni bir negatif test yoktu. **Sayıyı hareket ettirmek için test
uydurulmadı.**

⚠️ Ve `0010`un yeni çevrim testi paya **katkı yapmadı** — `differences: []`
iddia ediyor, yani **boş** bir envanter; körelen karşılaştırıcı tam olarak onu
üretiyor. §11.5'in başındaki ölçümün dördüncü tekrarı.

⚠️ **Serinin kendisi bayatlamıştı:** `spec/09` §11.5'te **4.6 satırı yoktu**
(dosyada `4.6` → 0 eşleşme) — ölçüm yapılmış, ANLIK DURUM'a ve rapora yazılmış,
ama serinin yaşadığı yere işlenmemişti. İki satır birlikte eklendi ve
`OUTPUT-FORMAT`a *"mutasyon serisi §11.5'e işlendi mi?"* **kontrol adımı** yazıldı.

## 9. Kapsam

| | 4.6 | **4.7** |
|---|---|---|
| satır | %86,49 | **%85,86** |
| ifade | %86,47 | **%85,76** |
| **fonksiyon** | **%75,91** | **%73,60** |
| dal | %88,64 | **%88,64** |

Sebep `coverage-summary.json`dan **dosya bazında** okundu: `staff.ts` **0/5**,
`managers.ts` **0/5**, `staff-attributes.ts` **0/1**, `manager-attributes.ts`
**0/1** → **12 kapsanmayan ok fonksiyonu**. Toplam **290/394**.

ℹ️ Tahmin −1,5…−2 puandı, gerçek **−2,31**: `staff.ts` beklenenden bir fonksiyon
fazla getirdi (yerel `literals` yardımcısı sayılmamıştı). **Eşik %70
düşürülmedi, dosya dışlanmadı, import testi yazılmadı.** Marj **3,60 puan**.

## 10. D5 — 25/25

Derlenmiş `dist`, düz `node`, testcontainers yok, ayrı veritabanı `fms_d5_47`,
gerçek PostgreSQL 18.6. Zincirin on bir adımı uygulandı. Ölçülenler: 22 tablo ·
32 FK · katalogdan sütun sayıları (6 · 19 · 12 · 11) · derlenmiş sabitler
(12 · 16 · 8 · 5 · 5) · **`managers.user_id` yok** · **POZİTİF** aynı kişiye iki
personel satırı · **NEGATİF** 13. rol ve 6. rozet reddedildi · **POZİTİF**
`philosophy` kısıtsız · `spoken_languages` `{tr,en}` döndü · **CASCADE** kişi →
personel/menajer/nitelikler (iki kademe) · **SET NULL** iki yönlü · `0010`
çevrimi birebir aynı, `comparedFacts=4205` **bağımsız doğrulandı**.

⚠️ D5 fixture kullanmıyor, ham SQL yazıyor — `clubs` sütun adları **hafızadan**
yazılınca gürültülü patladı (`column "tla" … does not exist`) ve
`information_schema`dan okunarak düzeltildi. Entegrasyon testleri bu tuzağa
düşmüyor çünkü `clubInsertSql` fixture'ını kullanıyorlar (günlük #28'in kararı).

## 11. §0.5 KONTROL NOKTASI — ilk gerçek sonucu

| Ölçüm anı | Geçen süre | Eşik (2 gün) |
|---|---|---|
| 4.7'nin **başı** (2026-08-31 03:34 +03) | **1,941 gün** | aşılmadı |
| 4.7'nin **kapanışı** (2026-08-31 20:11 +03) | **2,633 gün** | ✅ **AŞILDI** |

Kaynak `git show -s`: Faz 4'ün ilk commit'i `0682c5f` (2026-08-29 05:00:19 +0300).
§0.5'in asıl sınırı **3 gün** ve ona hâlâ mesafe var, ama kontrol noktasının
eşiği **2**.

**Kural uygulandı:** **4a = 4.0→4.7 (kapandı)** · **4b = 4.8→4.11**. Çizgi
ROADMAP'te zaten hazırdı. Kapsam, sıra ve kabul kriterleri **değişmedi**; tek PR
ve tek faz kaydı yine 4.11'de, bölünme faz kaydının §1'inde görünecek.

⚠️ **Faz 3'te aynı kural ateşledi (4 gün, sınır 3) ve HİÇBİR ŞEY OLMADI** —
çünkü süreyi ölçen bir adım yoktu. Adım 4.1'de `SESSION-TEMPLATE`e eklendi
(SAPMA-033) ve **bu ilk kez gerçekten bir kaydı değiştirdi**. Tahmin listesi bu
fazı işaretlememişti; yakalayan şey **ölçüm** oldu.

## 12. Kancanın kablolaması — ÜÇ YÖNLÜ doğrulandı (4.6'nın açık kalemi)

| Deneme | Taşıyıcı | Yük | Sonuç |
|---|---|---|---|
| heredoc + Türkçe + ters tırnak + `§`/`→` | `<<` | ters tırnak, ASCII olmayan bayt | 🛑 **ENGELLENDİ**, dosya **oluşmadı** |
| `node -e "…Türkçe…"` | `node -e` | ASCII olmayan bayt | 🛑 **ENGELLENDİ** |
| saf ASCII SQL heredoc | `<<` | (yok) | ✅ **GEÇTİ** |

Üçüncüsü olmasaydı ilk ikisi hiçbir şey kanıtlamazdı (kanca her şeyi kilitliyor
olabilirdi — D3). ⚠️ Ve kanca bu alt görevde **gerçek işe yaradı**:
`schema-constraints.itest.ts`e bölüm işareti eklerken saf ASCII bir heredoc
bilerek kullanıldı ve geçti, Türkçe içerik `Edit` ile yazıldı.

## 13. Kapsam sınırı (K12) — yapılmayanlar ve sebepleri

| Yapılmadı | Sebep | Yeri |
|---|---|---|
| İndeksler | kapsam | **4.8** |
| Seed | kapsam | **4.9** |
| Kriter 3 ölçümü | kapsam | **4.10** |
| ER + faz kaydı + PR | kapsam | **4.11** |
| `staff_roles` tablosu | 4.1'in kararı, **gerekçesi denetlendi** | açılmıyor |
| `managers.user_id` | SAPMA-032 / G-16 | **Faz 13** |
| Markalı kimlik tipleri | G-17 | **Faz 12** |
| CHECK yardımcısının ortaklaştırılması | altı dosyaya dokunur | **BORÇ-008 → 4.11** |
| Sarmalayıcının `runner.itest.ts`e taşınması | dosyalar arası kurulum paylaşımı | uyarı o dosyanın başlığında |
