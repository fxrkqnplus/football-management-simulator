> **Alt görev:** 4.0 — Faz 4 açılışı: doğrulama, üç süreç boşluğu, iki ölçüm
> **Tarih:** 2026-08-29 · **Dal:** feature/faz-04-schema-ii
> **İçerik commit'i:** 0682c5f · **CI koşusu:** ÖLÇÜLECEK (push sonrası) · **PR:** yok (faz sonunda)
> **Bağlam yüzdesi:** ölçülemedi — `/context` çıktısı bu turda mevcut değil

## 🟡 KARAR 4.0 — Faz 4 açılışı: doğrulama, üç süreç boşluğu, iki ölçüm

**Ne yapıldı:** Faz 3'ün merge'ü ve tüm kapılar doğrulandı, `docs/SPEC-COVERAGE-GAPS.md`'nin okuyucusuzluğu ölçülüp kapatıldı, iki spec çelişkisi karara bağlandı (SAPMA-028/029) ve Faz 4'ün iki en pahalı kararı (**tablo envanteri** ve **FK kuralı**) SQL yazılmadan ölçüldü.

**Kapılar:** typecheck [x] · lint [x] · **format — KAPSAM DIŞI** · test [x] · build [x] · arch [x] · test:db [x]
**Commit:** `0682c5f` · push [ ] · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- typecheck → `pnpm typecheck` → **10/10 görev, 0 hata**
- lint → `pnpm lint` → **0 uyarı**
- format → **`✅` YAZILMADI.** Negatif test: bilerek bozulmuş bir `.md` repoya kondu → `pnpm format:check` yine *"All matched files…"* + **exit 0**; aynı dosya `--ignore-path /dev/null` ile **yakalandı**. Bu commit'in 7 dosyasının 7'si `.md` → kapı **hiçbirine bakmadı** (SAPMA-024)
- test → `pnpm test` → **744 test / 52 dosya**; `pnpm test:db` → **163 test / 8 dosya** (gerçek PG 18.6, 31,20 s)
- kapsam → **satır %86,99 · ifade %87,04 · dal %88,68 · fonksiyon %80,00** — Faz 3 kapanışıyla birebir aynı (4.0 kod yazmadı)
- build → `.turbo/cache` **silinip** `pnpm build` → **8/8, `Cached: 0`, 5,627 s**
- arch → `pnpm arch:check` temiz; **kural sayısı `rule:` belirteçlerinden sayıldı → 9** (varsayılmadı)
- CI → **liste sorgusuyla sıfırdan çıkarıldı**, elle artırılmadı (aşağıda)

**KARARIN GEREKİYOR:** 4 madde — ADIM 4 planında sunuldu (tablo envanteri · FK kuralı · §3.1.0 sütunları · seed tohumu)

**Yeni kayıt:** SAPMA-028 (`karar`) · SAPMA-029 (`düzeltme`) · G-07…G-14 `Durum` ✅ · çalışma günlüğü #1, #2

**Sıradaki:** 4.1 — plan onaylandıktan sonra ROADMAP'e yazılır, sonra ilk alt görev.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. Ortam ve merge doğrulaması

| Kontrol | Beklenen | Ölçülen | |
|---|---|---|---|
| `node -v` / `pnpm -v` | v24.19.0 / 11.23.0 | aynı | ✅ |
| dal / ağaç | develop / temiz | aynı | ✅ |
| tepe commit | "Merge pull request #4" | **merge commit, 2 ebeveyn** (`9619dfa` + `7916ac2`); `gh` → PR #4 `MERGED` | ✅ metin farkı |
| docker | PG18 + Redis healthy | ikisi de **healthy** | ✅ |

Merge özel bir mesajla yapılmış; GitHub'ın varsayılan metnini taşımıyor. **Öz doğrulandı, DUR gerekmedi.**

## 2. CI — sıfırdan yeniden çıkarıldı

`feature/faz-03-database`: **34 koşu** → `push` **28** (25 ✅ · 2 ❌ · 1 iptal) + `pull_request` **6** (5 ✅ · 1 iptal). İki kırmızının ikisi de (`074e373`, `bd59fac`) `1c93890`'dan **önce**. İptal edilen `9579c3f`'in iki koşusu **sayaca katılmadı** — iptal edilen koşu ne yeşil ne kırmızıdır.

**`1c93890`'dan beri 18 ardışık yeşil push koşusu.** 3.10 `4a0eb43`'te "on dört" demişti ve o bu listede **tam 14. sırada** — yöntem birebir yeniden üretildi.

🆕 **Faz 3'ün "kaydedilemeyen son halka" notu KAPANDI.** Kaydedilemeyen üç commit (`bd53b60`, `0763e99`, `7916ac2`) üçü de yeşil; `develop`'ta merge koşusu **`33226162769`** (`b24c535`) → **6/6 iş yeşil, amd64 + arm64** (K14). `develop`'ın 4 push koşusunun dördü de yeşil.

## 3. Üç süreç boşluğu — ölçüm ve düzeltme

### ① Kütüğün okuyucusu yoktu

```
grep -n "SPEC-COVERAGE" CLAUDE.md docs/SESSION-TEMPLATE.md   -> boş (exit 1)
grep -n "G-09|…|G-14" docs/ROADMAP.md   -> 8 eşleşme, HEPSİ Faz 3 bölümünde (1153–1499)
G-07 -> ROADMAP'te hiç geçmiyor
```

Karşılaştırma: **G-01…G-06 doğru işlenmişti** (Faz 6·17·47·49·50). Desen Tarama 1'de kuruldu, Tarama 3–7'de unutuldu — bir okuyucunun yokluğunda beklenen şey. SAPMA-008'in bu kütüğü yarattığı sınıfın **üçüncü** tekrarı.

Yapılan **iki iş** (biri diğerinin yerine geçmez): (a) yedi satır atandıkları fazın ROADMAP kapsamına yazıldı — G-09+G-14 → Faz 7, G-10+G-11+G-12 → Faz 11 (tek blok, aynı sınıf), G-13 → Faz 17, G-07 → Faz 50; G-13 ve G-07 birer **kabul kriteri** de aldı; satırlar **silinmedi**, `Durum` güncellendi. (b) Kütüğe **iki okuyucu** bağlandı: `CLAUDE.md` (belge haritası + "Her Oturumun İlk İşi") ve `SESSION-TEMPLATE` (açılış adım 4, kapanış adım 20).

⚠️ **G-11 ROADMAP'e yazıldı ama `Durum`u kapanmadı** — o Faz 11'in doğrulayıcısında kapanır; ROADMAP'e yazılması yalnızca *"kimsenin işi değil"* durumunu bitirir.

**Aynı sınıftan ikinci vaka, bu ölçüm sırasında bulundu:** `docs/DEPENDENCY-WATCH.md` kendi başlığında *"Her faz açılışında bu tablo kontrol edilir (`SESSION-TEMPLATE` ÖN KONTROL)"* diyordu; şablonda o satır **yoktu** (`grep` boş). Şablona adım 7 olarak eklendi. **Bir belgenin "beni şurası okur" demesi, orada okunduğunu göstermiyor.**

### ② Nitelik CHECK'i — SAPMA-028 (`karar`)

⚠️ **Prompt yalnızca ROADMAP'i işaret ediyordu; ölçüm çelişkinin bir KAYNAK belgenin içinde de olduğunu gösterdi:** `docs/spec/01-database.md` §3.1'in `player_attributes` bloğu `// CHECK: her sütun 1-20 arasında` taşıyor ve **kendi §3.1.2 ②'siyle** çelişiyor. Yani düzeltme ROADMAP'le sınırlı kalsaydı, çelişki kaynakta kalırdı.

Ayraç §3.1.2 ②: *"bu değeri yarın bir denge ayarı değiştirebilir mi?"* Ölçülmüş emsal: 3.6'da altı hakem niteliği (1-20) CHECK **almadı**; `competitions.reputation` ve `stadiums.pitch_quality` da almadı. → **aralıklar CHECK ALMAZ**, denetim Faz 11. **`CA <= PA` ve `pa_range_min <= pa_range_max` CHECK ALIR** — ilişki değişmezi, kriter değişmedi.

ℹ️ Aynı ölçüm bir yan bulgu daha verdi: spec'in `INDEX: (primaryPosition, currentAbility), (finishing), (passing), (pace)` satırı **iki tabloyu karıştırıyor** — ilk iki sütun `players`'ta, son üçü `player_attributes`'ta. Yorum olarak işaretlendi, dağılım Faz 4'te kararlaştırılacak.

### ③ `source` kümesi — SAPMA-029 (`düzeltme`)

`spec/12` §17.1 ve ROADMAP Faz 7 **kabul kriteri** dört değer sayıyordu. Çelişki **§17.1'in kendi içinden** ölçüldü: aynı bölüm on beş satır yukarıda **beş** sağlayıcı listeliyor ve dördüncüsü `OpenFootballProvider`. Kod tarafı (`DATA_SOURCES`, CHECK kısıtlı) **beş**. Yanlış olan metindi. SAPMA-023'ün tamamlanmamış yayılımı; iki yer de düzeltildi.

ℹ️ Ayrıca hafızadaki bir boşluk kapandı: `PROJECT_MEMORY.md` 3.8'in seed'inin `source`a hangi değeri yazdığını taşımıyordu. **Ölçüldü: `procedural`** (`world-seed-data.ts` başlığı + G-14).

## 4. Faz 4 ölçümü ① — tablo envanteri

ROADMAP Faz 4 **19 tablo** sayıyor. `spec/01`'e karşı ölçüldü:

| | Sayı | Tablolar |
|---|---|---|
| **Master §3.1** | **11** | `people` · `players` · `player_attributes` · `player_hidden_attributes` · `player_positions` · `player_traits` · **`player_stats_history`** · `staff` · `staff_attributes` · `managers` · `manager_attributes` |
| **Save §3.2** | **3** | `contracts` · `contract_clauses` · `injuries` |
| **`spec/01`'de HİÇ YOK** | **7** | `player_personalities` · `player_relationships` · `player_career_history` · `player_injuries` · `injury_types` · `staff_roles` · `manager_career` |

Ad ayrışması doğrulandı: ROADMAP `player_career_history` ↔ spec `player_stats_history`; ROADMAP `player_injuries`+`injury_types` ↔ spec `injuries`.

⚠️ **Ölçüm aracı bir kez yanlış cevap verdi (D2):** ilk regex `^(\w+): \{$` satır sonu istiyordu ve `player_traits` **tek satırlık** yazıldığı için kaçırıldı → master 10 göründü. Gevşek regexle 11 çıktı. *Bir envanteri sayan aracın kendisi de sayılmalı.*

Tüketici aramasının sonucu ADIM 4'te tablo hâlinde.

## 5. Faz 4 ölçümü ② — FK kuralı, kâğıt üstünde

Kural değiştirilmeden, **derlenmiş `dist`ten** (D5) planlanan 20 FK'ya karşı koşuldu. Sonra dört varyant iki veri kümesine karşı ölçüldü: **A** = Faz 3'ün 12 gerçek FK'sı (migration SQL'inden — katalogu kuran temsil), **B** = Faz 4'ün 20 planlananı.

| Varyant | A (regresyon) | B | veri kaybettiren |
|---|---|---|---|
| **V0** bugünkü kural | **12/12 ✅** | **13/20** | **7** |
| V1 `nullable` denetimi en başta | **9/12 ⛔** | 19/20 | 1 |
| V2 `nullable`, `independent` sonrası | **12/12 ✅** | 19/20 | 1 |
| **V3 = V2 + `people` `key` taşır** | **12/12 ✅** | **20/20 ✅** | **0** |
| V4 = V2 + `players` `key` taşır | 12/12 ✅ | 17/20 | 1 |
| V5 = V2 + ikisi de | 12/12 ✅ | 18/20 | 0 |

**Üç sonuç:**

1. **Bugünkü kural Faz 4'te 7 veri kaybettiren cevap üretiyor** — en tehlikelisi `people.nationality_country_id` → CASCADE: *bir ülke silinince o uyruktaki herkes silinir*. Faz 3'te 12/12 çıkmasının sebebi tesadüftü: **12 FK'nın hepsinde bir uydunun FK'sı sahibini gösteriyordu.**
2. **Açık görünen düzeltme (V1) YANLIŞ** — Faz 3'ün üç FK'sını bozuyor (`competitions.country_id`, `clubs.competition_id`, `clubs.stadium_id`). Bu **F3**'ün yeni bir örneği: *"nullable ⇒ SET NULL"* ayracı örneklerden geriye okununca doğru görünüyor, ölçülünce değil. Sıra bağlayıcı: `independent` denetimi `nullable`'dan **önce** gelmeli.
3. **① ile ② tek kararın iki yüzü, ve bu artık ölçülmüş:** `key`i `people` taşırsa 20/20, `players` taşırsa **17/20**. Ayrı verilemez.

`expectedDeleteAction` bugün **`SET NULL` DÖNDÜREMİYOR** — ölçüldü: `DELETE_ACTIONS` dört değer tanımlıyor, fonksiyon yalnızca `['CASCADE','RESTRICT']` üretiyor. V3'ün gerektirdiği 7 `SET NULL` cevabının hiçbiri bugünkü koddan çıkamaz.

## 6. Değişen dosyalar

```
CLAUDE.md                     [DEĞİŞTİ] belge haritası + "Her Oturumun İlk İşi" 4. madde
docs/SESSION-TEMPLATE.md      [DEĞİŞTİ] açılış adım 4 (kütük) + ÖN KONTROL adım 7 (DEPENDENCY-WATCH)
                                        + kapanış adım 20 · numaralandırma 1–23'e yeniden dizildi
docs/SPEC-COVERAGE-GAPS.md    [DEĞİŞTİ] 7 satırın Durum'u · "Kim okur" bloğu · Tarama 8 · Kural 5
docs/ROADMAP.md               [DEĞİŞTİ] Faz 4 (SAPMA-028) · Faz 7 (G-09, G-14, source) · Faz 11
                                        (G-10/11/12 + CHECK notu) · Faz 17 (G-13) · Faz 50 (G-07)
docs/spec/01-database.md      [DEĞİŞTİ] §3.1 player_attributes CHECK yorumu (SAPMA-028)
docs/spec/12-data-packs.md    [DEĞİŞTİ] §17.1 source kümesi dört → beş (SAPMA-029)
PROJECT_MEMORY.md             [DEĞİŞTİ] ANLIK DURUM · SAPMA-028/029 · SAPMA-023 notu
                                        · BORÇ-001/002/004 notu · Faz 4 çalışma günlüğü
```

## 7. Kapılar hakkında dürüst not

Bu commit **yalnızca Markdown** değiştiriyor. `format:check` `.md`'ye bakmıyor (kanıtlandı); `typecheck`, `lint`, `test`, `build`, `arch:check` koştu ama **bu commit'in yüzeyine dokunmadılar**. Değişikliği denetleyen tek şey **ölçümlerin kendisi**: her boşluk önce `grep` ile gösterildi, sonra kapatıldı, sonra kapandığı yeniden `grep`le doğrulandı (`⏳` satır sayısı 6 → 0; G-xx atıfları Faz 3'ten Faz 7/11/17/50'ye taşındı).
