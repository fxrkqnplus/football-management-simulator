> **Alt görev:** 4.0b — CI işlendi, süreç boşluğu kapatıldı, kayıp plan yeniden üretildi
> **Tarih:** 2026-08-29 · **Dal:** feature/faz-04-schema-ii
> **İçerik commit'i:** 0cfb527 · **CI koşusu:** ÖLÇÜLECEK (push sonrası) · **PR:** yok (faz sonunda)
> **Bağlam yüzdesi:** ölçülemedi — `/context` çıktısı bu turda mevcut değil

## 🟡 KARAR 4.0b — CI işlendi, süreç boşluğu kapatıldı, plan yeniden üretildi

**Ne yapıldı:** 4.0'ın CI koşusu liste sorgusuyla işlendi, kaybolan planın bir daha kaybolmaması için `docs/OUTPUT-FORMAT.md`'ye kural yazıldı, ve Faz 4'ün alt görev listesi + yedi tablonun tüketici araması + altı karar yeniden üretildi (pahalı ölçümler **tekrarlanmadı**, 4.0 arşivinden okundu).

**Kapılar:** typecheck [—] · lint [—] · **format — KAPSAM DIŞI** · test [—] · build [—] · arch [—]
**Commit:** `0cfb527` (kural) + kayıt commit'i · push [ ] · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- CI → `gh run list --branch feature/faz-04-schema-ii` → **tek koşu** `33228266356` (`6063544`, `push`) → `gh run view` → **6/6 iş yeşil, amd64 + arm64**. `pull_request` olayı yok, iptal yok
- format/lint/test/build/arch → **koşturulmadı ve `✅` yazılmadı.** Bu alt görev yalnızca 3 Markdown dosyası değiştirdi; `format:check` `.md`'ye bakmıyor (4.0'da negatif testle kanıtlandı), diğer kapıların bu commit'te bakacağı dosya yok (SAPMA-024). `pnpm format:check` yine de koşuldu ve **exit 0** verdi — bu bir onay değil, bir kayıt
- Tüketici araması → yedi tablo için `docs/spec/**` + `docs/ROADMAP.md`'de **işlev** araması (ad değil) → DETAY §2
- `SeededRng` → **ölçüldü: YOK.** `packages/engine/src/` 4 dosya taşıyor (`index.ts` + 3 test); ROADMAP `SeededRng`i **Faz 22**'ye atıyor

**KARARIN GEREKİYOR:** 6 madde → **DETAY §3**. Altısı da aşağıda tam metinleriyle; onay gelmeden ROADMAP'e yazılmayacak (K11)

**Yeni kayıt:** çalışma günlüğü **#3** (F2'nin sekizinci vakası — `node -e` ters tırnak) ve **#4** (kabul kriteri 3 bugünkü şemayla ölçülemez) · iki kayıt düzeltmesi

**Sıradaki:** Onay → alt görev listesi ROADMAP'e yazılır → **4.1** (kararlar ve envanter mutabakatı, kod yok).

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. Bu alt görevde çıkan iki hata

**#3 — F2'nin SEKİZİNCİ vakası, ve kural yazılıyken ihlal edildi.**
`PROJECT_MEMORY.md`'nin üç ANLIK DURUM satırı `node -e "…"` ile değiştirildi; metindeki her ters tırnaklı parça (`` `docs/ROADMAP.md` ``, CI koşu numaraları, dal ve iş adları) **sessizce boşaldı** — `→ **6/6 iş yeşil** ( · · , her biri…)`. Betik `degistirilen satir: 3` diyerek **başarı raporladı**; hasarı yalnızca stderr'deki `command not found` satırları ele verdi. Hiçbir kapı ötmedi. Ayrıca komut ikamesi iki **0 baytlık artık dosya** yarattı (`sıralama`, `Zorunlu.`) — ikisi de izlenmiyordu, içerikleri görüldükten sonra silindi.

Kural zaten yazılıydı (ORTAM TUZAKLARI ⑤, 3.10'un yedinci vakası): *"Markdown metni kabuktan hiç geçmez."* Kapsamı netleşti: **`python -c`, `node -e`, heredoc ve tırnaklı dize aynı sınıftır** — hiçbiri kaçış yolu değil. Onarım `Edit` ile yapıldı, ters tırnak dengesi ve boş inline kod programatik doğrulandı.

**Asıl ders:** bir kuralın yazılı olması, **yeni bir araçta** hatırlanacağı anlamına gelmiyor. Sekiz vakanın her biri farklı bir taşıyıcıyla geldi.

**#4 — kabul kriteri 3 bugünkü şemayla ölçülemez.** Aşağıda Karar 5.

## 2. Yedi tablo için tüketici araması — `competition_seasons` yöntemi

Aranan yerler her satırda yazılı. ⚠️ **D7 uygulandı:** `spec/01` §3.1.2 ⑧'in *"Faz 4'te gelecek `injury_types`, `staff_roles`"* cümlesi ve ROADMAP:1123'ün `manager_career` atfı **bizim kendi sesimiz** (Faz 3'te yazıldı) — tüketici sayılmadılar, kaynaktan yeniden arandı.

| # | Tablo | Nerede arandı | Bulunan tüketici | Karar |
|---|---|---|---|---|
| 1 | `player_personalities` | `spec/02` §4.6 · `spec/06` §8.1–8.6 · ROADMAP Faz 10/11/19/44 | **YOK — spec AKTİF OLARAK KARŞI.** `spec/02` §4.6: *"Kişilik **saklanmaz, türetilir**. `derivePersonality(hidden): PersonalityCode`"*. ROADMAP Faz 10: *"Kişilik türetimi: gizli niteliklerden 25+ kişilik etiketi"*. Her tüketici (diyalog `personalityToneMatrix`, gelişim, moral, transfer) `personality`'yi **türetilmiş değer** olarak alıyor | ❌ **AÇILMAZ** |
| 2 | `player_relationships` | `spec/06` §8.6 · ROADMAP Faz 38 (Mentorluk S111) / Faz 44 | **YOK.** `affinity(a,b)` bir **formül** (`nationalityMatch×0.30 + languageMatch×0.25 + ageProximity×0.20 + personalityMatch×0.15 + tenureOverlap×0.10`), saklanan bir kenar listesi değil; klikler `affinity > 0.62` ile **hesaplanıyor**. Faz 38 Mentorluk bir **atama** — oyun oynanırken doğar, save-scoped | ❌ **AÇILMAZ** (Faz 4'te). Kalıcı mentor ataması gerekirse Faz 38'in save katmanı işi |
| 3 | `player_career_history` | `spec/01` §3.1 · ROADMAP Faz 10/19/47 | **VAR ama tablo ZATEN MEVCUT:** `player_stats_history` aynı işi yapıyor (sezon × turnuva, nitelik türetiminin girdisi, Faz 10 `StatsProvider`). ⚠️ **Ama bir eksik ölçüldü: `club_id` YOK (0 eşleşme).** Faz 19 *"İstatistikler — sezon / kariyer / turnuva bazlı"* ve Faz 47 *"kariyer geçmişi (her kulüp, süre, istatistik)"* onsuz cevaplanamaz | 🔁 **AD ÇAKIŞMASI — spec adı kazanır.** `player_stats_history` açılır **+ `club_id` eklenir** (SAPMA-026 sınıfı: spec'in kendi yazımı eksik) |
| 4 | `player_injuries` | `spec/01` §3.2 · ROADMAP Faz 39 | **VAR** ama tablo `injuries` adıyla ve **§3.2 save katmanında** (`saveId FK`) | 🔁 **AD ÇAKIŞMASI + save katmanı → FAZ 12** |
| 5 | `injury_types` | ROADMAP Faz 39 · `spec/01` §3.2 `injuries.injuryTypeCode` | **VAR:** Faz 39 *"Sakatlık tipolojisi (~40 tür): kas zorlanması (1–2 hf), hamstring (2–6 hf), … ön çapraz bağ (24–40 hf)"* + kabul kriteri *"40 sakatlık türünün tamamı tetiklenebiliyor"*. Satırlar **veri taşıyor** (süre aralığı, ciddiyet) → gerçek **sözlük tablosu**, CHECK değil | ⚠️ **FAZ 12** — `injuries` ile birlikte. Gerekçe aşağıda |
| 6 | `staff_roles` | `spec/01` §3.1 `staff.role` · ROADMAP Faz 37 | **Tüketici var, TABLO gerekmiyor.** `spec/01` `staff.role`u **satır içi kapalı küme** olarak yazıyor (12 değer); §3.1.2 ② gereği kapalı küme **CHECK** alır. Faz 37 kriteri *"12 rol de atanabiliyor"* — CHECK bunu sağlıyor. Rol **etkileri** (S164) motor katsayısı, tablo satırı değil (K3: motor veriyi parametre olarak alır) | ❌ **AÇILMAZ** — `staff.role` **CHECK** alır |
| 7 | `manager_career` | ROADMAP Faz 47 (S204, S207) | **VAR ve kaynaktan doğrulandı:** Faz 47 kapsamı *"Menajer profil sayfası (S207): **kariyer geçmişi (her kulüp, süre, istatistik), kupa vitrini**, en iyi sezonlar…"* + metrikler (S204) *"toplam maç, G/B/M, kupa sayısı, sezon sayısı"* + kabul kriteri *"Menajer profil sayfası tüm veriyi gösteriyor"* | ⚠️ **FAZ 12** — kariyer oyun oynanırken birikir, save-scoped. Gerekçe aşağıda |

**⚠️ `injury_types` neden Faz 4 değil — ölçülebilir gerekçe.** Tek FK kaynağı `injuries` ve o Faz 12'de. `fk-policy.ts`'in sözlük kuralı **yalnızca tabloyu HEDEFLEYEN FK'lar için** cevap üretir (`expectedDeleteAction` önce `classOf(targetTable) === 'dictionary'` diye bakar). `injuries` yokken `injury_types`'a giden **hiçbir FK yok**, yani onu Faz 4'te açmak, kendisi için yazılmış kuralı bile **çalıştırmıyor**. Boş, kimsenin göstermediği bir sözlük tablosu `competition_seasons`'ın tarifidir (SAPMA-021, D3).

**⚠️ `manager_career` neden Faz 4 değil.** `managers` master (§3.1) ama kariyer **kayıt başına** birikir: kaç maç, hangi kupa, hangi sezon. Faz 47 liderlik tablosu bunu kayıtlar arası topluyor. Master dünyada AI menajerlerin **önceden yüklenmiş** bir kariyeri isteniyor olsaydı Faz 8/9 ingest kapsamında geçerdi — geçmiyor (arandı). → save-scoped.

**Sonuç — 19 tablonun tam muhasebesi:** **11** Faz 4'e girer (master §3.1) · **5** Faz 12'ye taşınır (`contracts`, `contract_clauses`, `injuries`, `injury_types`, `manager_career`) · **3** açılmaz (`player_personalities`, `player_relationships`, `staff_roles`) · ve iki ad çakışması mevcut tablolara katlanır (`player_career_history` → `player_stats_history`, `player_injuries` → `injuries`).

## 3. ONAY BEKLEYEN ALTI KARAR

### Karar 1 — Tablo envanteri: 19 → 11 master

Faz 4 **11 master tablo** açar: `people` · `players` · `player_attributes` · `player_hidden_attributes` · `player_positions` · `player_traits` · `player_stats_history` (+`club_id`) · `staff` · `staff_attributes` · `managers` · `manager_attributes`.

⚠️ **Kapsam taşıması kütüğe yazmakla bitmez** (4.0'ın ① dersi): beş tablo ROADMAP Faz 4'ten **çıkarılıp Faz 12 kapsamına yazılır**, üçünün açılmama gerekçesi ROADMAP Faz 4'e yazılır, iki ad çakışması ROADMAP'te düzeltilir (`player_career_history` geçen **iki** satır dahil — bölümleme notu :1715). Bir **SAPMA** açılır (SAPMA-021'in üç katı).

### Karar 2 — FK kuralı: **V3**

4.0'ın ölçümü (arşiv §5, tekrarlanmadı): V3 tek varyant ki hem Faz 3'ün 12 gerçek FK'sında **12/12** (regresyon yok) hem Faz 4'ün 20 planlananında **20/20**, **0 veri kaybettiren cevap**.

```
① hedef dictionary                    → RESTRICT      (değişmiyor)
② kaynak independent                  → RESTRICT      (değişmiyor)
③ FK sütunu NULLABLE                  → SET NULL      ← YENİ, üçüncü olgu
④ kaynak satellite (NOT NULL)         → CASCADE       (değişmiyor)
```

**Sıra bağlayıcı ve bu ölçülerek bulundu:** ③'ü ①'in hemen ardına koymak (V1) Faz 3'ün **üç** FK'sını bozuyor (`competitions.country_id`, `clubs.competition_id`, `clubs.stadium_id` → hepsi nullable ve hepsi `RESTRICT`). Açık görünen düzeltme yanlış — **F3**'ün yeni bir örneği.

`expectedDeleteAction` bugün **`SET NULL` DÖNDÜREMİYOR** (`DELETE_ACTIONS` onu tanımlıyor, fonksiyon hiçbir dalda üretmiyor); V3'ün gerektirdiği 7 `SET NULL` cevabının hiçbiri bugünkü koddan çıkamaz. Üçüncü olgu (`is_nullable`) katalogdan okunur — kuralın zaten kullandığı kaynak.

### Karar 3 — §3.1.0 sütunları: **`people` taşır, `players` TAŞIMAZ**

Karar 2 ile **aynı kararın iki yüzü** ve fark ölçülmüş: `key`i `people` taşırsa **20/20**, `players` taşırsa **17/20** (`players` independent olunca `players.club_id` `SET NULL` yerine `RESTRICT`, `players.person_id` `CASCADE` yerine `RESTRICT` alıyor). İkisi birden taşırsa 18/20.

`people` `key` · `source` · `external_ids` alır. Gerekçe SAPMA-023'ün aynısı ve **daha pahalı**: sonradan eklemek `ALTER TABLE` + **5.000 satırlık seed'in yeniden yazımı** demek. `spec/12` §17.4 `players.json` zaten `key` ve `externalIds` taşıyor — pakette kendi kaydı olan varlık **kişidir** (aynı kişi oyuncu, sonra menajer olabilir; `players.person_id` UNIQUE).

ℹ️ Takip notu Faz 9'a: pakette anahtar `player-12847` biçiminde ve `people.key`e düşecek — adlandırma tutarsızlığı ingest'te ele alınır.

### Karar 4 — Seed tohumu (K2): **veriden türetilen, fixture'a özel deterministik üreteç**

⚠️ **Ölçüm bir varsayımı çürüttü: `SeededRng` YOK.** `packages/engine/src/` yalnızca `index.ts` + 3 test dosyası taşıyor ve ROADMAP `SeededRng`i **Faz 22**'ye (motor çekirdeği) atıyor. Yani "motordan taşıma" seçeneği aslında *"olmayan bir sınıfı bir faz erken, yanlış tüketici için tasarlamak"*.

Faz 4'te tohum isteyen alanlar: `people.portrait_seed` (`spec/01`: *"prosedürel portre tohumu"*), 5.000 oyuncunun nitelik dağılımı, isim üretimi.

**Öneri:** `tools/data-cli/src/seed/` içinde **saf, veriden türeyen** bir üreteç — girdi `(key, purpose)`, çıktı deterministik. `SeededRng` **adı kullanılmaz** ve dosya başlığı Faz 22'nin gerçek RNG'sinin sahibi olduğunu yazar.
- `arch:check` ① korunur (`tools/data-cli` motoru import etmez)
- K12 korunur (mimari değişiklik yok)
- İkisi **farklı şeyler**: `SeededRng` `(saveId, turnNumber, entityId, purpose)` ile anahtarlanıyor ve o kavramlar Faz 12/16'ya kadar yok
- 3.8'in emsali birebir: *"`SeededRng` TAŞINMADI, tohum UYDURULMADI"*

**Elenen:** `SeededRng`i `packages/shared`a koymak — katman kuralı buna izin verir (`tools/*` ve `engine` ikisi de `shared`ı import edebilir) **ama** `CLAUDE.md` §2.2 onu açıkça `packages/engine/src/rng/`e koyuyor; taşımak bir anayasa sapması olur ve Faz 22'nin tasarım kararını bugün, tüketicisi yokken vermek demektir. · Sabit değerler — 5.000 aynı oyuncu **kriter 3'ü öldürür**: seçicilik olmadan plan ölçümü anlamsız (3.9'un dersi).

**Determinizm nasıl kanıtlanır:** iki koşu **birebir aynı** satırları üretmeli. ⚠️ `created_at`/`updated_at` `defaultNow()` — karşılaştırmadan **çıkarılır ve dışlama sessiz olmaz** (3.2b'nin sequence kararıyla aynı biçim, 3.8 uyguladı).

### Karar 5 — Kabul kriteri 3 bugünkü şemayla ÖLÇÜLEMEZ (yeni bulgu)

Kriter: *"20–24 yaş, sağ bek, CA>120, **değer<15M**" sorgusu < 50 ms*.

**Ölçüldü:** `marketValue` **`player_state`** tablosunda ve o `spec/01` **§3.2 save katmanı** — Faz 12. Üstelik türev bir değer: `spec/02` §4.7 onu kalan sözleşme ayı, form, sakatlık cezası, enflasyon endeksiyle hesaplıyor — **hiçbiri master'da yok**, yani master'a bir `market_value` sütunu **konamaz** (koyulsaydı her tur bayatlardı).

**Öneri:** kriter Faz 4'te ölçülebilir biçimine daraltılır —
`[ ] "20–24 yaş, sağ bek, CA>120" sorgusu 5.000 oyuncu hacminde < 50 ms`
— ve `değer<15M` yüklemi **Faz 30** (piyasa değeri motoru) / **Faz 32** (transfer arama) kapsamına yazılır. Bir **SAPMA** açılır. Kriterin *amacı* (bileşik indeks transfer aramasını taşıyor mu) daraltılmış hâliyle korunuyor.

### Karar 6 — `managers.user_id` DÖRDÜNCÜ ileri FK → Faz 13 (yeni bulgu)

`spec/01` §3.1 `managers`i master'a koyuyor ama `userId FK nullable` taşıyor ve `users` **§3.2 save katmanında**, ROADMAP'te **Faz 13**'te (açık kayıt akışı) doğuyor.

Bu, Faz 3'ün üç ileri FK'sıyla **birebir aynı sınıf**: kısıtsız bir sütun *"tüm FK'lar tanımlı"* kriterini görünürde sağlayıp gerçekte deler. **Öneri:** `managers.user_id` Faz 4'te **hiç yazılmaz**; sütun ve kısıt **birlikte Faz 13'te** eklenir, ROADMAP Faz 13 kapsamına yazılır, gerekçe `managers.ts` başlığına konur.

ℹ️ İkinci soru — master bir tablonun save katmanına FK vermesi K4 açısından doğru mu — **Faz 12'nin işi**, burada açılmıyor (K12). Kayıt için bir boşluk satırı önerilir.

## 4. ALT GÖREV LİSTESİ (onay sonrası ROADMAP'e yazılacak)

| # | Alt görev | Çıktı (dosyalar) | Kabul kriteri |
|---|---|---|---|
| **4.1** | **Kararlar ve envanter mutabakatı** — Karar 1–6 uygulanır. Kod yok. | `docs/ROADMAP.md` (Faz 4 kapsamı + kriter 3 · Faz 12 + 5 tablo · Faz 13 + `user_id`) · `docs/spec/01-database.md` (`player_stats_history.club_id`, eksik tanımlar, `staff.role` CHECK notu) · `PROJECT_MEMORY.md` (3 SAPMA) · `docs/SPEC-COVERAGE-GAPS.md` (G-15 kişilik düzenlenebilirliği → Faz 11, G-16 master→save FK → Faz 12) | 6'nın zemini; 2·4·5'in ön koşulu |
| **4.2** | **`fk-policy.ts` V3'e genişletilir** — üçüncü olgu `is_nullable`, `SET NULL` üretimi, sıra `①②③④`. Migration yok. | `packages/db/src/schema/fk-policy.ts` + `.test.ts` · `packages/db/integration/schema-constraints.itest.ts` (katalogdan `is_nullable`) | **2** |
| **4.3** | **`people` + `players`** — §3.1.0 sütunları `people`'a, `person_id` UNIQUE | `src/schema/people.ts`, `players.ts` · `drizzle/0005_*.sql` + `drizzle/down/0005_*.sql` · round-trip `it()` · ER sabitleri | **1**, **6** |
| **4.4** | **Üç ileri FK** — sütun + kısıt **aynı** migration'da. `managers.user_id` **yazılmaz** | `drizzle/0006_*.sql` + `down` · `federations.ts`/`clubs.ts`/`referees.ts` | **2** |
| **4.5** | **`player_attributes` (47) + `player_hidden_attributes` (10)** — nitelik CHECK'i **YOK**; `players`'a `CA <= PA` ve `pa_range_min <= pa_range_max` CHECK'leri | `drizzle/0007_*.sql` + `down` · iki şema dosyası · 47+10 envanteri `spec/02` §4.1'den **sayılarak** doğrulanır | **4**, **5** |
| **4.6** | **`player_positions` + `player_traits` + `player_stats_history`** (+`club_id`) | `drizzle/0008_*.sql` + `down` · üç şema dosyası | **1**, **6** |
| **4.7** | **`staff` + `staff_attributes` + `managers` + `manager_attributes`** — `staff.role` **CHECK** (12 değer) | `drizzle/0009_*.sql` + `down` · dört şema dosyası | **1**, **6** |
| **4.8** | **Transfer arama indeksleri** — `players(primary_position, current_ability)` + `people(birth_date)`; kapsam `spec/01`'in indeks satırından değil **kriter 3'ün sorgusundan** türetilir | `drizzle/0010_*.sql` + `down` · `src/schema/search.ts` benzeri tek üretim noktası | **3** hazırlığı |
| **4.9** | **5.000 sahte oyuncu seed'i** + determinizm ölçümü (iki koşu birebir aynı; zaman damgaları **gürültülü** dışlanır) | `tools/data-cli/src/seed/` (üreteç + veri) · `integration/*.itest.ts` | **1** |
| **4.10** | **Kriter 3'ün ölçümü** — `ANALYZE` şart (`reltuples != -1` denetlenir) · **A** = 5.000 (kriteri kapatır) / **B** = sentetik hacim (indeksin gerekçesi) · **seçici + seçici olmayan iki terim** · mimari etiketi (amd64, üretim ARM64) | `integration/player-search-performance.itest.ts` | **3** |
| **4.11** | **ER diyagramı + `docs/schema/world.md` + faz kaydı + PR** | `docs/schema/world.md` (üretilir, çizilmez) · `PROJECT_MEMORY.md` faz kaydı (11 başlık) · `CHANGELOG.md` · PR → `develop` | **6** |

**Her migration üç şey daha getirir** (Faz 3 §11 ④⑤): `drizzle/down/<tag>.sql` (yoksa koşucu veritabanına dokunmadan durur) · round-trip testine bir `it()` · ER diyagramı bayatlar ve `er-diagram.itest.ts` kırılır — blok **elle düzenlenmez**, hata mesajındaki üretilmiş metin kopyalanır, `EXPECTED_TABLE_COUNT`/`EXPECTED_FOREIGN_KEY_COUNT` güncellenir.

**`comparedFacts` alt sınırı her migration'da yükselir ve TAHMİN EDİLMEZ** — bugün **1.627**. Yöntem 3.6'dan beri aynı: sınır erişilemez bir değere (`9_999_999`) konur, gerçek değer testin reddettiği çıktıdan okunur.

## 5. RİSKLER — Faz 3 §5'in on bir deseninden

| Desen | Neden en olası | Karşı önlem |
|---|---|---|
| **F1** — elle envanter şema büyüyünce bayatlıyor (Faz 3'te **3 kez**) | Faz 4 **6 migration** yazacak (Faz 3: 5). Bayatlayacak elle listeler: `schema-constraints.itest.ts` FK envanteri · ER sabitleri · round-trip `it()` blokları · `comparedFacts` sınırı | 4.2 kuralı `is_nullable` ile genişletiyor → yeni FK'lar liste güncellenmeden denetlenir. Elle liste **silinmez** (*"bugün şunlar var"* ≠ *"olması gereken bu"*) |
| **D2** — ölçüm aracı/düzeneği yanlış cevap üretiyor (Faz 3'te **6 kez**) | **Faz 4'te zaten 2 kez oldu:** envanter regex'i `player_traits`'i kaçırdı (4.0) · `node -e` ters tırnakları yedi (4.0b). 47 sütunlu tablo toplu metin değişimi cazibesinin zirvesi — #37'de `sed` `.notNull()`'ı sildi ve `typecheck` **exit 0** verdi | Şema dosyasında **toplu metin değişimi yok**; doğrulama `typecheck` değil **üretilen migration SQL'i**. Markdown yalnızca `Edit`/`Write` |
| **F2** — ortam katmanları yolu/kaçışı bozuyor (Faz 3'te **7 kez**) | **Sekizinci vaka 4.0b'de gerçekleşti.** Faz 4 çok sayıda SQL + Markdown üretecek | Mutlak yol · ters tırnaklı metin hiçbir kabuk argümanından geçmez · `python -c`/`node -e` kaçış yolu değil |

**Ayrıca izlenecek:** **D1** (`comparedFacts` sınırı tahmin edilmemeli) · **F3** (V1'in elenmesi bunun ölçülmüş örneği; CHECK/CHECK-yok sınırı ikinci aday) · **D3** (`packages/db` kapsamı %100 yalanı — kanıt round-trip ve `EXPLAIN`).

**Beklenen ve normal iki şey:** kapsam **düşecek** (yeni şema dosyaları paydaya %0 ile girer — eşik düşürülmez, dosya dışlanmaz, import testi yazılmaz) · mutasyon serisinin **payı artmalı** (3.8/3.9/3.10'da 19'da sabitti çünkü üçü de migration yazmadı; **pay 19'da kalırsa bu bir alarmdır**).

## 6. TAHMİNİ SÜRE

**11 alt görev, 2–3 gün.** Dayanak: Faz 3 aynı sınıf işti (11 tablo, 5 migration, 12 alt görev). Faz 4 bir migration ve 47 sütunlu bir tablo daha getiriyor ama üç tablo eksiliyor (Faz 12'ye) ve `fk-policy` hattı hazır.

⚠️ ROADMAP §0.5: *"hiçbir faz 3 günü aşmaz; aşacaksa ikiye bölünür ve bu belgeye kaydedilir."* Bölünme çizgisi hazır: **4a** = 4.1–4.7 (şema) · **4b** = 4.8–4.11 (indeks, seed, ölçüm, kapanış).
