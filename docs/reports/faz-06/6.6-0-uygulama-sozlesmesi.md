> **Alt görev:** 6.6 — on alan-özel bileşen · **GİRDİ ARTEFAKTI** (rapor değil): yazar ajanların bağlayıcı sözleşmesi
> **Tarih:** 2026-09-11 · **Dal:** feature/faz-06-design-system · **Taban (ölçüldü, `olcumcu` ①):** `43a555f` (CHECKPOINT `taban_commit: 76ed141` + 1 docs commit; ağaç temiz)
> **Sürüm:** v2 — v1 scratchpad'de yazıldı, `planci` ② 22 bulguyla denetledi (DÜZELTMEYLE UYGUN), v2 bulguları işledi ve K11 gereği depoya taşındı. Bu dosya **append-only**; sonraki düzeltme aynı dosyanın altına **v3** olarak eklenir.

# 6.6 — UYGULAMA SÖZLEŞMESİ (v2)

Paket `packages/ui`. ROADMAP maddesi `- [ ] **6.6**` (docs/ROADMAP.md) bu sözleşmenin üstüdür; çelişkide ROADMAP kazanır.

## 0. Bağlayıcı kurallar (her yazar için)

- **K5:** JSX'te ve `title`/`aria-label`/`alt`/`placeholder`/`label` niteliklerinde çıplak metin YOK; her metin `t(X_KEYS.y)` ile.
- **Anahtar sözleşmesi:** `common:ui.<grup>.<yol>` — grup = dosya adının camelCase'i (`attribute-badge.tsx` → `attributeBadge`); her segment `/^[a-z][a-zA-Z0-9]*$/` (BÜYÜK harfle başlayan segment YASAK → mevki kodları küçük harf: `position.gk`).
- **`*_KEYS` DÜZ bir nesnedir** (iç içe nesne YOK — `ALL_UI_KEYS` `Object.values` ile düzleştirir, iç içe nesne `[object Object]` olur). Anahtar **adı** camelCase düz (`bandVeryPoor`), **değeri** noktalı tam yol (`common:ui.attributeBadge.band.veryPoor`). Modül başına **EN FAZLA BİR** `_KEYS` ile biten dışa aktarım (`i18n-keys.test.ts` ② bunu iddia edecek; ikinci bir `_KEYS` kayıt defterini EZER — planci bulgusu).
- **`t()`nin kökü daima `*_KEYS` sabitinin kendisi:** `t(X_KEYS.a)` ya da `t(X_KEYS[expr])`. `t(band.key)`, `t(key)` (parametre), literal taşımayan türev sabitler **çözülmez** → `pnpm i18n:check` "kullanılmayan anahtar" der (tools/i18n-check/index.mjs:193-254). Sıralı eşleme gerekiyorsa **ad listesi** yaz: `const ORDER = ['bandVeryPoor', …] as const; t(X_KEYS[ORDER[i]])`.
- Her bileşen kendi `*_KEYS` sabitini KENDİ dosyasında tanımlar ve `t()` çağrısını AYNI dosyada yapar. Sabit iskelede ZATEN var ve `i18n-keys.ts` kayıt defterine kayıtlı; yazar anahtar ADLARINI ve DEĞERLERİNİ değiştirmez (kayıt defteri, `common.json` ve sözlük onlara bağlı). Eksik anahtar → ÇIKTI'da **İSTEK**.
- Yazar yalnızca kendi dosyalarına yazar: `packages/ui/src/components/<ad>.tsx` ve `<ad>.test.tsx` (iskeledeki stub'ı **tamamen** yeniden yazar). Paylaşılan dosyalara (`i18n-keys.ts`, `index.ts`, `common.json`, `glossary.md`, `package.json`, `vitest.setup.ts`, `test/render.tsx`, `apply-theme.ts`, `contrast-audit.test.ts`) **DOKUNMAZ** — istisna: AttributeBadge yazarı `contrast-audit.test.ts`e DENETİM ③'ü ekler (§1.1). Ek doldurma/bağımlılık → **İSTEK**.
- Renkler token'dan: `var(--…)` (`tokens.generated.css`). **Üç istisna, adıyla:** ① nitelik bant zeminleri `ATTRIBUTE_BANDS[i].color` (spec hex) ② AttributeBadge ön planı **tema-bağımsız hex** (`ATTRIBUTE_BADGE_FOREGROUNDS` = koyu temanın `--text-primary`/`--text-inverse` değerleri; zemin tema-bağımsız olduğu için ön plan da öyle — açık temada `var(--text-primary)` 1. bantta ≈1,9:1'e düşerdi, ölçüldü) ③ forma renkleri (çağırandan gelen veri). Başka sabit hex YAZILMAZ.
- `packages/ui` → yalnızca `@fms/shared` (§2.4). `@fms/db`, `@fms/engine`, `@fms/shared/server`, `apps/*` YASAK. `DataProvider` KURULMAZ, `DATA_MODE` OKUNMAZ, `Math.random()`/`Date.now()`/`console.*` YOK, `any` YOK, `types: []` korunur (`node:*`, `process`, `Buffer` yok; testte `process.stdout` yok — **iddia** kullan).
- Aralık dışı / geçersiz girdi SESSİZCE KIRPILMAZ: `RangeError`, Türkçe mesaj (emsal `bandForAttribute`).
- Saf hesap JSX dışında, dışa aktarılan bir fonksiyonda ve birim testli (6.5 `indicatorOffsetPercent` dersi). Envanterler LİSTE olarak dışa aktarılır (`as const`); sayı testte listeden türetilir, prose'a yazılmaz.
- **Test:** `packages/ui/src/test/render.tsx` → `renderWithI18n` + `testText(key)` (`[attributeBadge.band.veryPoor]`) — Türkçe metin ('çok zayıf') geçen bir iddia `t()`yi kanıtlamaz, **anahtar** iddia edilir. Radix Avatar yedeği bir makro görev sonra çizilir → `findBy*` (6.5 emsali). jsdom sınırı (layout 0×0) sahtelenmez; **dosya başına "TAKLİT ETMEDİĞİ"** ve gerçek doğrulama fazı (17/49) adıyla yazılır.
- **BORÇ-009 riski:** modül düzeyi Türkçe metin sabiti YAZILMAZ (`ATTRIBUTE_BANDS.label` tokens'ta veridir, **render edilmez**; `band.label` doğrudan basılmaz).
- Komutlar: `npx prettier --write <dosyalar>` · `pnpm exec eslint <dosyalar>` · `pnpm vitest run --project ui <dosya adı>` · `pnpm --filter @fms/ui typecheck`.
- **Teslim:** `## ÇIKTI` (dosyalar · komutlar + ham satırlar · test sayısı · İSTEK'ler · mutasyon adayları) ve `## GEREKÇE` ayrı. Denetleyenler yalnızca ÇIKTI'yı görür.

## 1. Bileşen sözleşmeleri

### 1.1 AttributeBadge — `attribute-badge.tsx` · grup `attributeBadge`
- Props: `value?: number` (1–20 tam sayı) · `range?: readonly [number, number]` · `size?: 'sm' | 'md'` (varsayılan `md`) · `className` · diğer `span` nitelikleri. `redundant` prop'u **YOK** (K-2: tek aktivasyon yolu).
- İkisi de yoksa **BİLİNMİYOR**: `?` glifi, yüzey token'ları (`--bg-elevated`/`--text-secondary`), `aria-label` = t(unknown). İkisi birden → `RangeError`.
- Bant: `bandForAttribute(value)` / `bandForAttributeRange(min, max)` (`tokens/attribute-scale.ts`). Aralık metni `13–17` (U+2013).
- Zemin: `style.backgroundColor = band.color`. Ön plan: `attributeBadgeForeground(band)` → `ATTRIBUTE_BADGE_FOREGROUNDS` (`[DARK_COLOR_TOKENS['--text-primary'], DARK_COLOR_TOKENS['--text-inverse']]`, `tokens/color.ts`) adayları arasından **hem düz bant hem desen bileşkesi** üzerinde en düşük oranı **en yüksek** olan seçilir (`contrastRatio`, `blendTowardWhite` — `tokens/contrast.ts`).
- **YEDEKLİ KODLAMA** (spec/05 §7.2 *"renk körlüğü modunda sayı kalınlaşır ve arka plan deseni eklenir"*; 6.2 SONUÇ: *"6.6'nın payı ağırlık + desen"*):
  · `ATTRIBUTE_PATTERN_ALPHA` tek sabit (0–1). Değeri **ÖLÇÜLÜR**: 0,05 adımlarla en büyük alfa öyle ki sekiz bandın hepsinde seçilen ön plan, `blendTowardWhite(band.color, alfa×100)` bileşkesi üzerinde **≥ 4,5:1** (kriter 4 — DZ-01: sayı ölçümden).
  · `attributeBandPattern(index, alpha)` → 8 **FARKLI** CSS `background-image` dizesi (`repeating-linear-gradient`, açı/aralık bant başına; beyaz `rgba(255,255,255,alfa)`); `ATTRIBUTE_BAND_PATTERNS` = 8'lik liste, hepsi farklı (testte `ATTRIBUTE_BANDS.length` ve `new Set(…).size`). Gerekçe dosya başına: tek desen komşu bantları ayırmaz (komşu kontrast 1,04–1,71), üçüncü kanal ancak bant başına farklıysa bilgi taşır — spec "desen" (tekil) diyor, bu bir GEREKÇE, SAPMA değil.
  · Kök `<span>` **her zaman** `--band-pattern` CSS değişkenini inline taşır (`style={{ '--band-pattern': … }}`; tip: `CSSProperties & Record<'--band-pattern', string>`).
  · Aktivasyon **yalnızca ata `[data-cvd]`** üzerinden — sınıf literalleri **verbatim** (Tailwind kaynak taraması template'i görmez): `[[data-cvd]_&]:font-bold` ve `[[data-cvd]_&]:[background-image:var(--band-pattern)]`. `CVD_ATTRIBUTE` sabiti (`theme/apply-theme.ts`, iskelede var) import edilir ve test, sınıf literalinin `[${CVD_ATTRIBUTE}]` içerdiğini iddia eder (iki liste ayrışmasın). Mod (3 tip, `html[data-cvd]`, ayar) **6.8'in işi**.
- `data-band-index` (0–7); sayı `font-[var(--font-mono)] tabular-nums`; normal ağırlık `font-medium`.
- `aria-label` = t(label, { value: '15' | '13–17', band: t(ATTRIBUTE_BADGE_KEYS[ORDER[i]]) }).
- `ATTRIBUTE_BADGE_KEYS` (düz): `bandVeryPoor · bandPoor · bandBelowAverage · bandAverage · bandGood · bandVeryGood · bandExcellent · bandWorldClass · unknown · label` (yollar `band.veryPoor` … `unknown` · `label`). `ATTRIBUTE_BAND_KEY_ORDER` (8 ad, `ATTRIBUTE_BANDS` sırasıyla; testte 8'e 8 iddia). ⚠️ Adı `_KEYS` ile biten İKİNCİ bir dışa aktarım YOK.
- **`contrast-audit.test.ts`e DENETİM ③** (AttributeBadge yazarı ekler): sekiz bant × (düz + bileşke) ≥ 4,5, kapsam `ATTRIBUTE_BANDS.length`ten; mevcut `BADGE_FOREGROUNDS` sabiti `ATTRIBUTE_BADGE_FOREGROUNDS` import'uyla değiştirilir (iki liste → bir).
- Dışa aktarım: `AttributeBadge`, `AttributeBadgeProps`, `ATTRIBUTE_BADGE_KEYS`, `ATTRIBUTE_BAND_KEY_ORDER`, `ATTRIBUTE_BAND_PATTERNS`, `ATTRIBUTE_PATTERN_ALPHA`, `ATTRIBUTE_BADGE_FOREGROUNDS`, `attributeBadgeForeground`, `attributeBandPattern`, `formatAttributeValue`.
- TAKLİT ETMEDİĞİ: CVD modu/3 tip (6.8) · ekranda ayırt edilebilirlik (Faz 49, G-05) · belirsizlik gösteriminin veri kaynağı (Faz 31).

### 1.2 StarRating — `star-rating.tsx` · grup `starRating`
- Kaynak `spec/04` §6.2: `stars = clamp(0.5, 5, round(x×10)/2)` → `STAR_MAX = 5`, `STAR_STEP = 0.5` dışa aktarılır; **`max` prop YOK** (K-1). `value ∈ {0,5; 1; …; 5}` değilse `RangeError` (0 dahil: formülün görüntü kümesi dışı); `range: [min, max]` ikisi de geçerli adım ve `min < max`. İkisi de yoksa BİLİNMİYOR (`?`, t(unknown)); ikisi birden → `RangeError`.
- Render: `<span role="img" aria-label>` + 5 inline SVG yıldız: `full` · `half` · `empty` · aralıkta `potential` (kontur `--accent`). Dolu `var(--warning)`, boş `var(--border-strong)`. `data-value`.
- `aria-label` = t(aria, { value: formatNumber(value) | `${formatNumber(min)}–${formatNumber(max)}`, max: STAR_MAX }) — `formatNumber` `@fms/shared` (`2,5`).
- Saf: `starStates(value | range): readonly StarState[]` (uzunluk `STAR_MAX`).
- Anahtarlar: `aria · unknown`. TAKLİT ETMEDİĞİ: yuvarlama (motorun işi, spec formülü), etkileşimli derecelendirme.

### 1.3 FormIndicator — `form-indicator.tsx` · grup `formIndicator`
- `FORM_RESULTS = ['win', 'draw', 'loss'] as const`. Props: `results: readonly FormResult[]` (eski→yeni) · `max?: number` (varsayılan **5** — kaynak `spec/01`:990 *"son 5 maç"*) · `size` · `className`. Küme dışı sonuç → `RangeError`.
- ⚠️ **"form" iki anlamlı:** `spec/01` `players.form` OYUNCU reytingi (sayı); bu bileşen TAKIM/oyuncu **sonuç dizisi** (ROADMAP'in sesi, "G/B/M"). Dosya başına yazılır ki Faz 18 oyuncu formunu bununla göstermeye kalkmasın.
- Render: `<ol aria-label=t(aria,{count})>` + `<li>` başına harf t(<r>Short) (G/B/M), `title` t(<r>Long). Renk: win → `SEMANTIC_TONE_CLASSES.success`, draw → nötr yüzey (`--bg-elevated`/`--text-secondary`), loss → `.danger`. Boş → `<span>` t(empty), `--text-muted`.
- Saf: `lastResults(results, max)`. Anahtarlar (düz): `winShort · winLong · drawShort · drawLong · lossShort · lossLong · aria · empty` (yollar `win.short` … `aria` · `empty`).

### 1.4 MoraleIcon — `morale-icon.tsx` · grup `moraleIcon`
- Kaynak `spec/01`:987 `morale: smallint // 0-100` — seviye YOK → **KALİBRASYON** (6.2 `Math.floor` emsali; SAPMA değil): `MORALE_LEVELS = [ {key:'veryLow', min:0, max:19}, {key:'low', min:20, max:39}, {key:'neutral', min:40, max:60}, {key:'high', min:61, max:80}, {key:'veryHigh', min:81, max:100} ] as const` — nötr dilim **50 merkezli** (40–60, 21 değer), uçlar 20'şer; gerekçe dosya başına **sayılarla uyumlu** yazılır. Kapsayıcılık testi: 0…100 her tam sayı **tam bir** dilimde (6.2 emsali).
- `moraleLevelFor(value)`: tam sayı ve 0–100 değilse `RangeError`.
- Props: `morale: number` · `size` · `className`. Render inline SVG (çift ok yukarı / ok yukarı / yatay çizgi / ok aşağı / çift ok aşağı), `role="img"`, `aria-label` = t(aria, { level: t(MORALE_ICON_KEYS[levelKeyName]) }), `data-level`. Renk: veryHigh/high `--success`, neutral `--text-secondary`, low `--warning`, veryLow `--danger`.
- Anahtarlar (düz): `levelVeryLow · levelLow · levelNeutral · levelHigh · levelVeryHigh · aria` (yollar `level.veryLow` … `aria`).

### 1.5 ClubCrest — `club-crest.tsx` · grup `clubCrest`
- Props: `name: string` · `src?: string` · `size?: 'sm' | 'md' | 'lg'` (`CLUB_CREST_SIZES = { sm: 24, md: 40, lg: 64 }` px — KALİBRASYON) · `className`. Radix Avatar (`avatar.tsx`): kök `rounded-[var(--radius-sm)]` (arma dairesel kırpılmaz — `Avatar` kökü `radius-full`, `cn` ezer; testte iddia), `AvatarImage alt=t(alt,{name})`, `AvatarFallback delayMs={0}` → kalkan SVG + `initialsOf(name)` (≤ 2 harf, `toLocaleUpperCase('tr')`; dışa aktarılır, saf).
- TAKLİT ETMEDİĞİ: prosedürel arma (Faz 8), varlık getirme/önbellek (Faz 7), `DATA_MODE`, boyut varyantı seçimi (64/128/256 kaynağı çağıranın). jsdom görsel YÜKLEMEZ → yalnızca yedek sınanır.
- Anahtar: `alt`.

### 1.6 PlayerPortrait — `player-portrait.tsx` · grup `playerPortrait`
- Props: `name` · `src?` · `size?: 'sm' | 'md' | 'lg'` (`PLAYER_PORTRAIT_SIZES = { sm: 32, md: 48, lg: 96 }` — KALİBRASYON) · `className`. **`shape` YOK** (K-6). Radix Avatar; kök `rounded-[var(--radius-md)]`; yedek: silüet SVG (baş + omuz) yüzey token'larıyla; `alt` = t(alt,{name}).
- TAKLİT ETMEDİĞİ: yüz hizalı kırpma, `PORTRAIT_STYLE=stylized`, prosedürel avatar (Faz 9). Anahtar: `alt`.

### 1.7 KitSwatch — `kit-swatch.tsx` · grup `kitSwatch`
- `KIT_TYPES = ['home', 'away', 'third'] as const` ve `KIT_COLOR_SLOTS = [2, 3] as const` — db (`club-kits.ts`, `kit-templates.ts`) ile AYNI; eşitlik `inventory-guards` ④'te. Props: `kind: KitType` · `colors: readonly string[]` (uzunluk `KIT_COLOR_SLOTS`tan biri ve her biri `#RRGGBB` — değilse `RangeError`) · `size` · `className`.
- Render `<span role="img" aria-label=t(KIT_SWATCH_KEYS[kindKeyName])>` + forma silüeti SVG: gövde `colors[0]`, kollar `colors[1]`, yaka `colors[2] ?? colors[1]`; `data-kind`.
- TAKLİT ETMEDİĞİ: şablon/desen (`kit_templates`, Faz 8), sponsor/numara, gerçek forma görseli. Anahtarlar (düz): `kindHome · kindAway · kindThird` (yollar `kind.home` …).

### 1.8 PositionMap — `position-map.tsx` · grup `positionMap`
- `POSITION_CODES = ['GK','DC','DL','DR','DM','MC','ML','MR','AMC','AML','AMR','ST'] as const` ve `POSITION_LEVELS = ['natural','accomplished','competent','awkward','ineffectual'] as const` — db (`players.ts` PLAYER_POSITIONS, `player-positions.ts` POSITION_LEVELS) ile AYNI, sıra dahil; eşitlik ④'te. `POSITION_COORDINATES: Record<PositionCode, {x, y}>`, viewBox `0 0 68 105` — **oran** spec/05 §7.6'dan (105×68), **yönelim KARAR** (dikey, kale altta; spec PositionMap tanımlamıyor). Tüm noktalar viewBox içinde, hepsi farklı (test).
- Props: `positions: readonly { position: PositionCode; level: PositionLevel }[]` (küme dışı kod/seviye ya da tekrar → `RangeError`) · `size?: 'sm' | 'md'` · `className`.
- Render `<svg role="img" aria-label>`: saha (dış çizgi, orta çizgi, orta yuvarlak, iki ceza sahası; `stroke=var(--border-strong)`, zemin `var(--bg-elevated)`); 12 işaret dairesi — verilenler seviyeye göre `data-level` + stil (natural dolu `--accent` · accomplished dolu %70 · competent kontur · awkward kesikli kontur · ineffectual soluk), verilmeyenler soluk boş. İşaret içinde **İngilizce kod** (K-8; `spec/02` §4.2 `GK (Kaleci)` emsali — kod bir kimlik, `{code}` ifadeyle) + `<title>` = t(entry, { position: t(POSITION_MAP_KEYS[posKeyName]), level: t(POSITION_MAP_KEYS[levelKeyName]) }).
- `aria-label` = t(aria, { entries }) — birleştirme `t()` enterpolasyonuyla (`', '` ayraç sabit dizeyle JSX'e girmez); boş liste → t(empty).
- TAKLİT ETMEDİĞİ: geometri/ölçek (jsdom 0×0 — Faz 17), etkileşim (Faz 20), oyuncu hareketi/Faz 27 sahası (ortak koordinat kaynağı V2 adayı).
- Anahtarlar (düz): `positionGk … positionSt` (12, yollar `position.gk` …) · `levelNatural … levelIneffectual` (5) · `aria` (`"Mevki haritası: {{entries}}"`) · `entry` · `empty`.

### 1.9 CurrencyValue — `currency-value.tsx` · anahtar YOK (grup AÇILMAZ)
- Props: `amount: number` (ANA birim; kuruş/cent → ana birim dönüşümü veri katmanının işi, burada YAPILMAZ) · `currency?: string` · `className`. `formatMoneyCompact` (`@fms/shared`) — yeniden yazılmaz. Sonlu değilse `RangeError`. Render `<span class="font-[var(--font-mono)] tabular-nums …">`. U+00A0 korunur; testte kod noktasıyla iddia (`€1,2 mn` — küçük harf `mn` Faz 5 kararı).

### 1.10 DateChip — `date-chip.tsx` · anahtar YOK
- Props: `date: Date` · `timeZone?: string` · `className`. `Number.isNaN(date.getTime())` → `RangeError` (Türkçe mesaj). `formatDate` (`@fms/shared`, `23 Ağustos 2026`). Render `<time dateTime={date.toISOString()}>` çip (Badge nötr yüzeyi).
- TAKLİT ETMEDİĞİ: göreli zaman, zaman dilimi tespiti (varsayılan UTC — `@fms/shared` kararı).

## 2. Ana oturumun iskelesi (yazarlardan ÖNCE) — yapıldı mı, raporda
- `packages/ui/package.json` `dependencies` → `"@fms/shared": "workspace:*"` + `pnpm install` (lockfile).
- `theme/apply-theme.ts` → `CVD_ATTRIBUTE = 'data-cvd'` (kardeşleri `THEME_ATTRIBUTE`/`MOTION_ATTRIBUTE`/`FONT_SCALE_ATTRIBUTE`); `theme/index.ts`ten dışa aktarım.
- On stub: sekizi `*_KEYS` sabitiyle (yukarıdaki adlar/yollar), ikisi anahtarsız; `i18n-keys.ts` **sekiz** grup; `i18n-keys.test.ts` ② `moduleName` kebab→camelCase + kapsam iddiasına `attributeBadge`/`dateChip` + "modül başına en fazla bir `*_KEYS`" iddiası; `components/index.ts` dışa aktarımlar.
- `apps/web/src/locales/tr/common.json` `ui.*` — değerler sözlük §7/§8'den.
- `docs/glossary.md` §7 (`### 7.1 Mevki kodları` 12 · `### 7.2 Mevki yetkinliği` 5) + §8 (üyeler adıyla); §6 `POSITION_LEVELS` satırı düşer; satır 43–51 (*"çakışma yok / nöbetçi yok / Faz 18"*) güncellenir; `tools/glossary-check/index.test.mjs` dağılım + toplam güncellenir (gevşetilmez) + **K-5 nöbetçisi**: §7/§8'in her TR karşılığı `common.json` `ui.*` değerleri arasında.
- `scripts/inventory-guards.test.mjs` ④ ui ↔ db kapalı kümeler (`POSITION_CODES↔PLAYER_POSITIONS` · `POSITION_LEVELS` · `KIT_TYPES` · `KIT_COLOR_SLOTS`, sıra dahil); kanarya gerçek depoda.
- `packages/db/src/schema/player-positions.ts:80` yorumu (*"Faz 5'in i18n anahtarları: … Zayıf · Yabancı"*) — ölçüldü, locales'te 0 → sözlük §7.2'ye işaret edecek şekilde düzeltilir; ROADMAP Faz 10 *"Doğal/Yetkin/Kabul Edilebilir/Zayıf/Yabancı"* sözlükle hizalanır (`awkward` ≠ bant "zayıf", `ineffectual` ≠ "Yabancı Kotası" — K-4).
- ROADMAP 6.8 kapsamına DZ-14 devri (CVD_ATTRIBUTE + 3 tip + AttributeBadge kanallarının etkinleştiği iddiası + tip farkı kararı).

## 3. Türkçe karşılıklar (K-4 — kullanıcı onayına sunulur; cevap yoksa böyle kalır)
Mevki: GK Kaleci · DC Stoper · DL Sol Bek · DR Sağ Bek · DM Defansif Orta Saha · MC Merkez Orta Saha · ML Sol Orta Saha · MR Sağ Orta Saha · AMC Ofansif Orta Saha · AML Sol Kanat · AMR Sağ Kanat · ST **Santrfor** (spec/02). Yetkinlik: natural Doğal · accomplished Yetkin · competent Kabul Edilebilir · awkward **Zorlanır** (ROADMAP'in "Zayıf"ı bant 4–6 ile çakışıyordu) · ineffectual **Yetersiz** ("Yabancı" Yabancı Kotası'yla çakışıyordu). Sonuç: Galibiyet (G) · Beraberlik (B) · Mağlubiyet (M). Moral: Çok Kötü · Kötü · Normal · İyi · Çok İyi. Forma: İç Saha Forması · Deplasman Forması · Üçüncü Forma. Bant adları spec §7.2 birebir (küçük harf). Bilinmiyor. Yıldız Derecesi.
