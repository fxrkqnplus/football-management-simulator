> **Alt görev:** 6.0 — Doğrulama alt görevi (Faz 6 açılışı)
> **Tarih:** 2026-09-04 · **Dal:** `feature/faz-06-design-system` (taban `2ec1548`)
> **İçerik commit'i:** `3073deb` · **CI koşusu:** yazım anında bilinmiyor · **PR:** faz sonunda (6.12)
> **Bağlam yüzdesi:** ölçülemedi

## 🟡 KARAR 6.0 — Doğrulama alt görevi: dokuz ölçüm, dört karar

**Ne yapıldı:** Faz 6'nın alt görev listesi ROADMAP'e yazıldı ve dokuz doğrulama koşturuldu — kod yazılmadı, paket kurulmadı.

**Kapılar:** typecheck [x] · lint [x] · format [x] ⚠️ · test [x] · build [x] ⚠️ · arch [x] · test:db [x] · gaps [x] · i18n [x]
**Commit:** `3073deb` (içerik) · push [ ] · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- typecheck **10/10** — ⚠️ `Cached: 2`, **soğuk değil** (ön kontrol soğukluk istemiyordu).
- lint **0** — çıkış kodu **ayrıca** ölçüldü (`exit 0`); ilk ölçüm boru hattında `$?` okuyordu, yani `tail`in kodunu (D2).
- format **0** ⚠️ `.md` `.prettierignore`da — bu alt görevin **bütün ürünlerine** hiç bakmadı (SAPMA-024).
- arch **9 kural** — komut sayı basmıyor, `tools/arch-check/arch-check.test.mjs`:536-544'ten sayıldı.
- test **1120 / 75** · test:db **301 / 10** · gaps **20 · 3 · 17 · 0 ✗** · i18n:check **temiz, 308 dosya**.
- build ⚠️ `Cached: 8` **FULL TURBO — soğuk DEĞİL**; `dist` Faz 5'ten kalma, dal kod değiştirmediği için güncel. 6.10 kendi soğuk ölçümünü alır.
- Kapsam **364 / 451 = %80,70**; marj `364 / 0,70 = 520` → payda boşluğu **69**.
- Kütükler **iki desenle** sayıldı (kalın + düz): SORUN 1 (kapalı) · BORÇ 10, **3 ÖDENDİ → 7 açık** · SAPMA 40 · G 20.

**KARARIN GEREKİYOR:** **4 madde** — 6.1'e geçilmiyor.

1. **Tarayıcı yolu: zincir çözülüyor. ② korunsun mu, yoksa dördüncü yol alınsın mı?**
   Önerim: **② korunur, dördüncü yol ALINMAZ.** Gerekçe: zincirin çözülmesi bir kurulum kanıtı değil, ve üç bedelden **biri ölçülmedi** — Chromium ikilisinin `ubuntu-24.04-arm` üzerinde koştuğu (K14). Faz 17 G-02 ile aynı sorunu **zaten çözmek zorunda**.
   Cevap vermezsen: **duracağım** — bu kararı sessizce vermek fazı "yapıldı" diye kapatmanın kendisi olur.

2. **`28 vs 30+`: kriter bir SAYI değil bir LİSTE olsun mu?**
   Önerim: **kriter listeye çevrilir** — *"kapsamın adıyla saydığı her bileşenin hikayesi var"*, ayrıştıran bir testle. Deponun kendi deyimi (`VISIBLE_ATTRIBUTES`, on namespace). **SAPMA-041** (`düzeltme`) açılır.
   Cevap vermezsen: **duracağım** — bir kabul kriterini değiştiriyor.

3. **§11.6'ya "üretim paketi boyutu" satırı eklensin mi?**
   Önerim: **eklenir.** Aksi hâlde `perf:budget` **hiçbir §11.6 satırını ölçemez** ve kriter 2 yalnızca bir fixture'la kanıtlanır — *"bakacak bir şey bulamayan kapıya ✅ yazılmaz"*. Taban hazır: **380.908 bayt**. Bir spec otorite tablosunu düzenliyor → **SAPMA-042** (`düzeltme`).
   Cevap vermezsen: **duracağım.**

4. **Kriter 3 (DataTable 10.000 satırda 55+ fps) hangi faza taşınsın?**
   Önerim: **Faz 49.** Kapsamında *"Liste sanallaştırma denetimi"*, *"Tüm performans bütçeleri sağlanıyor"* ve *"`perf:budget` genişletmesi"* **adıyla** var (doğrulandı). Faz 18 alternatifti ama onun kriterleri 500 satır / sıralama-filtre, kaydırma fps'i değil.
   Cevap vermezsen: **duracağım.**

**Yeni kayıt:** SAPMA/BORÇ/SORUN **henüz yok** — SAPMA-041 ve SAPMA-042 kararlara **bağlı**, onay gelmeden açılmaz (kimsenin belirlemediği alana değer uydurma, SAPMA-026).

**Sıradaki:** **6.1 — BORÇ-010**, `local/no-hardcoded-path` kablolama kanaryası. Dört karar gelmeden başlamıyorum.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. ① Tarayıcı yolu — ve yarıda bırakılmayan bir ölçüm

**İlk okuma yanlıştı ve bu kaydediliyor.** `npm view @vitest/browser-playwright versions --json | tail -8` çıktısı yalnızca `5.0.0-beta.5 … 5.0.0` gösterdi ve buradan *"4.x hattı yok, peer `vitest: 5.0.0`, yani bizim 4.1.11'imizle uyumsuz"* sonucu çıkıyordu. **Sayım bunu çürüttü:** `grep -c "^4\."` → **43**. `tail` bir listenin **kuyruğunu** gösterir, kapsamını değil.

> **Ders:** *"bir ölçümü yarıda bırakma — ilk okuma yanlış olabilir."* Burada yanlış cevap **daha kısıtlayıcı** yöndeydi (bir yolu kapatıyordu), yani "muhafazakâr hata" bile bir hatadır.

**Doğru ölçüm:**

| Paket | Sürüm | Peer | Boyut (unpacked) |
|---|---|---|---|
| `@vitest/browser` | 4.1.11 | `vitest: 4.1.11` | 1.849.305 B |
| `@vitest/browser-playwright` | 4.1.11 | `vitest: 4.1.11` · `playwright: '*'` | 49.195 B |
| `playwright` | 1.62.1 | — (`os`/`cpu` kısıtı **yok**) | 5.074.152 B |

Bizde `vitest` **4.1.11** → **tam eşleşme**. `@storybook/addon-vitest@10.6.0`in `@vitest/browser-playwright: ^4.0.0` peer'ı da karşılanıyor.

**Ve `storybook` çekirdeği Playwright TAŞIMIYOR** — `npm view storybook dependencies | grep -iE "playwright|puppeteer|chromium"` → **0 eşleşme**. Yani planın ③ seçeneği (*"Storybook zaten getiriyor, ①'in maliyeti düşük"*) **çürüdü**: Playwright yalnızca test yollarında görünüyor (`@storybook/test-runner@0.24.5` → `playwright ^1.14.0` **doğrudan** bağımlılık).

**Üç bedel:**

| # | Bedel | Durum |
|---|---|---|
| ① | Peer **tam sabit** (`vitest: 4.1.11`, aralık değil). Her `vitest` bump'ı `vitest` + `@vitest/browser` + `@vitest/browser-playwright` + `playwright` **dörtlü kilit adımı** olur | **ölçüldü** |
| ② | CI'ın `Kalite kapıları` işi **matrix**: `ubuntu-24.04` **ve** native `ubuntu-24.04-arm` (`.github/workflows/ci.yml`:25-33, K14 gerekçesiyle QEMU **kullanılmıyor**). Bir tarayıcı adımı **ikisinde de** koşmalı; yalnızca amd64'te koşarsa kapı **kısmi** olur ve D3 yanılsaması üretir | **ölçüldü** |
| ③ | Playwright'ın Chromium ikilisi `ubuntu-24.04-arm` üzerinde iniyor ve koşuyor mu | ⚠️ **ÖLÇÜLMEDİ** — ikili npm paketinde **değil**, `playwright install` ile ayrı iniyor; registry meta verisi bunu söylemiyor |

⚠️ **Bu bir kurulum kanıtı değil.** 6.0 paket kurmuyor (kullanıcı kararı); yukarısı registry meta verisinden yapılmış bir **çözünürlük ölçümü**. *"Başka bir ortamda alınmış ölçüm bu ortam için bir hipotezdir"* — burada ortam bile denenmedi.

## 2. ⑦ `perf:budget`in ölçecek satırı yok — G-01'in en pahalı bulgusu

`docs/spec/09` §11.6'nın **15 satırı** tek tek incelendi (16 satır eşleşti, biri başlık):

| Grup | Satırlar | Faz 6'da? |
|---|---|---|
| Gerçek tarayıcı gerekiyor | LCP · Lighthouse Performance · Lighthouse Accessibility · 2D fps (masaüstü) · 2D fps (mobil) · Bellek | ❌ |
| Gezilebilir uygulama gerekiyor | Ekran geçişi (**Faz 17**) | ❌ |
| Bir ekran gerekiyor | Kadro tablosu 500 satır (**Faz 18**) | ❌ |
| Motor/veritabanı gerekiyor | Tek maç · Maç günü (Dengeli) · Maç günü (Tam Detay) · Tur atlama · Sezon geçişi · Transfer araması · Kayıt yazma | ❌ |

**Ölçülebilen: 0 / 15.**

⚠️ **Ve ROADMAP Faz 6'nın kapsamı üçüncü bir metrik sayıyor — *"üretim paketi boyutu"* — ama o §11.6'da HİÇ YOK.** Ölçüldü: `paket|bundle|boyut` → §11.6'da **0**, ROADMAP §0.4'ün kısaltılmış tablosunda **0**. Yani ROADMAP bu fazın kapısına, otorite listede **var olmayan** bir metrik veriyor.

Bu, SAPMA-024'ün tam biçimi: kapı kurulur, yeşil yanar, **hiçbir bütçeye bakmaz**.

**Taban ölçüldü:** `apps/web/dist/assets/index-msBHCD33.js` = **380.908 bayt** (harita hariç; `index.html` 344 B, harita 2.294.991 B). Faz 5'in kaydettiği sayıyla **birebir** — ama ⚠️ build `Cached: 8 FULL TURBO` idi, yani bu **soğuk bir ölçüm değil**; artefakt Faz 5'ten kalma ve dal kod değiştirmediği için güncel.

ℹ️ Deponun bu metriği zaten bir bütçe gibi kullandığına dair emsal: `CHANGELOG.md` Faz 1 → `.env` içinden `NODE_ENV` kaldırıldı, **429 kB → 228 kB**.

## 3. ② `28 vs 30+` — programatik sayım

| Küme | Sayı | Üyeler |
|---|---|---|
| Temel bileşenler | **18** | Button (6 varyant) · Input · Select · Combobox · Checkbox · RadioGroup · Slider · Switch · Tabs · Dialog · Sheet · Popover · Tooltip · Toast · Badge · Avatar · Progress · Skeleton |
| Alan-özel bileşenler | **10** | `AttributeBadge` · `StarRating` · `FormIndicator` · `MoraleIcon` · `ClubCrest` · `PlayerPortrait` · `KitSwatch` · `PositionMap` · `CurrencyValue` · `DateChip` |
| **Toplam** | **28** | kabul kriteri 1: **"30+"** |

Kapsamda bileşen sayılabilecek iki şey daha var — **DataTable** ve tema sağlayıcısı — ve 28 + 2 = **30**, yani kriter **sıfır marjla** karşılanır. ⚠️ Sıfır marj kırılgan: 28'in biri birleştirilirse kriter düşer, ve *"30'a saymak"* için bileşen icat etme baskısı doğar.

**Önerim listeye çevirmek**, çünkü depo bu kalıbı zaten kullanıyor: `VISIBLE_ATTRIBUTES` (4.5), on namespace listesi (*"bu bir SAYI değil bir LİSTE"*), `docs/glossary.md`nin terim sayısını **prose'a yazmama** kararı. Bir sayı prose'da yaşarsa bayatlar.

## 4. ③ `DEPENDENCY-WATCH` — beş ekleme, bir düzeltme

Registry'den **bugün** okundu:

| Paket | Sürüm | Not |
|---|---|---|
| `storybook` · `@storybook/react-vite` | **10.6.0** | satır **hiç yoktu**; `CLAUDE.md` §2.1 Storybook'u **hiç anmıyor** |
| `tailwindcss` · `@tailwindcss/vite` | **4.3.3** | satır **hiç yoktu**; §2.1 *"Tailwind CSS 4.3"* diyor → **güncel** |
| `@tanstack/react-virtual` | **3.14.10** | satır **hiç yoktu** |
| `@radix-ui/react-dialog` (örnek) · `class-variance-authority` · `tailwind-merge` · `clsx` | **1.1.23** · **0.7.1** · **3.6.0** · **2.1.1** | satır **hiç yoktu**; shadcn/ui bir paket değil, bir CLI — takip edilen onun çalışma zamanı |
| `axe-core` | **4.13.0** | satır **hiç yoktu**; §2.1 axe'ı **hiç anmıyor** ama kriter 7 ve Faz 49 istiyor |
| `@tanstack/react-table` | ~~9.1.2~~ → **9.2.4** | ⚠️ **iki düzeltme** |

**`react-table` satırının iki hatası:**
- **Faz ataması bayattı** — satır *"Faz 18"* diyordu; ROADMAP Faz 6 kapsamı *"DataTable motoru: TanStack Table + sanallaştırma"* diyor, Faz 18 ise motoru **yapılandırıyor** (60 sütun). Notların okunacağı yer, paketin **kurulduğu** yerdir.
- **Sürüm bayattı** — 9.1.2 → **9.2.4**.

⚠️ Bu, *"kapsam taşıması kütüğe kayıtla bitmez"*in **üçüncü** vakası: 4.11 (BORÇ-003/005) · 5.9 (BORÇ-009/010) · bugün bu satır. İlk ikisi kütük→ROADMAP yönündeydi, bu üçüncüsü **watch listesi→faz** yönünde — yani desen kütüklerle sınırlı değil, **faza atanan her kayıt** için geçerli.

**Peer doğrulaması** (`strict-peer-dependencies=true` ⇒ peer bir **kapı**):

| Paket | Peer | Bizde | Sonuç |
|---|---|---|---|
| `@storybook/react-vite` | `vite ^5\|\|^6\|\|^7\|\|^8` | `^8.2.2` | ✅ |
| | `react` / `react-dom` `^16.8\|\|^17\|\|^18\|\|^19` | `^19.2.8` | ✅ |
| | `storybook ^10.6.0` | 10.6.0 | ✅ |
| | `typescript >= 4.9.x` | `~6.0.3` | ✅ |
| `@tailwindcss/vite` | `vite ^5.2\|\|^6\|\|^7\|\|^8` | `^8.2.2` | ✅ |
| `tailwindcss` | *(peer yok, bağımlılık yok)* | — | ✅ |

ℹ️ `storybook@10.6.0` `@testing-library/jest-dom@6.9.1` ve `@testing-library/user-event@^14.6.3` **geçişli** getiriyor. Geçişli bir paket doğrudan import **edilmez** — `arch:check`in `undeclared-dependency` kuralı beyanı zorunlu kılıyor.

## 5. ⑧ `jsdom` sondası — 2.0b'nin iddiası sınandı

2.0b'nin cümlesi: *"`jsdom`'un boşlukları **belgeli ve bilinen çözümü olan** boşluklar."* Sonda (jsdom **30.0.1** · Node **24.19.0** · win32/amd64) yedi yeteneği ölçtü:

| Yetenek | Sonuç |
|---|---|
| `window.matchMedia` | **undefined** |
| `getComputedStyle().color`, değer `var(--x)` | **`"var(--attr-good)"`** — çözülmüyor |
| `getPropertyValue('--attr-good')` | `#5FA84C` — ham bildirim okunabiliyor |
| `getBoundingClientRect()` | **0×0** · `offsetWidth` **0** |
| `ResizeObserver` / `IntersectionObserver` | **undefined** |
| `scrollIntoView` / `hasPointerCapture` | **undefined** |
| `requestAnimationFrame` | `function` |

⚠️ **2.0b'nin adıyla saydığı iki boşluk (`scrollIntoView`, `hasPointerCapture`) aynen çıktı** — yani o günkü gerekçe bir tahmin değil bir öngörüymüş, ve şimdi **ölçülmüş** hâli var.

**Karar: `jsdom` korunur.** `happy-dom@20.14.0`'ın aynı beş boşlukta ne yaptığı **ölçülmedi** (kurulu değil, 6.0 paket kurmuyor). Ölçülmüş beş boşluğu ölçülmemiş bir kümeyle takas etmek — üstelik fazın ortasında — 2.0b'nin adıyla kaçındığı asimetri.

⚠️ **Doldurmalar bir borç üretir:** her doldurma bir davranışı **sahteliyor**. 6.8'de hangi doldurmanın **neyi** sahtelediği yazılır; yoksa geçen bir test tarayıcıda geçeceğini göstermez (D5).

**`@testing-library/user-event@14.6.7` 6.4'te kurulur.** İhtiyaç bu fazda doğdu: kriter 6 klavye navigasyonu istiyor ve `fireEvent` tab sırasını/tuş dizisini modelleyemiyor. ℹ️ `@testing-library/react` **16.3.3** çıkmış (bizde 16.3.2) — 6.3'ün kurulum turunda birlikte alınır.

## 6. ⑤ Namespace ön eki ve ⑥ ikinci kaynak kökü

**`common.json` bugün 45 anahtar, beş üst seviye:** `country` · `competition` · `value` · `diagnostics` · `debugPanel`. Deseni: **üst seviye = bir yüzey**.

**Seçilen sözleşme: `common:ui.<bileşenAdıCamelCase>.<alan>`** — 28 bileşenin 28 üst seviye anahtar açmasını önlüyor, var olan desene oturuyor, ve paket adıyla (`@fms/ui`) hizalı (bir ad bir sözleşmedir). Ön ek **nokta ile biter**: `i18n-dynamic-keys.ts`in gerekçesi birebir — `ui.` yanlışlıkla `uiHelper`ı yakalamasın.

⚠️ **Nöbetçi bugün yazılmıyor.** `common.ui` **yok**; bakacak bir şey bulamayan bir kontrol bir onay değildir (SAPMA-024). Emsal `docs/glossary.md`nin kendisi: nöbetçisi Faz 18'e bırakıldı çünkü `squad.json` boştu. Test, **ilk anahtarı yazan** alt görevle gelir (6.3/6.4).

**`i18n:check` ikinci kök (6.3):** `tools/i18n-check/index.mjs`:368 → `sourceDir` varsayılanı **`apps/web/src`**. `packages/ui/src` taranmıyor, yani eksik/kullanılmayan/boş anahtar denetimleri bileşenleri **görmez**. `local/no-bare-jsx-text` **görür** (kuralın `files:` kısıtı yok — `eslint.config.js`:127-131 depo geneli). Aracın kendi başlığı bu kökü **öngörmüş**: *"başka bir uygulama i18n kazanırsa kendi kökünü ekler."*

⚠️ Bugünkü sayılar (**59 tanımlı · 34 kullanılan · 12 kaynak · 308 dosya**) **artacak** — bir beklenen kırılma **gevşetilmez, iddia güncellenir**.

## 7. ④ §15.1 doğrulaması — ateşlenmedi, ve bu da bir sonuç

`docs/spec/05-design-system.md` başlığı: **`# 7. TASARIM SİSTEMİ`**. Yani §15.1'in *"Faz 6 → Bölüm 7"* satırı **doğru yeri** gösteriyor ve `CLAUDE.md` belge haritasının saydığı dosyayla **aynı**. SAPMA-035 sınıfı bir hata **yok** — 5.0-ön'de eklenen adım ilk kez **planlı olarak** koştu ve *"bulgu yok"* dedi.

Satır yine de eksikti: `docs/glossary.md`yi anmıyordu, oysa ROADMAP Faz 6 kapsamı ve Faz 5 faz kaydı §11 onu istiyor **ve Faz 5'in kendi satırı sözlüğü adıyla anıyor**. Eklendi. **SAPMA açılmadı** — mismatch yok, eksiklik var; iki şey aynı sınıf değil.

## 8. Değişen dosyalar

```
docs/ROADMAP.md                    [DEĞİŞTİ] Faz 6 alt görev listesi (6.0–6.12) + 6.0 SONUÇ bloğu
docs/SESSION-TEMPLATE.md           [DEĞİŞTİ] §15.1 Faz 6 satırı → + docs/glossary.md
docs/DEPENDENCY-WATCH.md           [DEĞİŞTİ] 5 yeni satır · react-table faz+sürüm düzeltmesi · jsdom/user-event sonucu
PROJECT_MEMORY.md                  [DEĞİŞTİ] günlük başlığı FAZ 5 → FAZ 6 (Faz 5 notu arşive) · ANLIK DURUM
docs/reports/faz-06/6.0-*.md       [YENİ]    bu rapor
```

**Kod dosyası: 0. Kurulan paket: 0.**
