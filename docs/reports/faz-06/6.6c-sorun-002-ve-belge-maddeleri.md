> **Alt görev:** 6.6c — SORUN-002 (nöbetçi + etiketleme + derlenmiş CSS) ve yedi belge maddesi
> **Tarih:** 2026-09-13 · **Dal:** feature/faz-06-design-system
> **İçerik commit'i:** yazım anında bilinmiyor — içerik ve kayıt **tek commit** (kapı tabanı `ea970b5`) · **CI koşusu:** yazım anında bilinmiyor — SKILL.md'nin yeni kuralı gereği sayıları **bir sonraki alt görevin kaydına** girer · **PR:** faz sonunda
> **Bağlam yüzdesi:** ölçülemedi — `/context` yalnızca kullanıcı terminalinden çalışır; vekil: bu alt görevde ~60 araç çağrısı, 6.6b turu da aynı bağlamda (özetleme görülmedi)
> **Protokol:** `/faz-yurut 6.6c`, beş aşama ana oturumda — **ajan yok, workflow yok** (kullanıcı kararı). Devir notunun her sayısı yeniden sayıldı (DZ-01).

## 🟢 TAMAM 6.6c — SORUN-002 kapandı: `local/no-untyped-arbitrary-value` (kaynaktan türetilen, kapalı-güvenli) gerçek depoda 34/12 öttü, 34 geçiş etiketlendi, derlenmiş CSS'te yanlış çift 0; yedi belge maddesi yazıldı

**Ne yapıldı:** Tailwind 4'ün belirsiz `text-`/`font-` yardımcılarında etiketsiz `var()` değerini yakalayan ESLint kuralı **düzeltmeden önce** yazılıp kablolandı (gerçek depoda 34 hata / 12 dosya — 6.6b sayımıyla birebir), sonra 34 geçiş etiketlendi ve soğuk build'in CSS'inde yanlış çiftlerin 0, doğru çiftlerin var olduğu ölçüldü; yol üstünde ikinci bir bulgu çıktı (Tailwind yorumları da tarıyor — 15 yorum örneği ölü CSS üretiyordu, düzeltildi); DZ-21 metni, SKILL.md CI kuralı, §16.2, 6.8 notu, §0.5 sayımı, CI #121 düzeltmesi ve SORUN-003 yazıldı.

**Kapılar (bu ağaç, yüksüz):** gaps [x] 20·3·17·0✗ · debt [x] 16·5·11·0✗ · format [x] 0 · i18n [x] 117/92/3 · 58 dosya · görünmez 425 (rapor dâhil) · arch [x] temiz · typecheck [x] 11/11 · lint [x] **0 (303)** · test:coverage [x] **1746/118 + 1 skipped** (§③ kirli ağaç) · test:db [x] 301/10 · build [x] 8/8 **SOĞUK ×2** · fonksiyon **%85,71 (522/609)**
**Commit:** yazım anında bilinmiyor · push [ ] (rapordan sonra) · ANLIK DURUM güncellendi [x] · CHECKPOINT güncellendi [x]

**Kanıtlar:**
- **Kapı tabanı yeniden ölçüldü** (`ea970b5`, yerel, FC26 kapalı — 6.6b'nin kırmızısının aksine): test **1702/1702 · 117 dosya · 0 skipped**, §③ `atlanan: yok` · fonksiyon **%85,71 (522/609)** · lint 0 (301) · i18n 117/92 · görünmez 422 · gaps 20·3·17 · debt 16·5·11 · typecheck 11/11. Devir notu = CI #121 ile aynı; fark yok.
- **SORUN-002 yeniden sayıldı:** kod satırlarında (yorum hariç) **34 geçiş / 12 dosya** — avatar 2 · badge 2 · button-variants 3 · button 1 · combobox 5 · dialog 4 · form-indicator 5 · input 2 · select 4 · tabs 2 · toast 2 · tooltip 2. Kısa sözdizimi `text-(--x)` depoda **0**. Eski literali iddia eden test **0** (4 eşleşme, hepsi yorum).
- **twMerge 3.6.0 ölçüldü** (`packages/ui/node_modules/tailwind-merge`): `text-[var(--text-sm)] text-[var(--text-primary)]` → **`text-[var(--text-primary)]`** (boyut düştü) · `font-[var(--font-ui)] font-medium` → **`font-medium`** (aile düştü) · `text-[length:var(--text-sm)] text-[var(--text-primary)]` → **ikisi de kaldı** · `font-[family-name:var(--font-ui)] font-medium` → ikisi de kaldı · `text-[length:var(--text-xs)] text-[length:var(--text-sm)]` → sonuncusu (doğru birleşme) · `text-(length:--text-sm) text-(--text-primary)` → ikisi de kaldı. Kullanıcının sorusu — *"aynı grupta mı?"* — **hayır**, etiketli boyut font-size grubunda, renk text-color grubunda; `cn()` düşürmüyor.
- **Tailwind 4.3.3 kaynağı okundu (F4):** `dist/lib.js` — `function Q(e,i){if(e.startsWith("var("))return null;…}`; `font-`: `switch(a.value.dataType??Q(g,["number","generic-name","family-name"]))` → `generic-name|family-name` → `font-family`, **default → `font-weight`**; `text-`: `Q(g,["color","length","percentage","absolute-size","relative-size"])` → uzunluk türleri → `font-size`, **default → renk**. Kural bu listeleri taşıyor ve testi adıyla iddia ediyor.
- **Nöbetçi önce — kanarya gerçek depoda öttü (DZ-12):** kural + test + kablolama yazıldı, `eslint . --format json` → **linted 303 · 35 problem / 13 dosya**: `local/no-untyped-arbitrary-value:untypedValue` **34** (12 dosya, sayımla birebir) + `preserve-caught-error:missingCause` 1 (kuralın kendi `catch`i — düzeltildi, günlük #42). Sonra etiketleme → `lint` **0 / 303**.
- **Etiketleme:** Node betiği, yalnızca kod satırları: `font-[var(--font-*)]` → `font-[family-name:var(--font-*)]` (15) · `text-[var(--text-<boyut>)]` → `text-[length:var(--text-<boyut>)]` (19); 12 dosya, 22 satır (prettier bir satırı 2'ye kırdı: 23 ekleme / 22 silme). Etiketli biçim depoda toplam: `family-name` ui 23 · mono 4 · `length` sm 13 · xs 5 · 2xs 4 · lg 1 · base 1.
- **Derlenmiş CSS'te doğrulama — iki soğuk build (`turbo run build --force`, `Cached: 0 cached, 8 total`):** ① ilk build: doğru çiftler **var** (`font-size:var(--text-2xs|xs|sm|base|lg)` 5 · `font-family:var(--font-ui)` 2 · `font-family:var(--font-mono)` 2) ama yanlış çiftler **hâlâ var** (`font-weight:var(--font-ui|mono)` · `--tw-font-weight:var(--font-*)` · `color:var(--text-2xs|sm)`), CSS 33.820 B. Sebep ölçüldü: `apps/web/src/app/theme.css:37` `@source '../../../../packages/ui/src'` — Tailwind düz metin tarar; kalan adaylar **yorumlarda** (`attribute-badge.tsx`:86/88 · `club-crest.tsx`:69/76 · `currency-value.tsx`:35/38/42 · `date-chip.tsx`:46/48 · `player-portrait.tsx`:82 · `currency-value.test.tsx`:94 · `date-chip.test.tsx`:17/146 · `player-portrait.test.tsx`:177 — 15 geçiş). Yorumlar `[…var(` biçimine çevrildi (ASCII dışı aday reddediliyor; depoda `font-[var(…)]` emsali). ② ikinci build: yanlış çift **0** (`font-weight:var(--font-*)` 0 · `--tw-font-weight:var(--font-*)` 0 · `color:var(--text-<boyut>)` 0), doğru çiftler aynı; CSS **33.844 → 33.778 B**, JS **390.629** (değişmedi). Renkler yerinde: `color:var(--text-primary)` 5 · secondary 1 · muted 3 · inverse 2.
- **İkinci liste kararı (DZ-07) — kaynaktan türetme, gerekçesiyle:** kural `packages/ui/src/theme/tokens.generated.css`i `readFileSync` ile yükler; her `--ad: değer;` satırı değerin şekliyle sınıflanır (`#hex`/`rgb(`/`oklch(` → renk · `px|rem|em|%|…` → uzunluk · tırnak ya da `sans-serif|serif|monospace|system-ui|ui-monospace` → aile · çıplak sayı → ağırlık). Neden bu dosya: token TS kaynağının yansıması ve tazeliğini `css-projection.test.ts` zaten iddia ediyor (bayat dosya `pnpm test`i kırar) — ESLint kuralı TS'i içe alamaz, üretilmiş CSS'i okuyabilir. **Ayrışma nöbetçisi ayrıca var** (testin ② katmanı): her `--text-*`/`--font-*` bilinen sınıfa düşer (histogram tam olarak `color · family-name · length`), `text-sm`→length · `text-primary`→color · `font-ui`→family-name, dosya yoksa yükleyici **fırlatır**. Alternatif (elle liste + ayrışma nöbetçisi) elendi: ayrışma nöbetçisi listeyi kaynağa bağlayınca liste zaten kaynaktan türetilebiliyor demektir — ikisini birden tutmak aynı bilgiyi iki yerde tutmaktır. Üçüncü yol (her `text-[var(…)]`e `color:` zorunlu kılıp listesiz kalmak) K12 gereği elendi: ~45 renk sınıfını daha değiştirirdi ve renkler doğru çalışıyor.
- **Kural sözleşmesi:** `untypedValue` (token türü ≠ yardımcının varsayılanı, ipucu yok) · `unknownToken` (token CSS'te yok — kapalı-güvenli) · `hintMismatch` (ipucu ≠ token türü). İki sözdizimi (`[var(--v)]` · `(--v)`), varyant önekleri, `/değiştirici`, JSX niteliği, şablon parçaları. Kapsam iki önek; diğerleri V2-BACKLOG.
- **Kablolama kanaryası (③ katman):** `calculateConfigForFile('packages/ui/src/components/form-indicator.tsx')` → `[2]`; `lintText` üç mesaj kimliğini de veriyor; `*.test.tsx` → `[0]` (muafiyet iddiası); `*.stories.tsx` / `*.spec.tsx` → `[2]` (muafiyet dar).
- **Yeni ayar yüklendi mi:** `eslint.config.js` değişikliği **ölçüldü** — kanarya `[2]` döndü ve `pnpm lint` gerçek depoda 34 öttü (dosyanın varlığı değil, etkisi).
- **Mutasyon 4/4 ısırdı** (yedek → md5 → boz → koş → yedekten geri → md5 aynı; DETAY §3).
- **D5:** paket `dist` ✅ — `packages/ui/dist/index.js` `import()` edildi, `contrastRatio`/`attributeBadgeForeground` çağrıldı (bant 1 = 4,555), `dist/components/badge.js` etiketli sınıfı taşıyor · web paketi ✅ (yukarıdaki CSS ölçümü) · **imaj koşturulmadı** — CI'da okunur.
- **Devir (DZ-14):** SORUN-003 → **6.12** kapsamında adıyla (*"SORUN-003 BURADA KARARA BAĞLANIR"*); `gaps:check`/`debt:check` yeşil (SORUN kütüğünü hiçbiri okumuyor — devir elle kontrol edildi, ROADMAP 6.12 bloğunda). 6.8 bant 1 notu 6.8 bloğunda; §0.5 listeleri 6.7 ve 6.8 bloklarında.
- **`format:check` bu commit'te neye baktı:** `eslint.config.js` (js) · `no-untyped-arbitrary-value.js` · `.test.mjs` · `index.js` · 21 `.tsx/.ts` bileşen/test dosyası — `prettier --check .` *"All matched files use Prettier code style!"*; `CLAUDE.md`, ROADMAP, PROJECT_MEMORY, CHECKPOINT, SKILL.md, ajan `.md`leri, V2-BACKLOG, bu rapor **bakılmadı** (`*.md` `.prettierignore`).
- **`CLAUDE.md` §14'e dokunulmadı** (`git diff CLAUDE.md`: §16.2 madde 6 + §18.2 DZ-21 satırı).
- **Kırılması beklenen ve kırılan:** `lint` (34 → 0). **Kırılmayan ve sebebi:** bileşen testleri (eski literal iddia eden yoktu — ölçüldü; comment-only değişiklikler mantığa dokunmadı) · `css-projection` (token dosyasına dokunulmadı) · `inventory-guards` ① (yeni `*:check` betiği yok — kural `lint` içinde).
- **Bağlam yüzdesi:** ölçülemedi.

**KARARIN GEREKİYOR:** 1 madde
   1. **6.7'ye geçiş (K11)** — DataTable; §0.5'e göre 8 birim, sığıyor; `planci` açılışta yeniden sayar; etiketli sınıf idiyomu + kural altında.
      Önerim: **geç.** Gerekçe: 6.6b/6.6c'nin bütün devirleri kapandı; 6.7'yi bloklayan bir şey kalmadı.
      Cevap vermezsen: duruyorum.

**Yeni kayıt:** **SORUN-002 ✅ çözüldü (6.6c)** · **SORUN-003** (beş ağır test yük altında zaman aşımı — zamana duyarlı, mantığa değil; sahibi 6.12) · günlük **#41–#43** · *"Bilinen kayıt düzeltmeleri"* KAPANIŞ bloğu (CI #121) · V2-BACKLOG (diğer belirsiz önekler). Yeni BORÇ/SAPMA/G **yok**.

**Sıradaki:** 6.7 — DataTable motoru. Bu commit'in CI koşusu okunur (6/6 mi, §③ temiz ağaçta `son_commit_baslik` yeşil mi, İmaj koştu mu); sayıları 6.7'nin kaydına girer (SKILL kuralı).

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. ÖLÇÜM KOMUTLARI VE HAM ÇIKTILAR (①)

- **Sayım:** `P='font-\[var\(--font-[a-z]+\)\]|text-\[var\(--text-(2xs|xs|sm|base|lg|xl|2xl|3xl)\)\]'`; dosya başına `grep -vE '^\s*(\*|//)' "$f" | grep -oE "$P" | wc -l` → `total=34 files=12`. Kısa sözdizimi: `grep -rnE "(text|font)-\((length:|color:|family-name:)?--" … | wc -l` → 0. Testlerde: 4 eşleşme, hepsi yorum satırı (currency-value.test.tsx:94 · date-chip.test.tsx:17/146 · player-portrait.test.tsx:177).
- **twMerge:** scratchpad `twmerge-probe.mjs` (`createRequire` ile `packages/ui`nin `tailwind-merge`i, sürüm `package.json`dan `readFileSync` — `exports` haritası `require('tailwind-merge/package.json')`i reddetti, D2 küçük): 13 vaka, DROP/KEEP yukarıda.
- **Tailwind kaynağı:** `grep -oE '.{0,140}"family-name".{0,160}' node_modules/.pnpm/tailwindcss@4.3.3/node_modules/tailwindcss/dist/lib.js` → `Q()` ve iki `switch`.
- **Kapı tabanı:** `gates.sh` → `g66c-base/`: sekiz kapı EXIT 0; `Tests 1702 passed (1702)` · `All files 92.91 | 91.97 | 85.71 | 92.88` · `coverage-summary.json` functions 522/609.
- **Beş ağır test (SORUN-003):** `vitest run --reporter=verbose` beş dosya → `Tests 102 passed`, süreler: boot-failure 2440 ms · main `#root` 2361 ms · no-hardcoded-path uçtan uca 3126 ms · no-bare-jsx-text uçtan uca 3301 ms · i18n-check *"GERÇEK DEPO bugün TEMİZ"* 479 ms; `it` bütçeleri: web ve i18n 5.000 (varsayılan), ESLint 30.000 (açık).
- **Bant 1:** scratchpad `band-margin.mjs` `packages/ui/dist/index.js`i import etti → `{"i":1,"color":"#B04A3C","fg":"#E8ECF3","flat":4.555,"margin":0.055}`, `ATTRIBUTE_PATTERN_ALPHA=0.1`. Girdiler: `ATTRIBUTE_BADGE_FOREGROUNDS` + `ATTRIBUTE_BANDS` + siyah mürekkep — `--text-muted`/kenarlık yok.

## 2. KAPI ZİNCİRİ — bu ağaç (`gates.sh` → `g66c-after/`, her kapı kendi `.log` + `.exit`)

| Kapı | Çıkış | Ham satır |
|---|---|---|
| gaps:check | 0 | `kütükteki G satırı : 20 · atlanan (kapalı) : 3 · taranan (açık) : 17` |
| debt:check | 0 | `kütükteki BORÇ satırı : 16 · atlanan (kapalı) : 5 · taranan (açık) : 11` |
| format:check | 0 | *"All matched files use Prettier code style!"* |
| i18n:check | 0 | `tanımlı 117 · kullanılan 92 · dinamik ön ek 3 · taranan 58 dosya · 2 kök · görünmez tarama 424 dosya` (422 + kural + test) — rapor yazıldıktan sonra son ağaçta yeniden: **425**, EXIT 0; `gaps`/`debt`/`format`/`inventory-guards`+`glossary-check` (49 passed \| 1 skipped) de yeniden koştu |
| arch:check | 0 | `✓ arch:check temiz` (kapsam basmıyor — BORÇ-012) |
| typecheck | 0 | `Tasks: 11 successful, 11 total · Cached: 9 cached` |
| lint | 0 | `--format json`: **303 dosya · 0 hata · 0 uyarı** (düzeltme öncesi: 35 / 13 — 34 hedef + 1 `preserve-caught-error`) |
| test:coverage | 0 | `Test Files 118 passed (118)` · `Tests 1746 passed \| 1 skipped (1747)` · 32,72 s · `All files 92.91 \| 91.97 \| 85.71 \| 92.88` · `inventory-guards ③ atlanan: son_commit_baslik → ağaç kirli` |
| ↳ eslint-rules projesi | 0 | `Test Files 3 · Tests 107` (62 + **45** yeni) |
| test:db | 0 | `Test Files 10 passed · Tests 301 passed` |
| build (soğuk) | 0 | `turbo run build --force` → `Tasks: 8 successful · Cached: 0 cached, 8 total · 9.381s`; ikinci koşu aynı (`Cached: 0`) |

**D5:** paket `dist` ✅ (`import()` + çağrı; `dist/components/badge.js` `font-[family-name:var(--font-ui)]` 1 · `text-[length:var(--text-2xs)]` var) · web paketi ✅ (`index-DV3slQ54.css` 33.778 B · `index-CFzlxtBt.js` 390.629 B) · imaj **koşturulmadı**.

## 3. MUTASYON SERİSİ — 4 (hepsi ısırdı; yedekten geri, md5 ile)

| # | Hedef | Ne bozuldu (md5 önce → sonra) | Kırılan (adıyla) | Geri (md5) |
|---|---|---|---|---|
| M1 | **kural, gerçek depoda** | `badge.tsx` bir `length:` silindi (`c81f34c9` → `93604c9d`) | `eslint badge.tsx` EXIT 1 — **1 × `untypedValue`, :39:3** | ✅ `c81f34c9` |
| M2 | **türetme nöbetçisi** | sınıflayıcıda `px` uzunluk olmaktan çıktı (`6f2da61e` → `800755c2`) | **20 test**: RuleTester geçerli 6 + geçersiz ~10 · türetme *"her token bilinen sınıfa"* + *"üç ad"* + *"sınıflayıcı şekilleri"* · kanarya *"ötüyor"* | ✅ `6f2da61e` |
| M3 | **kablolama** | `eslint.config.js` kural `error` → `off` (`2674dd51` → `2045c8bd`) | **3 kanarya**: *"GERÇEK yapılandırma … [2]"* · *"ötüyor"* · *"MUAFİYET DAR"*; **ve `eslint badge.tsx` EXIT 0 kaldı** — kanaryanın var olma sebebi (BORÇ-010'un dersi) | ✅ `2674dd51` |
| M4 | **kapalı-güvenli** | `TOKEN_CSS_PATH` var olmayan dosyaya (`6f2da61e` → `58b77302`) | `eslint badge.tsx` **EXIT 2** — *"no-untyped-arbitrary-value: token dosyası okunamadı — C:\fms\packages\ui\src\theme\tokens…"*; testler 37/45 kırmızı | ✅ `6f2da61e` |

Sonrası: `git status` yalnızca amaçlanan değişiklikleri gösteriyor (mutasyon artığı yok).

## 4. BELGE MADDELERİ — nerede, ne yazıldı

- **DZ-21** (`CLAUDE.md` §18.2 satırı): *"Denetim `## ÇIKTI` üzerinden yapılır; orkestratör denetçiye yalnızca o alanı iletir."* + bedel sütununa günlük #33. `denetci.md`:9–11 ve `kapici.md`:14–15'te aynı *"verilmez ve verilirse okumazsın"* kalıbı **vardı** → *"iletilir; denetim o alan üzerinden yapılır (DZ-21)"*. Frontmatter dokunulmadı.
- **SKILL.md** kapanış listesine paragraf: CI sayıları yalnızca CI koşusu teslimatsa kendi commit'ini alır; aksi hâlde sonraki kayda. İlk uygulaması: bu kaydın *"Bilinen kayıt düzeltmeleri"* bloğu.
- **§0.5:** 6.7 → 8 birim (liste ROADMAP 6.7'de) · 6.8 → 13 birim (liste ROADMAP 6.8'de; sınırda; ③ ⑨ ⑩ karar taşıyor; 6.8a = ① ② ⑦ ⑧ / 6.8b = geri kalanı çizgisi hazır; **bölünme önerilmiyor**, karar kullanıcının).
- **§16.2** madde 6: moral eşikleri kalibrasyon; Faz 38 · 44.
- **6.8 notu:** bant 1 payı 0,055 — ⚠️ devir notu *"`--text-muted` ve kenarlık token'ları"* diyordu; ölçüm girdilerin `ATTRIBUTE_BADGE_FOREGROUNDS` + bant renkleri + siyah alfa olduğunu gösterdi; not **CVD alfası** riskiyle yazıldı (alfa 0,15'te bant 2 = 4,447 — 6.6 ölçümü).
- **"Bilinen kayıt düzeltmeleri":** KAPANIŞ (6.6c) — `ea970b5` · CI #121 6/6 · 1702/1702 · atlanan 0 · %85,71 · Entegrasyon 10·301.
- **SORUN-003:** beş test, bütçe, yüksüz süre, pay; sahibi 6.12 (ROADMAP 6.12'de adıyla). **SORUN-002:** ✅ satır güncellendi.
- **V2-BACKLOG:** kuralı diğer belirsiz öneklere genişletmek (bugün yakalayacağı hata oluşamıyor — DZ-12'nin tersi).

## 5. DEĞİŞEN / EKLENEN DOSYALAR

Yeni: `tools/eslint-local-rules/no-untyped-arbitrary-value.js` · `.test.mjs`. Değişen: `tools/eslint-local-rules/index.js` · `eslint.config.js` · 12 bileşen dosyası (etiket) · 5 bileşen + 3 test dosyası (yorum, `[…var(`) · `CLAUDE.md` (§16.2 · §18.2) · `.claude/agents/denetci.md` · `kapici.md` · `.claude/skills/faz-yurut/SKILL.md` · `docs/ROADMAP.md` (6.6c plan + SONUÇ · 6.7/6.8 §0.5 listeleri · 6.8 notu · 6.12 SORUN-003) · `PROJECT_MEMORY.md` (ANLIK DURUM · SORUN-002/003 · günlük #41–#43 · düzeltme bloğu) · `docs/CHECKPOINT.md` · `docs/V2-BACKLOG.md` · bu rapor. Token dosyaları, `CLAUDE.md` §14, i18n: **dokunulmadı**.

## 6. YAPILMAYANLAR — açıkça

Renk sınıflarına `color:` ipucu (çalışıyorlar; kural serbest bırakıyor) · diğer belirsiz önekler (V2-BACKLOG) · beş testin bütçesi (SORUN-003, 6.12) · 6.7 · imaj yüzeyi yerelde (CI'da okunur) · 6.3b'nin üç kenarlığı ve `--text-muted` (6.8).
