## 🟡 KARAR 6.4-ön — Geriye dönük denetim ve kapı genişletmesi

> **İçerik commit'i:** yazım anında bilinmiyor · **CI koşusu:** ÖLÇÜLECEK (push sonrası) · **PR:** yok (faz sonunda)
> **Bağlam yüzdesi:** ölçülemedi

**Ne yapıldı:** Sekiz ölçülmüş hata sınıfı geriye dönük tarandı; bulguların hepsi bir eve taşındı ve iki yenisi koşan kapıya dönüştü.

**Kapılar:** typecheck [x] 11/11 · lint [x] 0 · format [x] · test [x] 1315/87 · **test:db [x] 301/10** · build [x] **8/8 SOĞUK** · arch [x] · **gaps [x] 20·3·17·0✗** · **debt [x] 12·5·7·0✗** · i18n [x] **341 dosya** (bu rapor dahil)
**Commit:** yazım anında bilinmiyor · push — bilinemez · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- **A — kapı ilk koşusunda KIRMIZI:** açık 6 BORÇ satırının **5'i** (001·002·004·006·007) hedef fazının ROADMAP bölümünde geçmiyordu → beşi de Faz 16/50/12 kapsamına **adıyla** yazıldı → kapı yeşil. Ayrıca `BORÇ-010`un satırı **4 hücreliydi** (başlık 5 diyor) — 5.6'dan beri görünmeyen bir bozukluk, ayrıştırıcı ilk koşuda buldu.
- **İki yönlü kanıt:** sahipsiz satır uydurulunca **ötüyor** (exit 1, satırı adıyla) · bugünkü depoda **susuyor** (exit 0) · **kaç satır taradığını basıyor** (12·5·7).
- **Mutasyon 3/3, üçü de kırdı:** ① `ci.yml`den `debt:check` adımı → nöbetçi ① kırıldı ② `CLAUDE.md` ağacından bir betik adı → nöbetçi ② kırıldı ③ çekirdeğin sütun kontrolü körleştirildi → 2 test kırıldı. Üçü de **md5 ile** yerine oturdu ve **dosya yedeğinden** geri alındı (`git checkout` değil).
- **Refactor kanıtı:** `gaps:check` çekirdeğe taşındı, çıktı **bayt bayt aynı** (md5 `24fe781e…` → `24fe781e…`).
- **D — 3 riskli ayar ölçüldü:** turbo `globalEnv` ✅ (hash `a58f9247`→`893eb7b4`→`a58f9247`) · `globalDependencies` ✅ (içerik değişti → 3 hash değişti) · vitest `sourceResolve` ✅ (8/8 TS projesi, kökte `resolve` **yok**).
- **H① — üç yüzeyin üçü:** CI `33949199057` **6/6 iş yeşil**, adım listesi tek tek okundu (imaj derlendi **ve koşturuldu**, amd64+arm64).
- **Üretim paketi bayt bayt aynı:** JS **383.976** · CSS **17.064** — uygulama yüzeyine dokunulmadı.

**KARARIN GEREKİYOR:** 1 madde
   1. `docs/spec/09-quality-protocol.md` §11.5'e **iki satır eklendi** (`gaps:check`, `debt:check`) ve SAPMA-044 açıldı — ama `SESSION-TEMPLATE` *"spesifikasyonda eksik varsa **sor**"* diyor ve ben sormadan yazdım.
      Önerim: **kalsın**. Gerekçe: liste CI'da koşan iki kapıyı taşımıyordu, kısmi bir kapanış listesi D3 yanılsaması üretir ve düzeltme spec'i genişletmiyor, gerçeğe hizalıyor.
      Cevap vermezsen: bırakırım; *"geri al"* dersen iki satır ve SAPMA-044 tek commit'te sökülür.

**Yeni kayıt:** **BORÇ-012** (`arch:check` çıktısı kapsamını söylemiyor → **6.10**) · **SAPMA-044** (`spec/09` §11.5 kapı listesi eksikti)

**Sıradaki:** 6.4 — shadcn/ui uyarlaması + temel bileşenler I. Button (6 varyant), Input, Select, Combobox, Checkbox, RadioGroup, Slider, Switch, Tabs.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 0. ADIM 0 — durum tespiti

| Ölçüm | Sonuç |
|---|---|
| `pwd` | `/c/fms` ✅ |
| Dal / tepe | `feature/faz-06-design-system` · `b3a259e` · altında `cf12fa8`, `955bb66` ✅ |
| `git status --porcelain` | boş · `origin` ile eşit ✅ |
| Docker | **kapalıydı** — `docker version` `Server:` satırı yok, `wsl -l -v` → `docker-desktop Stopped`. Docker Desktop başlatıldı, `docker version --format '{{.Server.Version}}'` → **29.7.2** |
| Kapılar (giriş) | typecheck 11/11 · lint 0 · format 0 · arch temiz · gaps **20·3·17·0✗** · i18n temiz (**336** dosya) · test **1290/85** · test:db **301/10** · fonksiyon **409/495 = %82,62** — hepsi beklenenle aynı |
| Kütükler | sorun **0** · BORÇ **11** (5 ÖDENDİ → 6 açık) · SAPMA **43** · G **20** — hepsi beklenenle aynı |
| CI | `gh run list` ile **liste sorgusu**; 6.3b'nin koşusu `33949199057` **success** ve **adım listesi okundu** |

⚠️ **Kapı zinciri BORUSUZ koştu** — her komut kendi çıkış kodunu ayrı yazdı
(`########## END <ad> EXIT=<kod> ##########`). Bir boru hattında `$?` son
komutun kodunu okur ve bu tuzak bu depoda iki kez ısırdı.

⚠️ **`docker version 2>&1 | head -30` "EXIT: 0" dedi** — oysa daemon kapalıydı.
Aynı tuzağın kendisi. Karar `Server:` satırının **yokluğuna** bakılarak verildi.

---

## 1. A — SAHİPLİK: `debt:check` kuruldu

### 1.1 Uygulama kararı — **ORTAK ÇEKİRDEK** (yazmadan önce verildi)

Üç seçenek tartıldı:

| Seçenek | Neden seçilmedi / seçildi |
|---|---|
| İkinci bir betik, kopyala-yapıştır | **Reddedildi.** *"İki uygulama bir gün ayrışır"* — ve ayrışacak yarı tam olarak **ortak** olan yarı: ROADMAP dilimleme, kaçışlı boru işaretine dayanan hücre bölme, üstü çizili eleme, rapor biçimi |
| Tek betiğe ikinci mod (`--ledger=debt`) | **Reddedildi.** İki kütük **farklı dosyalarda**, farklı sütun düzeninde, farklı kapanış kelimesiyle ve farklı faz yazımıyla yaşıyor; tek betiğe sıkıştırmak yine aynı çekirdek+adaptör yapısını üretir, yalnızca örtük hâlde. Ayrıca `gaps:check` adı ROADMAP, `SESSION-TEMPLATE` ve `ci.yml`de yaşıyor |
| **Ortak çekirdek + iki ince giriş** | ✅ **SEÇİLDİ.** `scripts/lib/ledger-coverage.mjs` ortak; `check-gap-coverage.mjs` ve `check-debt-coverage.mjs` yalnızca **kendi sözleşmesini** taşıyor (dosya · sütun · kapanış kelimesi · faz ayracı) |

⚠️ **İki giriş = CI'a bağlamayı iki kez unutma şansı.** Bu risk ayrı bir
nöbetçiyle kapatıldı (bölüm 5): `inventory-guards.test.mjs` ① kök
`package.json`daki her `*:check` betiğini `ci.yml`de arar.

**Refactor bedava değildi ve bedeli ölçüldü:** `gaps:check`in çıktısı taşımadan
önce ve sonra dosyaya alındı — **md5 `24fe781e8b42dd232a3713e5c5faeec0` her
ikisinde de**, `diff` boş. Tek bilinçli davranış değişikliği: satır şekli
kontrolü `< 4`ten **`!== 4`e** sıkıştı (ölçüldü: 20 G satırının **20'si** tam 4
hücre; fazladan bir hücre de sütunları kaydırır, eksik olan kadar tehlikeli).

### 1.2 Kütüğün yazım deseni — **ölçüldü, varsayılmadı**

11 satırın hepsi ayrıştırıldı:

| Gözlem | Ölçüm |
|---|---|
| Sütun sayısı | 10 satır **4 hücre** (kimlikten sonra), **BORÇ-010 → 3** |
| `✅ ÖDENDİ` işaretinin sütunu | **sabit değil**: 003/005 → **2.**, 010 → **3.**, 008/011 → **4.** |
| Vade sütununun başı | açık satırların hepsinde **kalın sayı**: `**16**` · `**50**` · `**12**` · `**6**`; 011'de `**6.4**` |
| Hücre içindeki diğer kalın sayılar | **var** — BORÇ-011'in vade hücresinde `**0**` geçiyor. *"Hücredeki tüm kalın sayılar"* yöntemi **gürültülü** olurdu |

→ Bu yüzden **kapanış satırın tamamında** aranıyor (sütuna sabitlemek yanlış
sonuç verirdi) ve **vade hücrenin başına sabitlendi** (tüm hücreyi taramak
gürültü üretirdi). İkisi de `gaps:check`in yönteminden **bilerek farklı**;
kopyalamak değil uyarlamak gerekti (4.8 idiomu).

### 1.3 Kapının bulduğu — iki ayrı sınıf

**① ŞEKİL BOZUKLUĞU (kimsenin görmediği).** İlk koşu:

```
Error: BORÇ-010: satır 4 hücre taşımalı, 3 taşıyor — kütük satırı bozuk.
```

`BORÇ-010`un satırı **5.6'da dört hücreyle yazıldı**; tablo başlığı beş sütun
bildiriyor. Markdown eksik hücreyi sessizce boş gösterdiği için gözle
görünmüyordu ve **beş alt görev boyunca kimse fark etmedi**. Eksik sütun
(*"Ödenmesi gereken faz"*) 6.4-ön'de dolduruldu.

**② SAHİPSİZ SATIRLAR.** Onarımdan sonraki koşu:

```
  kütükteki BORÇ satırı : 11
  atlanan (kapalı)   : 5 — BORÇ-011, BORÇ-010, BORÇ-003, BORÇ-005, BORÇ-008
  taranan (açık)     : 6
    ✓ BORÇ-009 → Faz 6
    ✗ BORÇ-001 → Faz 16
    ✗ BORÇ-006 → Faz 50
    ✗ BORÇ-004 → Faz 16
    ✗ BORÇ-007 → Faz 12
    ✗ BORÇ-002 → Faz 16
```

**Altı açık satırın beşi sahipsizdi** — ve kural (*"kapsam taşıması kütüğe
kayıtla bitmez"*) 4.11'den beri yazılı. Tek sahipli satır **BORÇ-009**, ve o da
5.9'un **elle** yaptığı bir taşımaydı.

### 1.4 Beşinin evi — ve hedef fazın **yapabildiğinin** doğrulanması

| Satır | Ev | Hedef faz bunu **yapabilir mi** |
|---|---|---|
| **BORÇ-001** (`ioredis` 5→6) | **Faz 16** kapsamı, yeni blok | ✅ kuyruk bu fazda kuruluyor (`BullMQ kuyruğu + SSE`) |
| **BORÇ-002** (`bullmq` 5→6) | **Faz 16** kapsamı | ✅ aynı; bullmq 6'nın peer değişikliği **kuyruk yapılandırmasını** ilgilendiriyor |
| **BORÇ-004** (`job.data.correlationId`) | **Faz 16** kapsamı | ✅ üretici ve tüketici bu fazda birlikte doğuyor |
| **BORÇ-006** (Sentry kaynak haritası yükleme) | **Faz 50** — madde **zaten vardı**, kimlik yoktu | ✅ dağıtım hattı orada; ⚠️ *"bir sahibin adını yazmak devretmek değildir"*nin saf vakası |
| **BORÇ-007** (master'a `GRANT SELECT`) | **Faz 12** kapsamı, yeni blok | ✅ `apps/api` veritabanına **bu fazda** bağlanıyor — kısıtlanacak bir bağlantı daha önce **yoktu** |

### 1.5 SAPMA denetlenebilir mi — **HAYIR, ve sebebi ölçüldü**

SAPMA tablosunun sütunları ölçüldü:
`ID | Tür | Faz | Sapma | Gerekçe | Spec/ROADMAP güncellendi mi`

**Bir vade fazı sütunu YOK.** Bir SAPMA bir borç değil, olmuş bitmiş bir kayıt;
*"hangi fazda ele alınacak"* sorusu onun için tanımsız. Denetlenebilir bir hedefi
olmadığı için kapının dışında — **uydurma bir hedef sütunu eklemek SAPMA-026
olurdu**. Karar `check-debt-coverage.mjs`in başlığına gerekçesiyle yazıldı.

ℹ️ SAPMA'nın son sütunu (*"Spec/ROADMAP güncellendi mi"*) **başka** bir
denetlenebilir iddia taşıyor: adı geçen dosyanın gerçekten değiştiği. Bu
**farklı bir kontrol** ve bu alt görevin kapsamı değil (K12) — yazıldı, yapılmadı.

### 1.6 Kapının kapsamı — yazılı, sessizce daralmasın diye

**Denetlenen:** açık her satırın kimliği, vade fazının `## FAZ N` gövdesinde
düz metin olarak geçiyor mu.
**Denetlenmeyen:** satırın o fazda gerçekten **yapılabilir** olduğu (insan
kararı — bölüm 1.4'te elle doğrulandı), kapalı satırların doğruluğu, fazın alt
görev listesi, **SAPMA kütüğü**.

---

## 2. B — RAPORDA KALAN GÖZLEMLER

**Yöntem:** `docs/reports/faz-04` · `faz-05` · `faz-06` (**30 dosya**) sekiz
desenle tarandı (`sahibi` · `devir` · `taşındı` · `ölçülmedi/ÖLÇÜLECEK` ·
`sonraki faz` · `ertelendi/açık uç` · `Faz N`'e (yönelme hâli) · `6.N`'e).
**137 aday satır** çıktı; devir iddiası taşımayanlar (rapor şablonunun
*"Bağlam yüzdesi: ölçülemedi"* satırı gibi) elendi.

**Bulunan: 0 sahipsiz devir iddiası.** Sekiz aday tek tek doğrulandı:

| Gözlem | Kaynak | Evi var mı |
|---|---|---|
| Chromium ikilisi arm64'te koşuyor mu — **ÖLÇÜLMEDİ** | 6.0 ③ | ✅ ROADMAP **Faz 17**, G-02'nin yanında (`arm64`/`Chromium` geçiyor) |
| `happy-dom` beş boşlukta ne yapıyor — ölçülmedi | 6.0 | ✅ `DEPENDENCY-WATCH` satır 535, Faz 6.0 kaydı |
| Markalı kimlik tipleri | 4.3/4.4 | ✅ **G-17 → Faz 12**, kapsamda adıyla |
| `referees.ts:23` karşılıksız cümle | 4.10 → *"4.11'e sahiplendirildi"* | ✅ 4.11'de **yapıldı** (faz kaydı §5 satır 36) |
| `bash-text-guard` kablolaması ölçülemedi | 4.6 | ✅ 4.7'de ölçüldü (faz kaydı §5 satır 26) — ⓘ bu oturumda **üçüncü kez** doğrulandı: bir `node -e` çağrısı kanca tarafından reddedildi |
| CSP hiç yazılmamış | 6.3 | ✅ **Faz 50** kapsamında adıyla |
| `--text-muted` / `--accent` kontrast kusurları | 6.2 → 6.3 | ✅ ROADMAP **6.8** maddesinde adıyla |
| *"Başarısız bir adım işi kırıyor mu"* — 5.6 *"ölçülemedi, çıkarım"* | 5.6 → 5.9 | ⚠️ **BUGÜN ÖLÇÜLDÜ ve eve taşındı** (aşağıda) |

**Sekizincisi bir çıkarımdı, artık bir gözlem.** Kırmızı koşu `33937132528`
adım listesiyle okundu: `Web imajını derle` **failure** → iş **failure** →
sonraki dört adım **skipped** → koşu **failure**. Yani maskelenmemiş bir adımın
hatası işi gerçekten kırıyor.
⚠️ **Kapsam dürüstçe yazıldı:** gözlem `İmaj` işinden geldi, bir `Kalite
kapıları` adımından değil. Ev: `inventory-guards.test.mjs`teki maskeleme
iddiasının **üstünde**, yani iddianın dayandığı yerde (*"nöbetçi hatanın
olacağı yerde yaşar"*).

---

## 3. C — BAYAT PROSE

On yüzey bugünkü gerçeğe karşı ölçüldü. **Dört bayat, altı temiz.**

| Yüzey | Ölçüm | Sonuç |
|---|---|---|
| `CLAUDE.md` §2.2 ağacı | `apps` 3/3 ✅ · `packages` 4/4 ✅ · `tools` 6/6 ✅ · **`scripts` 0/6** · `docs/` alt dizinleri: `reports/` ve `schema/` **yok**, `LEGAL/` **var ama diskte yok** | 🔴 **BAYAT — üçüncü kez** |
| `CLAUDE.md` §2.3 `.env.example` | blok `NODE_ENV=development` ile **başlıyor**; `.env.example`ta o değişken **yok** ve `check-env-file.mjs` onu görürse **exit 1** | 🔴 **BAYAT — üstelik koşan bir kapıyla ÇELİŞİYOR** |
| `README.md` *"Anayasa (14 değişmez kural)"* | `CLAUDE.md`de **K1…K15** = **15** | 🔴 **BAYAT** |
| `README.md` `docs/` envanteri | 9 belgeden **2'si yok**: `SPEC-COVERAGE-GAPS.md` (Faz 2.0, CI kapısı var) · `glossary.md` (Faz 5.7, bu fazın bağlayıcı sözleşmesi) | 🔴 **BAYAT** |
| `CLAUDE.md` §2.1 yığın tablosu | 45 kurulu paket tarandı; adı geçmeyen 22'nin hepsi ya adı geçen bir girdinin çalışma zamanı yoldaşı (`react-dom`↔React, `rxjs`/`reflect-metadata`↔NestJS) ya da **`DEPENDENCY-WATCH`te kayıtlı** (`@fontsource-variable/*` → satır 31, `tailwindcss` → 32) | 🟢 temiz |
| `CLAUDE.md` belge haritası | 23 satır, hepsi diskte var; eksik olan tek belge `HOSTING-FALLBACK.md` | 🟡 eklendi (+`docs/schema/world.md`) |
| `docs/schema/world.md` | **koşan bir nöbetçisi var** — `packages/db/integration/er-diagram.itest.ts` diyagramı gerçek şemadan yeniden üretip birebir karşılaştırıyor | 🟢 temiz, ve **korumalı** |
| `docs/DEPENDENCY-WATCH.md` | Faz 6 satırlarının hepsi güncel (6.0'da beş satır eklendi, 6.3'te ikisi ✅) | 🟢 temiz |
| `docs/SESSION-TEMPLATE.md` §15.1 | Faz 6 satırı `Bölüm 7 + docs/glossary.md`; ikisi de doğru yeri gösteriyor | 🟢 temiz |
| ROADMAP Faz 6 *"Ana dosyalar"* bloğu | **Faz 6'nın böyle bir bloğu YOK** (`grep` → 0 eşleşme; bloklar yalnızca Faz 1, 2, 5'te) | 🟢 bakılacak bir şey yok |

**Çare bir düzeltme değil, bir nöbetçi.** `tools/` ağacı **iki kez** (4.11, 5.9)
bayatlamıştı; bugün üçüncü kez aynı ağaç bayat çıktı. `scripts/inventory-guards.test.mjs`
② artık **diskten belgeye** tarıyor: `apps`/`packages`/`tools` altındaki her
dizin, `scripts/` altındaki her betik, `docs/` altındaki her alt dizin ve her
`*.md` belge, `CLAUDE.md`de anılıyor mu.

⚠️ **Nöbetçinin kapsamı dar ve yazılı:** yön **tek**. Ağaçta olup diskte olmayan
*"hayalet"* girdi denetlenmiyor, çünkü ağaç bilerek **planlanan** dizinleri de
gösteriyor (`docs/LEGAL/` Faz 13'te açılacak, `data/` `.gitignore`da). Bu, ağacın
`LEGAL/` satırının **bugün yok** diye işaretlenmesiyle telafi edildi.

---

## 4. D — "YAZILMIŞ AMA HİÇBİR ŞEY YAPMAYAN AYAR"

### 4.1 Ölçülen üç ayar (riskli = bir kapının davranışını belirleyen)

| # | Ayar | Yöntem | Sonuç |
|---|---|---|---|
| ① | `turbo.json` → `globalEnv: ["NODE_ENV","PUBLIC_BASE_PATH"]` | `turbo run build --dry=json` hash'i üç kez | `/fms` → **`a58f92478cde00a0`** · `/degistirildi` → **`893eb7b4d2a67915`** · `/fms` → **`a58f92478cde00a0`** — **iki yönlü**, ayar **çalışıyor** |
| ② | `turbo.json` → `globalDependencies: ["tsconfig.base.json", ".npmrc"]` | dosya içeriği değiştirildi (md5 `3d4a194d`→`32c86cc1`), hash okundu, **dosya yedeğinden** geri alındı (md5 geri döndü, `git status` boş) | web build hash `a58f9247`→**`3eff37766627a2d5`**; üç paketin üçü de değişti — ayar **çalışıyor** |
| ③ | `vitest.config.ts` → `resolve: sourceResolve` (6.3b'nin BORÇ-011 düzeltmesi) | 13 proje ayrıştırıldı | **8 TS projesinin 8'i** alias taşıyor · **kökte `resolve` YOK** (6.3b'nin kusuru taşınmadı, **kaldırıldı**) · 5 `.mjs` aracı bilerek dışarıda ve doğrulandı: hiçbiri `@fms/*` **import etmiyor** |

**Ucuz ekstralar (aynı sınıf, tek komutluk):** `.gitignore`'un `!.env.example`
negasyonu ✅ (dosya izleniyor) · `.prettierignore`'un beş girdisi ✅
(`--file-info` ile tek tek, `--check` ile **değil** — gerekçe günlük #12).

### 4.2 ÖLÇÜLMEYENLERİN LİSTESİ — ve neden

| Ayar | Neden ölçülmedi |
|---|---|
| `.npmrc` `engine-strict=true` | **Pozitif** etkisi (uyumsuz `engines` taşıyan bir bağımlılığın reddi) ancak böyle bir paket kurularak ölçülür — kurulum ağacını kirletir. **Negatif** etkisi zaten ölçülü ve yorumda yazılı (kök `engines.node`'u zorlamıyor; çare `preinstall` kapısı) |
| `.npmrc` `strict-peer-dependencies=true` | Bir peer çakışması **üretmeden** ölçülemez. 6.0 bu ayardan **çıkarım yaptı** (zincir çözülüyor mu), ayarın kendisini sınamadı |
| `turbo.json` `globalDependencies`in **`.npmrc` yarısı** | Mekanizma `tsconfig.base.json` ile kanıtlandı; ikinci dosya için tekrarlanmadı |
| `turbo.json` `ui: "stream"` | Kozmetik — hiçbir kapının davranışını belirlemiyor |
| `tsconfig.base.json`ın 14 katılık bayrağı | Etkileri **toplu** olarak `pnpm typecheck`in geçmesiyle kanıtlanıyor; bayrak bazında ölçüm 14 ayrı kontrol deneyi demek ve bir denetim alt görevinin kapsamı değil (K12) |
| `eslint.config.js` | Kurallarının kablolaması 5.5/6.1'de **kanaryayla** ölçüldü (BORÇ-010); bu oturumda yeniden ölçülmedi |

---

## 5. E — KAPILAR CI'A BAĞLI MI

**Ölçüm (tam eşleşme, bkz. günlük #13):** kök `package.json`ın 16 betiği
`ci.yml`in `run:` satırlarıyla karşılaştırıldı.

| CI'da olan | CI'da OLMAYAN | Bilinçli mi |
|---|---|---|
| `typecheck` · `lint` · `format:check` · `test:coverage` · `build` · `arch:check` · `i18n:check` · `gaps:check` · `test:db` (+ bugün **`debt:check`**) | `format` · `lint:fix` (yazıcı kipler) · `test` (`test:coverage` kapsıyor) · `dev` · `test:watch` · `preinstall` (`pnpm install` + ayrı bir `node scripts/check-node-version.mjs` adımı) | ✅ hepsi bilinçli — **0 unutulmuş kapı** |

**Maskeleme:** `continue-on-error` → **yalnızca bir yorumda** (satır 99, `i18n:check`in
gerekçesi) · `|| true` → **0** · adım `if:` → **0**. Maskeleme **yok**.

**⚠️ Ama bu bir GÖZLEMDİ, bir kontrol değil** — ve `gaps:check` 4.11'de yazılıp
**beş alt görev** boyunca bağlanmamıştı. `scripts/inventory-guards.test.mjs` ①
artık kök `package.json`daki adı **`:check` ile biten** her betiği `ci.yml`de
arıyor ve maskelemenin yokluğunu iddia ediyor.

**Kapsamı DAR ve çıktıya basılıyor:** `arch:check · gaps:check · debt:check ·
i18n:check · format:check`. ⚠️ **`perf:budget` bu desene GİRMİYOR** — desen ada
bakıyor, işe değil. ROADMAP 6.10 maddesi bunu **adıyla** söylüyor: o alt görev ya
kapıyı `:check` ile biten bir adla kurar ya deseni genişletir.

ℹ️ `tools/glossary-check` için ayrı bir betik/adım **bilerek yok** — `vitest.config.ts`
onu bir test projesi olarak taşıyor ve gerekçe orada yazılı. Bir eksiklik değil.

---

## 6. F — TALİMATLARDAKİ BAYAT SAYILAR

| Talimat | İddia | Gerçek | Çare |
|---|---|---|---|
| ROADMAP **6.12** | *"**yedi** kabul kriteri tek tek"* | **6** (6.1'in daraltması sonrası) | Sayı **kaldırıldı**: *"aşağıdaki kabul kriteri listesinin tamamı"* |
| ROADMAP **6.10** | *"CI'ın işi bugün **8 adım** taşıyor … bu **dokuzuncusu**"* | 8 ✅ **bugün doğru** — ama 6.4-ön dokuzuncuyu ekledi, yani **anında bayatlardı** | Sayı **kaldırıldı**, yerine koşan nöbetçiye atıf + `perf:budget`in desene girmediği uyarısı |
| `SESSION-TEMPLATE` **adım 6** | `pnpm typecheck && pnpm lint && pnpm test` | Kapıların **yarısı yok**: `format:check` · `arch:check` · `i18n:check` · `gaps:check` | Liste **kaldırıldı**, kaynak `package.json`a çevrildi |
| `SESSION-TEMPLATE` **adım 17** | `pnpm typecheck lint test build arch:check` | Aynı eksiklik **+ komut olduğu gibi çalışmıyor**: pnpm bunu tek betik (`typecheck`) + dört argüman olarak okur, turbo argümanları görev adı sanıp kırılır | Adım 6'nın zincirine atıf yapıldı |
| ROADMAP **6.6** | *"56 nitelik + 77 çekirdek terim"* | ✅ **doğru** — `glossary-check` 133 · 77 · 47 · 10 sayılarını **testte** iddia ediyor (SAPMA-040 ile tutarlı) | dokunulmadı |

⚠️ **Sınır:** yalnızca **koşulacak** talimatlar tarandı. Faz kayıtları ve
`docs/reports/**` **geriye dönük değiştirilmedi** — 5.9'un *"yedi kriter"* cümlesi
(ROADMAP satır 2906) **Faz 5'in** kapalı alt görev kaydı ve Faz 5'in gerçekten
yedi kriteri var; dokunulmadı.

---

## 7. G — KRİTER ↔ KAPSAM/SPEC ÇELİŞKİLERİ

**Kapsam:** Faz 6'nın kriterleri + iş devrettiği fazlar (**17 · 18 · 49**;
denetim sırasında **12** ve **50** de eklendi). Kalan 44 faz **taranmadı** —
o, Faz 50'nin bütünsel denetimi (K12).

**Faz 6 — 6 kriter, 6'sı da kapsamda karşılık buluyor:** Storybook ✅ ·
`perf:budget` ✅ · DataTable ✅ · yedekli kodlama (ağırlık + desen) ✅ ·
klavye ✅ · axe ✅.

**6.1'in devrettiği yarılar — 9/9 hedef fazında ADIYLA duruyor:**

| Devredilen | Hedef | Ölçüm |
|---|---|---|
| G-05 görsel regresyon (iki temada render) | Faz 49 | ✅ |
| DataTable kaydırma fps | Faz 49 | ✅ |
| Renk körlüğünde ayırt edilebilirlik | Faz 49 | ✅ |
| axe `color-contrast` kuralı | Faz 49 | ✅ (kural **adıyla**) |
| Kart modu render | Faz 18 | ✅ |
| G-02 Playwright kurulumu | Faz 17 | ✅ |
| arm64 Chromium ölçümü (6.0 ③) | Faz 17 | ✅ |
| G-17 markalı kimlik tipleri | Faz 12 | ✅ |
| CSP gözlemi (6.3) | Faz 50 | ✅ |

**Yeni çelişki: 0 bulundu.** (*"Bakılmadı"* değil — yukarıdaki 15 iddia
programatik olarak ölçüldü.)

---

## 8. H — D5'İN ÜÇ YÜZEYİ

### ① Bugünkü durum — **üçü de çalışıyor, adıyla**

CI koşusu **`33949199057`** (`b3a259e`), `gh run view --json jobs` ile **adım
listesi** okundu — *"success"* etiketine güvenilmedi (5.7'nin dersi):

| Yüzey | Kanıt | amd64 | arm64 |
|---|---|---|---|
| **paket `dist`i** | `Derleme` adımı (`pnpm build`) | ✅ | ✅ |
| **web paketi** | `Web imajı çalışıyor mu` — varlık yolu `/fms` ile başlıyor, kök `/` **404** | ✅ | ✅ |
| **imaj** | `API/Web imajını derle` + `API imajı çalışıyor mu` (`"status":"ok"`) + `Eksik ortam değişkeniyle API AÇILMAMALI` + `Mimari doğrulaması` (`x86_64`/`aarch64`) | ✅ | ✅ |

**Altı işin altısı yeşil, atlanan adım yok.** 6.3b'nin imaj düzeltmesi
doğrulandı — varsayılmadı.

**Yerelde bugün:** `pnpm build` **8/8 SOĞUK** — soğukluk komutun kendi
çıktısından iddia ediliyor: `Cached: 0 cached, 8 total` (6.959 s). Üretim paketi
**383.976 B** / CSS **17.064 B** — 6.3b'nin ölçümüyle **bayt bayt aynı**.

### ② İleriye dönük kural — `docs/OUTPUT-FORMAT.md`

*"D5 tam"* iddiası artık **hangi yüzeyde koşulduğunu adıyla** söylemeden
yazılamaz. Kural üç yüzeyi tek tek sayıyor, 6.3 → 6.3b'nin ödenmiş bedelini
gerekçe olarak taşıyor, ve *"koşturulmayan yüzey **koşturulmadı** diye yazılır"*
diyor (SAPMA-024 ile aynı sınıf). Ayrıca üçüncü yüzeyin tek güvenilir ölçüm
yerinin CI'ın `İmaj` işi olduğunu ve **adım listesinin** okunması gerektiğini
yazıyor.

⚠️ **Geriye dönük rapor düzeltmesi YAPILMADI** — arşiv append-only.

---

## 9. MUTASYON SERİSİ — 3/3, üçü de kırdı

| # | Mutasyon | Yerine oturdu mu | Sonuç |
|---|---|---|---|
| ① | `ci.yml`de `run: pnpm debt:check` → `run: echo atlandi` | ✅ md5 `8c7cdcce`→`f052f3cd`, `grep -c` **0** | **1 test kırıldı** — `pnpm debt:check ci.yml de bir adım olarak var` |
| ② | `CLAUDE.md` ağacında `check-debt-coverage.mjs` adı bozuldu | ✅ md5 `21527928`→`0da51cb3`, `grep -c` **1** (bozuk hâli) | **1 test kırıldı** — `scripts/ altındaki her betik ağaçta anılıyor`, ve **eksik betiği adıyla** gösterdi |
| ③ | Çekirdekte `if (cells.length !== expectedCells)` → `if (false && …)` | ✅ md5 `ed013141`→`86f9fcf8`, satır 94 doğrulandı | **2 test kırıldı** — eksik **ve** fazla sütun vakalarının ikisi de |

**Geri alma dosya yedeğinden yapıldı** (`git checkout` **değil** — commit
edilmemiş işi siler). Üç dosyanın md5'i yedek öncesi değerlerine **birebir**
döndü ve `git status --porcelain` beklenen dosya listesini gösterdi.

---

## 10. DEĞİŞEN / EKLENEN DOSYALAR

```
scripts/lib/ledger-coverage.mjs        [YENİ] ortak çekirdek — iki kütük kapısı
scripts/lib/ledger-coverage.test.mjs   [YENİ] 3 katman: çözümleme · iki yönlü deney · alt süreç kanaryası
scripts/check-debt-coverage.mjs        [YENİ] `debt:check` — BORÇ kütüğü ↔ ROADMAP
scripts/inventory-guards.test.mjs      [YENİ] ① kapı↔CI kablolaması ② CLAUDE.md envanteri
scripts/check-gap-coverage.mjs         çekirdeğe taşındı — çıktı BAYT BAYT AYNI
vitest.config.ts                       14. proje: `scripts` (kapsam paydası DEĞİŞMEDİ)
package.json                           `debt:check` betiği
.github/workflows/ci.yml               `Teknik borç ↔ ROADMAP tutarlılığı` adımı
CLAUDE.md                              §2.2 `scripts/` + `docs/reports|schema` + LEGAL notu · §2.3 NODE_ENV · belge haritası +2
README.md                              K1–K15 · `SPEC-COVERAGE-GAPS.md` + `glossary.md`
docs/ROADMAP.md                        6.4-ön + SONUÇ · 6.10 ve 6.12 sayısızlaştırıldı · Faz 12/16/50'ye beş BORÇ
docs/SESSION-TEMPLATE.md               adım 6 · adım 17 · adım 21 (`debt:check`)
docs/OUTPUT-FORMAT.md                  D5'in üç yüzeyi kuralı (H②)
docs/spec/09-quality-protocol.md       §11.5 + iki kapı (SAPMA-044) — ⚠️ KARAR BEKLİYOR
PROJECT_MEMORY.md                      BORÇ-010 satırı onarıldı · BORÇ-012 · SAPMA-044 · günlük #12/#13 · ANLIK DURUM
```

---

## 11. KAPSAM — yeniden hesaplandı

| | Önce (6.3b) | Şimdi |
|---|---|---|
| Fonksiyon | **409 / 495 = %82,62** | **409 / 495 = %82,62** |
| İfade | %91,01 (1509/1658) | **%91,01 (1509/1658)** |
| Dal | %90,73 (627/691) | **%90,73 (627/691)** |
| Satır | %90,89 (1388/1527) | **%90,89 (1388/1527)** |
| Test | 1290 / 85 dosya | **1315 / 87 dosya** (+25 test, +2 dosya) |

**Marj:** `409 / 0,70 = 584` → payda 584'e kadar çıkabilir, boşluk **89**
(495'ten). Değişmedi.

⚠️ **Pay ve payda ikisi de sabit kaldı ve bu bir kaçamak değil, ölçülmüş bir
sonuç:** `coverage.include` deseni `packages/*/src/**` · `apps/*/src/**` ·
`tools/*/src/**` — `scripts/` **hiçbirine girmiyor**. Yeni test projesi 25 test
ekliyor ama kapsam paydasına **hiçbir dosya** eklemiyor. **Eşik düşürülmedi,
hiçbir dosya dışlanmadı**; gerekçe `vitest.config.ts`teki proje yorumunda yazılı
ki bir sonraki okuyucu bunu bir dışlama sanmasın.

---

## 12. YAPILMAYANLAR — açıkça

- **`arch:check`in çıktısı düzeltilmedi** → **BORÇ-012**, vade **6.10**. Gerekçe:
  `runArchCheck()` yalnızca `violations` döndürüyor; sayaçları dışa vermek imzayı
  değiştirir ve onu çağıran testleri ilgilendirir — bir denetim alt görevinin
  kapsamı değil.
- **SAPMA kütüğü için kapı yazılmadı** → vade fazı sütunu **yok**, denetlenebilir
  bir hedefi yok (bölüm 1.5). Uydurulmadı.
- **SAPMA'nın *"Spec/ROADMAP güncellendi mi"* sütunu doğrulanmadı** → farklı bir
  kontrol, kapsam dışı (K12). Yazıldı, yapılmadı.
- **Kalan 44 fazın kriter denetimi yapılmadı** → G'nin sınırı, Faz 50'nin işi.
- **`docs/reports/**` ve faz kayıtları değiştirilmedi** → append-only.
