> **Alt görev:** 6.6-ön — Süreç göçü (ürün kodu yok)
> **Tarih:** 2026-09-11 · **Dal:** feature/faz-06-design-system
> **İçerik commit'i:** yazım anında bilinmiyor — güncel durum ANLIK DURUM'da · **CI koşusu:** yazım anında bilinmiyor — güncel durum ANLIK DURUM'da · **PR:** yok (faz sonunda)
> **Bağlam yüzdesi:** ölçülemedi — `/context` çıktısı bu turda mevcut değil
> **İki oturum:** ilki kullanım limitinde kesildi (ajanlar/skill/CHECKPOINT/§18 yazılmış, kapılar koşmuş, mutasyon koşmamıştı); bu rapor kurtarma oturumunda yazıldı.

## 🟢 TAMAM 6.6-ön — Süreç göçü: altı ajan, `/faz-yurut`, CHECKPOINT + nöbetçisi, §18, SAPMA-045

**Ne yapıldı:** Her turda prompt olarak yeniden gönderilen çalışma kuralları depoya taşındı (altı ajan, bir skill, hata kataloğu + değişmezler, makine durum dosyası ve nöbetçisi, danışman protokolü), §0.5 gün sayma yerine iş birimi ölçüsüne geçti ve sunucu modu kararı SAPMA olarak kaydedilip Faz 7–9/50 kapsamına devredildi.

**Kapılar:** typecheck [x] 11/11 · lint [x] 0 · format [x] 0 (json·yaml·mjs baktı, `*.md` bakmadı) · **test [x] 1451/107 + 1 skipped** · **test:db [x] 301/10** · build [x] **8/8 SOĞUK** · arch [x] temiz · gaps [x] 20·3·17·0✗ · debt [x] 12·5·7·0✗ · i18n [x] 48 dosya·2 kök
**Commit:** yazım anında bilinmiyor · push — bilinemez · ANLIK DURUM güncellendi [x] · CHECKPOINT güncellendi [x]

**Kanıtlar:**
- **Ajanlar ve skill YÜKLENDİ — kurtarma oturumunda ölçüldü, yaratan oturumda ölçülemedi:** altı ajan sistem listesinde (açıklamalar frontmatter'dan birebir); `olcumcu` haiku ile çağrıldı ve **döndü** (gövde başlığı + `Bash, Glob, Grep, Read` = `tools:` + gövde iddiası; 0 araç, 5,0 s); `Skill('faz-yurut')` dosyanın **tamamını** yükledi. İlk oturumun üç denemesi (`Agent` → not found · `Skill` → unknown · `claude -p` → kuruluş politikası) ve sebebi (harness `.claude/`yi **oturum başında** okur) DETAY §1'de.
- **§② `.claude/` eklenince KIRILMADI** — nöbetçi o dizini taramıyordu; yüzey eklendi, M3 ısırdı. **`docs/CHECKPOINT.md` için §② gerçekten kırıldı** ve belge haritasıyla kapandı (M4 ısırdı).
- **§③ nöbetçisi dosyadan ÖNCE yazıldı; kanarya gerçek depoda öttü** (`durum: tamamlandi` + ROADMAP `[ ]` → *"expected false to be true"*). **Bayat CHECKPOINT'te YEŞİLDİ** — devir notu *"kırmızı olmalı"* demişti; sebep ölçüldü (nöbetçi yalnızca kaynağı olan dört alana bakar), DETAY §3.
- **Mutasyon 5/5** — M1 `faz: 7` → 2 test · M2 `taban_commit: deadbee` → 1 · M3 ağaçtan `agents/` → 1 · M4 haritadan `CHECKPOINT.md` → 1 · M5 kanarya → 1; hepsi md5 ile yerine oturdu ve **yedekten** geri alındı.
- **`CLAUDE.md` §14'ün md5'i `c1a6b26` ile AYNI** (`471911085a1da219cf35d8249de06e45`, 90 satır); `glossary-check` yeşil, 77 kayıt.
- **`format:check`:** `.claude/settings.json` (json) · `ci.yml` (yaml) · `inventory-guards.test.mjs` (babel) **bakıldı** — ilk zincirde `.mjs`i **kırmızı** buldu (Prettier biçimi), düzeltildi; `.gitignore` (parser yok) ve tüm `.md` (`.claude/agents/*.md`, `SKILL.md`, `CHECKPOINT.md`) **bakılmadı**.
- **⚠️ `ci.yml` `fetch-depth: 0` YERELDE DOĞRULANAMAZ** — bu commit'in CI koşusunda ölçülecek (§③ `git merge-base --is-ancestor`; depth 1'de ebeveyn yok).
- **SAPMA-045 devri Faz 7 · 8 · 9 · 50 KAPSAMINDA adıyla** (ROADMAP satır 4077 · 4158 · 4212 · 6203/6213, `grep` ile).
- **Kapı tabanı yeniden ölçüldü:** promptun iddiasıyla aynı (typecheck 11/11 · build 8/8 · gaps · debt · i18n); test **1443 → 1451 + 1 skipped** (+9 = §③'ün sekizi + `.claude/`), fonksiyon %84,09 (460/547) değişmedi.
- **D5:** paket `dist` ✅ (`pnpm build` soğuk, 8/8) · web paketi ✅ (aynı derleme) · imaj — **koşturulmadı** (ürün kodu değişmedi; tek güvenilir yer CI'ın `İmaj` işi).

**KARARIN GEREKİYOR:** YOK

**Yeni kayıt:** **SAPMA-045** (`karar` — sunucu modu `private`, kriter yerine kondu, Faz 7–9/50'ye devir) · günlük **#25–#28** · Faz 4 kırmızı koşusu `33419337117` **kapandı** ("Bilinen kayıt düzeltmeleri", faz kayıtları değişmedi)

**Sıradaki:** 6.6 — on alan-özel bileşen (`/faz-yurut 6.6`); etiketler `docs/glossary.md`den türetilir. ⚠️ Önce bu commit'in CI koşusu okunur: `Kalite kapıları` iki mimaride yeşil mi ve adım günlüğünde `inventory-guards ③ kapsam:` satırı var mı.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 0. ADIM 0 — iki oturum

| Ölçüm | İlk oturum | Kurtarma oturumu |
|---|---|---|
| `pwd` · dal · tepe | `/c/fms` · `feature/faz-06-design-system` · `c1a6b26` · tag `faz-06-5-son` | aynı tepe; **8 değişmiş + 4 izlenmeyen** dosya, commit yok — devir notuyla aynı |
| Kirli ağaç | `.claude/settings.json` (kullanıcı: `permissions` + `workflowSizeGuideline`, Prettier'dan geçmiş) — kapsamda, commit'e giriyor | aynı |
| Giriş kapıları (yeniden ölçüldü) | typecheck 11/11 (cached) · build 8/8 (7 cached, `@fms/web` miss) · gaps 20·3·17 · debt 12·5·7 · i18n 48·2 · scripts+glossary 45/45 · format 0 · `.claude/settings.json` `ignored:false` | son zincir §5'te |
| CI | `gh run list --json`: #116 `34504587371` `c1a6b26` success · #87 = **`33419337117`** `0459fa5` attempt 2 → 6/6 success; adım günlükleri **868/63** ve **241/8** | — |
| Docker | `29.7.2` | `test:db` koştu (301/10) |

**Neden ölçümleri (prompt "ölçüldü" diyordu, yeniden sayıldı):** `[x]` alt görev **71** (HEAD, boşluksuz kimlik deseni) · ROADMAP Faz 1–6 bölümü **3.740 satır** (BLOK B başlığına kadar), Faz 7–50 **2.170** (v2 kasasına kadar) — promptun 3.745/2.298'i sınır tanımına göre değişiyor, büyüklük sırası aynı · `.claude/` altında **1 dosya**.

---

## 1. AJANLAR VE SKILL — yazıldı, yüklenmesi iki oturuma yayıldı

### 1.1 Ne yazıldı

| Dosya | Araçlar | Rol | Bayt |
|---|---|---|---|
| `.claude/agents/olcumcu.md` | Read, Grep, Glob, Bash | ölçer, **yazmaz**; D1 + D2; standart taban tarifi; çıktı = komut + ham satır + değer | 3.485 gövde |
| `planci.md` | Read, Grep, Glob | kapsam (K12) + "yazmadan önce analiz" + kırılması beklenen kapılar + açık kararlar; **yazmaz** | 2.912 |
| `gelistirici.md` | Read, Edit, Write, Bash | **tek yazıcı**; `## ÇIKTI` ve `## GEREKÇE` ayrı | 3.151 |
| `kapici.md` | Read, Bash | **düşman**: her kapının negatif kanıtı · mutasyon (yedek→md5→boz→geri) · kablolama · D5 üç yüzey | 3.323 |
| `denetci.md` | Read, Grep, Bash | on soru (hangi yüzey · türev mi · sayı ölçümden mi · D7 · envanter sayı mı · kapsam · ayar yüklendi mi · kriter yerine kondu mu · devir tam mı · beklenen kapı kırıldı mı) | 2.681 |
| `kayitci.md` | Read, Edit, Write | ROADMAP/PROJECT_MEMORY/CHECKPOINT/rapor/commit mesajı dosyası; **DEVİR KURALI** | 3.354 |
| `.claude/skills/faz-yurut/SKILL.md` | — | beş aşama (ölç · planla · yaz · sına · kaydet) + ADIM 0 + kapı zinciri + mutasyon + rapor zorunlulukları + **faz kapanışında `git tag -a faz-XX-son`** + YAPMA | 6.892 |

`denetci` ve `kapici` **yalnızca `## ÇIKTI`yı görür** — kural üç yerde (iki ajan + skill + DZ-21). `model` alanı bilerek **boş** (varsayılan devralınır; bir model kararı bu alt görevin kapsamında değildi).

### 1.2 Yüklenme — ilk oturumda ÜÇ yol, üçü kapalı

| Yol | Sonuç | Ne gösteriyor |
|---|---|---|
| `Agent(subagent_type:'olcumcu', model:'haiku')` | *"Agent type 'olcumcu' not found. Available agents: claude, claude-code-guide, Explore, general-purpose, Plan, statusline-setup"* | liste oturum başında donmuş |
| `Skill('faz-yurut')` | *"Unknown skill: faz-yurut"* | aynı |
| `claude -p --model haiku --max-turns 1 "…"` (alt süreç, taze oturum) | *"Your organization has disabled Claude subscription access for Claude Code"* | kuruluş politikası; taze oturum yolu kapalı |

**Sebep:** harness `.claude/agents/` ve `.claude/skills/`i oturum **başında** okuyor; dosyayı yaratan oturum onları yükleyemez. İlk oturum bunun yerine YAML frontmatter'ı `yaml@2.9.0` ile ayrıştırdı (7/7: `name` = dosya adı, `description` dolu, `tools` bilinen araçlar, gövde var) ve **bunun bir yüklenme kanıtı olmadığını yazdı** (DZ-10): *"ölçülemedi — ilk ölçülebilecek yer bir sonraki oturumun sistem listesi"*.

### 1.3 Yüklenme — kurtarma oturumu ÖLÇTÜ

- **Liste:** oturumun araç listesinde altı ajan, açıklamaları frontmatter'daki `description` ile **birebir** (harness dosyaları ayrıştırmış); skill listesinde `faz-yurut`.
- **Çağrı:** `olcumcu` (haiku, araç yok) → satır 1 `# ÖLÇÜMCÜ — sayı ölçer, iddia etmez` (gövdenin ilk başlığı, birebir) · satır 2 `Bash, Glob, Grep, Read` (= `tools:` alanı; başka araç **yok** — kısıt uygulanmış) · satır 3 `GOVDE VAR` ("Standart taban ölçümü" ifadesi gövdede). `tool_uses: 0 · duration 4.988 ms · 26.937 token`.
- **Skill:** `Skill('faz-yurut')` → *"Base directory for this skill: C:\fms\.claude\skills\faz-yurut"* + dosyanın tamamı.

Kalan beş ajan **çağrılmadı** (maliyet); yüklenme kanıtı onlar için **liste + birebir açıklama**. Bir sonraki alt görev (`/faz-yurut 6.6`) hepsini doğal akışta çağıracak.

---

## 2. CLAUDE.md — §18 eklendi, §14'e dokunulmadı

- **§18.1** D1–D7 + F1–F5: 12 satır, her biri belirti + reçete; vakalar faz kayıtlarının §5'inde (ayrışırsa **faz kaydı kazanır**, yazılı).
- **§18.2** DZ-01…DZ-22 — süreç değişmezleri; numaralar sabit, yeni olan sona.
- Numara **18**: MASTER-SPEC 1–17'yi kullanıyor. Bölüm önce §2'nin arkasına kondu, sonra **dosyanın sonuna** taşındı (1 · 2 · 14 · 16 · 18 — bayt düzeyinde ASCII çapalı bir Node betiğiyle; üçlü ayraç artığı elle temizlendi).
- **§14:** `git show c1a6b26:CLAUDE.md | awk '/^# 14\. /{f=1} /^# 16\. /{f=0} f' | md5sum` = **`471911085a1da219cf35d8249de06e45`**, 90 satır; bugünkü dosyada **aynı**. `glossary-check` 77 kayıt, yeşil (`parseSection14` başlığa çapalı; §18 sona konduğu için `\n# ` sınırı da değişmedi).
- Ayrıca: 🚦 listesine `docs/CHECKPOINT.md` **ilk** sıraya (6 madde) · belge haritasına 4 satır · §2.2 ağacına `.claude/` bloğu + `docs/CHECKPOINT.md` + `docs/DANISMAN-PROTOKOLU.md` · §1.5 `.gitignore` listesine `*.bak`, `*.yedek`.

---

## 3. CHECKPOINT + NÖBETÇİ §③ — sıra, kanarya, sınır

### 3.1 Sözleşme

`docs/CHECKPOINT.md`: tek `yaml` çitli blok, 12 alan, tek satırlık değerler, YAML ayrıştırıcı **yok** (düz `anahtar: değer`). Nöbetçili alanlar: `faz` (ROADMAP'in etkin fazı = son `[x]` **veya** ilk `[ ]` alt görevin fazı) · `alt_gorev` (satır var) + `durum` (`tamamlandi` ⇔ `[x]`) · `asama`/`durum` kapalı küme · `taban_commit` (`git cat-file -e` + `merge-base --is-ancestor HEAD`) · `dal` (`skipIf`: ayrık HEAD / `main` / `develop`) · `son_commit_baslik` (`skipIf`: kirli ağaç — dosya kendi commit'ini taşıyamaz, `spec/11` §12.3). Atlanan vakalar **sebebiyle basılıyor**. ROADMAP alt görev deseni `^- \[( |x)\] \*\*(\d+)\.([^*\s]+)\*\*` — boşluksuz kimlik, Faz 4'ün `**5.000 sahte oyuncu…**` kriterini dışarıda bırakıyor (gevşek desen 80, gerçek **79**).

### 3.2 Sıra (DZ-12)

nöbetçi yazıldı → koştu: **7 kırmızı** (dosya yok; `.claude/` yüzeyi de kırmızı) → dosya `durum: devam` → **yeşil** (§③ 8 vaka + 1 skipped) → kapanış: `durum: tamamlandi` yazıldı, ROADMAP `[ ]` → **kırmızı**: `× alt_gorev ROADMAP te VAR ve kutusu durum ile aynı şeyi söylüyor — expected false to be true` → `[x]` → **yeşil**. Kanarya uydurma bir fixture değil, kapanışın kendi sırasıydı.

### 3.3 İlk gerçek sınav — oturum kesintisi ve NÖBETÇİNİN SINIRI

Kurtarma oturumu dosyayı `asama: 3-yaz`, `yarim_kalan` eski, `son_commit_baslik` var olmayan bir commit ile buldu. Devir notu *"§③ şu an KIRMIZI olmalı; değilse sebebini ara"* dedi. **Ölçüm: yeşil** (33 passed, 1 skipped — `son_commit_baslik → ağaç kirli`). Sebep: nöbetçi **kaynağı olan** dört alana bakar; `asama`, `biten`, `yarim_kalan`, `siradaki_komut` **anlatı** — karşılaştırılacak kaynakları yok ve onlara bir kapı yazmak uydurma bir kaynak gerektirirdi. "Bayat" ≠ "tutarsız"; nöbetçi ikincisini yakalar ve **kapsamını basıyor** (`kapsam: faz=… alt_gorev=… durum=… taban=…`). Devir notunun beklentisi nöbetçinin bastığından fazlaydı → günlük **#28** (D7: devir notu kaynak değildir). Nöbetçi **değiştirilmedi**.

### 3.4 CI — `fetch-depth: 0`

`actions/checkout` varsayılanı depth 1; `taban_commit` HEAD'in ebeveyni olur olmaz `git cat-file -e` bulamaz ve §③ CI'da her koşuda kırılırdı. Sığ klonda **atlamak** yerine (D3) `quality` işine `fetch-depth: 0` eklendi (yorumu ci.yml'de; `integration` ve `image` bu testi koşturmuyor). **Yerelde doğrulanamaz** — bu commit'in koşusunda `Kalite kapıları (amd64/arm64)` işinin geçtiği ve adım günlüğünde `inventory-guards ③ kapsam:` satırının bulunduğu okunur. `inventory-guards` ① (`run: pnpm <ad>` + maskesiz) değişiklikten etkilenmedi (yeşil).

---

## 4. MUTASYON SERİSİ — 5/5

| # | Hedef | Ne bozuldu (md5 önce → sonra) | Kırılan (adıyla) | Geri alındı (md5) | Yorum |
|---|---|---|---|---|---|
| M1 | §③ | `CHECKPOINT.md` `faz: 6` → `7` (`95ceca4a…` → `60dc5358…`) | `faz ROADMAP in ETKİN fazı` · `alt_gorev … kutusu durum ile aynı` | `95ceca4a…` ✅ | iki test — satır araması da faza bağlı |
| M2 | §③ | `taban_commit: c1a6b26` → `deadbee` (`95ceca4a…` → `ea40acbd…`) | `taban_commit git te VAR ve HEAD in atası` | ✅ | `cat-file -e` reddetti |
| M3 | §② `.claude/` | `CLAUDE.md` ağacından `agents/` satırı silindi (`551822ae…` → `537ed833…`) | `.claude/ altındaki her alt dizin ağaçta anılıyor` | `551822ae…` ✅ | yeni yüzey ısırıyor |
| M4 | §② harita | belge haritasından `docs/CHECKPOINT.md` satırı silindi (`551822ae…` → `a3431f11…`) | `docs/*.md belgelerinin hepsi BELGE HARİTASINDA` | ✅ | 6.4-ön'ün yüzeyi yeni belgeyi görüyor |
| M5 | §③ (kanarya) | `durum: tamamlandi` iken ROADMAP `[ ]` | `alt_gorev … kutusu durum ile aynı şeyi söylüyor` | ROADMAP `[x]` (kalıcı düzeltme) | kapanışın doğal sırası |

Geri alma **yedekten** (`cp` scratchpad → dosya), `git checkout` **hiç** kullanılmadı; yerine oturma md5 ile.

---

## 5. KAPI ZİNCİRİ — son ağaç, her komut kendi çıkış koduyla

| Kapı | Çıkış | Ham satır |
|---|---|---|
| `pnpm typecheck` | 0 | `Tasks: 11 successful, 11 total` (3 cached — önceki soğuk build önbelleği temizlemişti) |
| `pnpm lint` | 0 | hata satırı yok |
| `pnpm format:check` | 0 | `All matched files use Prettier code style!` — **baktığı:** `settings.json` json · `ci.yml` yaml · `inventory-guards.test.mjs` babel (`--file-info`); **bakmadığı:** `.gitignore` (parser null), `*.md` (ignored) |
| `pnpm arch:check` | 0 | `✓ arch:check temiz (410 ms)` — kural sayısını **basmıyor** (BORÇ-012, 6.10) |
| `pnpm gaps:check` | 0 | `20 · atlanan 3 (G-03, G-08, G-18) · taranan 17` |
| `pnpm debt:check` | 0 | `12 · atlanan 5 · taranan 7` |
| `pnpm i18n:check` | 0 | `tanımlı 66 · kullanılan 41 · taranan kaynak 48 dosya · 2 kök · görünmez tarama 398 dosya` (389 + 9 yeni `.md`) |
| `pnpm test:coverage` | 0 | `Test Files 107 passed · Tests 1451 passed | 1 skipped (1452)` · fonksiyon `460/547 = 84.09` (`coverage-summary.json`) · `Errors` satırı **yok** |
| `pnpm test:db` | 0 | `Test Files 10 · Tests 301` |
| `pnpm build` (`.turbo/cache` silindi) | 0 | `Tasks: 8 successful, 8 total · Cached: 0 cached, 8 total` |

**İlk zincirde `format:check` EXIT=1** (`scripts/inventory-guards.test.mjs`) → `prettier --write` → ikinci zincir tam yeşil. Belge düzenlemeleri bittikten sonra belge okuyan kapılar **yeniden** koştu: scripts+glossary 53/54 (1 skipped) · gaps 0 · debt 0 · i18n 0 (görünmez tarama **399** — bu rapor eklenince) · format 0. Test sayısı 6.5'e göre **+9**: §③ sekiz vaka + `.claude/` bir vaka; skipped **1** = `son_commit_baslik` (kirli ağaç; CI'da ve commit sonrası yerelde koşar).

---

## 6. §0.5, SAPMA-045, KÜTÜK

- **§0.5:** başlık *"Faz Boyutu — tek workflow koşusu"*; ölçü ≤ 15 bağımsız iş birimi; "bağımsız iş birimi" tanımı; Faz 6 bölünmez; eski kural `<details>` arşivinde; SAPMA-033 hikâyesi duruyor. Hizalanan yerler: 6.5'in §0.5 bloğu (karar notu eklendi, ölçüm silinmedi) · 6.12 · Faz 6 Risk satırı · Faz 47 notu · "bölünme riski yüksek fazlar" · `SESSION-TEMPLATE` adım 15 (**numara korundu**, içerik değişti).
- **SAPMA-045** (`karar`): tarih, kararı veren, iki sonuç, üç soru (spec · ROADMAP kaç fazda · kod yorumu). Bilerek dokunulmayanlar yazılı: Faz 13 hedefi, Faz 47 (yetenek ≠ işletme modu), MASTER-SPEC (donmuş).
- **Faz 4 kırmızı koşusu:** "Bilinen kayıt düzeltmeleri"nde kapandı — `gh run view 33419337117 --json jobs,attempt,conclusion` → attempt 2, 6/6; adım günlüklerinden 868/63 ve 241/8. Faz 4/5 kayıtları **değişmedi**.
- **ANLIK DURUM'un "12 commit"i yanlıştı:** `git rev-list --count 2ec1548..HEAD` = **15** (bu commit'le 16). DZ-01'in canlı vakası, satır düzeltildi.

---

## 7. DEĞİŞEN / EKLENEN DOSYALAR

`.claude/agents/{olcumcu,planci,gelistirici,kapici,denetci,kayitci}.md` (yeni) · `.claude/skills/faz-yurut/SKILL.md` (yeni) · `.claude/settings.json` (kullanıcı) · `docs/CHECKPOINT.md` (yeni) · `docs/DANISMAN-PROTOKOLU.md` (yeni) · `scripts/inventory-guards.test.mjs` (§② `.claude/`, §③) · `.github/workflows/ci.yml` (quality `fetch-depth: 0`) · `.gitignore` (`*.yedek`) · `CLAUDE.md` (🚦, harita, §1.1, §1.5, §2.2, §16.3, §18) · `docs/ROADMAP.md` (§0.5, 6.5 karar notu, 6.6-ön, 6.12, Risk, Faz 7/8/9/47/50, bölünme listesi) · `docs/SESSION-TEMPLATE.md` (adım 15) · `PROJECT_MEMORY.md` (SAPMA-045, kapanış notu, günlük #25–#28, ANLIK DURUM) · bu rapor.

## 8. YAPILMAYANLAR — açıkça

Workflow kurulmadı/koşturulmadı (6.6) · SONUÇ blokları taşınmadı · Faz 7–50 boyutlandırılmadı · §14'e dokunulmadı · `SERVER_MODE` için ifade dışı hiçbir şey · `.env` okunmadı · ajanların `model` alanı boş bırakıldı (karar değil, kapsam dışı) · beş ajan çağrılmadı (yüklenme kanıtı liste + birebir açıklama; ilk çağrı 6.6'da) · imaj yüzeyi koşturulmadı (ürün kodu değişmedi; CI'ın `İmaj` işi).
