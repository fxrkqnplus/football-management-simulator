> **Alt görev:** 5.8 — G-13 kararı (kabul kriteri 6)
> **Tarih:** 2026-09-04 · **Dal:** `feature/faz-05-i18n`
> **İçerik commit'i:** `28eef2a` · **CI koşusu:** yazım anında bilinmiyor — güncel durum ANLIK DURUM'da · **PR:** faz sonunda (5.9)
> **Bağlam yüzdesi:** ölçülemedi

## 🟢 TAMAM 5.8 — G-13 kararı · KRİTER 6 KAPANDI · FAZ 5'İN YEDİ KRİTERİ DE TAM

**Ne yapıldı:** Çeviri kaynağının nerede yaşadığı karara bağlandı, üç seçenek gerekçeyle şartlandırıldı, ve iki ölçüm G-13'ün kapsamını daralttı — kod yazılmadan, şema değişmeden.

**Kapılar:** typecheck [x] · lint [x] · format [x] · test [x] · arch [x] · test:db [x] · **gaps [x]** · **i18n [x]** · build **[koşmadı — gerekçe §7]** · mutasyon **[koşmadı — gerekçe §7]**
**Commit:** `28eef2a` · push [ ] · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- **ŞEMA DEĞİŞMEDİ, ve kanıtlandı:** `drizzle-kit generate` → *"No schema changes, nothing to migrate"*, `drizzle/` **git-temiz**.
- typecheck 10/10 · lint 0 (önbellek silinmiş) · format 0 · arch `✓ temiz` 9 kural.
- test → **1120 / 75** · test:db → **301 / 10** (ikisi de değişmedi — beklenen).
- **gaps → 20 · 3 atlandı · 17 tarandı · 0 ✗**, ve `✓ G-13 → Faz 5 + Faz 17` (§4).
- **i18n:check → temiz, 306 dosya.**
- kapsam → **364 / 451 = %80,70**, değişmedi (§7).

**KARARIN GEREKİYOR:** YOK

**Yeni kayıt:** SAPMA/BORÇ/SORUN **yok**. Çalışma günlüğü: **yeni satır yok** — bu alt görevde hiçbir şey kırılmadı (günlük *kırılan* şeyleri taşır).

**5.7'nin CI koşusu:** `33804107034` → **success**, ve adım adım okundu: `i18n:check` + `gaps:check` **her iki mimaride de** success.

**Sıradaki:** 5.9 — **faz kapanışı**: §0.5 ölçümü · yedi kriter tek tek · soğuk kapılar · faz kaydı (11 başlık) · CHANGELOG · **PR → `develop`**.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. Karara bağlanan şey — ve *"mümkün ≠ seçildi"* sınırı nerede çizildi

**KARAR (Faz 5'in payı):**

> Çeviri kaynağı **tek yerde** yaşar — `apps/web/src/locales/tr/*.json`.
> Veritabanı **anahtarı** taşır, görünen **metni taşımaz**.

⚠️ Bu bir tercih değil, **bugünkü mimarinin adının konması**. Ölçüldü: `Süper Lig` / `Premier League` gibi adlar **yalnızca** `common.json`da (11 anahtar); kalan tüm eşleşmeler test iddiaları ve ek motoru fixture'ları — hiçbiri üretim kaynağı değil. 5.6 aynı mimariyi `i18n-dynamic-keys.ts`te `common:competition.` ön ekiyle **zaten beyan etmişti**; bu karar veritabanı tarafını da adıyla bağlıyor.

**SINIR — 5.8 ne yaptı, ne YAPMADI:**

| | 5.8 (Faz 5) | Faz 17 |
|---|---|---|
| Soru | *"Veri **nerede** yaşıyor?"* | *"Arama **nasıl** uygulanacak?"* |
| Cevap | Tek kaynak: locale JSON | üç seçenekten biri |
| Bugün | **kapandı** | açık |

⚠️ **Tuzak A'dan (fazla ileri gitmek) kaçınıldı:** hiçbir cümle *"şunu kullanacağız"* demiyor. ⚠️ **Tuzak B'den (hiç karar vermemek) de:** Faz 17 artık **hiçbir şey varsaymak zorunda değil** — dört şey onun yerine ölçülüp yazıldı: ① çevirinin nerede yaşadığı ② veritabanı tarafındaki her metnin **türetilmiş** olmak zorunda olduğu ③ arama sorununun **tek bir tabloya** indiği ④ istemcide çevirilerin **zaten hepsinin bulunduğu**.

## 2. Üç seçenek — ① mümkün mü ② bedeli ③ sahibi

| | ① İstemci tarafı arama | ② Arama tablosu | ③ `display_name` sütunu |
|---|---|---|---|
| **Mümkün mü** | **Evet, şartsız** | **Evet, ŞARTLI** | **Evet, ŞARTLI + ek bedelli** |
| **Şart** | — | Metin **türetilmiş** olmalı (locale'den projeksiyon) + ayrışmayı önleyen **koşan kontrol** | ②'nin şartı **artı** master tabloda migration + `docs/spec/01` güncellemesi |
| **Kopya** | yok | **var** (türetilmezse karara aykırı) | **var** (aynı) |
| **İndeks** | yok | yeni trigram indeksi | yeni trigram indeksi |
| **Migration** | yok | yeni tablo | **master tabloda** — Faz 12'nin zemini |
| **Ölçülmüş kolaylık** | 5.3: çeviriler **statik paketleniyor**, istemcide **zaten hepsi var** | — | — |
| **Bedeli** | arama yükü istemcide | ayrışma riski (türetme + kontrol ile kapanır) | aynı + şema yüzeyi |
| **Sahibi** | **Faz 17** | **Faz 17** | **Faz 17** |

⚠️ **Hiçbiri elenmedi — ama ② ve ③'ün NAİF biçimi elendi.** Elle yazılmış bir kopya (`Süper Lig` hem JSON'da hem veritabanında, ikisi de kaynak) karara aykırıdır: bu, deponun en çok tekrarlanan hata sınıfı (BORÇ-008'in dokuz kopyası · `spec/09` §11.5'in *"hiçbir kural iki yerde denetlenmez"* disiplini · 5.5'in emekli ettiği ikinci uygulama).

ℹ️ **3.7'nin indeks kararı bir argüman değil ÖLÇÜLMÜŞ BİR OLGU:** `competitions`ta trigram indeksi **yok** ve olmamasının sebebi *"indekslenecek metin yok"*. ① o boşluğu **hiç doldurmadan** çalışır; ② ve ③ hem indeks hem migration ister.

## 3. Fiyat notunun yeniden doğrulanması (iki alt görev eskimişti)

5.3'ün notu *"çeviri artık yalnızca istemci JSON'ında"* diyordu. **Bugün yeniden ölçüldü ve TUTTU** — ama 5.7 araya girdiği için bir soru daha soruldu:

| Soru | Ölçüm |
|---|---|
| Türkçe yarışma adları hâlâ yalnızca `common.json`da mı? | **Evet** — 11 anahtar; diğer eşleşmeler test/fixture |
| 5.7'nin sözlüğü (`docs/glossary.md`) onları taşıyor mu? | **Hayır — 0 eşleşme** |

⚠️ İkincisi önemliydi: 5.7 `glossary.md`yi **yukarı akış sözleşmesi** ilan etti. Eğer yarışma adlarını taşısaydı, *"tek kaynak"* kararı iki aday arasında seçim yapmak zorunda kalırdı. Taşımıyor — ve sebebi yapısal: **özel ad bir terim değildir**. Sözlük `Competition | Yarışma` gibi **kavramları** taşır, `Süper Lig` gibi **varlık adlarını** değil.

## 4. G-13 satırı KAPATILMADI — ve gerekçe betiğin KENDİ sözleşmesi

`scripts/check-gap-coverage.mjs` okundu. Ölçülen sözleşme:

- `isClosed()` yalnızca `Durum` sütunu (yıldız ve ✅ soyulduktan sonra) **`KAPANDI` ya da `ÇÖZÜLDÜ` ile BAŞLIYORSA** satırı kapalı sayıyor.
- Kapalı satırlar **`skipped`e düşüyor ve bir daha hiç denetlenmiyor**.

G-13 **iki** faza atanmış (`**Faz 5**` + `**Faz 17**`). Kapatsaydım:

> Faz 17 bir gün G-13'ü kapsamından düşürse `gaps:check` **hiçbir şey demezdi** — çünkü satır zaten atlanıyor olurdu.

Bu, 4.11'in **koşan** kontrolünün yakaladığı körlüğün birebir aynısı (o gün `gaps:check` 17 satırdan 16'sını ✓, G-13'ü ✗ vermişti). Emsal betiğin **kendi başlığında** yazılı: *"G-11'in durumu «DARALDI — kapanmadı» diyor ve satır **açık kalmalı**"*.

→ **Satır açık; `Durum` hücresine Faz 5'in payı ve kapanma koşulu yazıldı** (*"Faz 17 mekanizmayı seçtiğinde kapanır"*). Metin bilerek `KAPANDI` ile **başlamıyor**.

**`gaps:check` sonucu — sayılar DEĞİŞMEDİ:**

```
kütükteki G satırı : 20
atlanan (kapalı)   : 3 — G-03, G-08, G-18
taranan (açık)     : 17
  ✓ G-13 → Faz 5 + Faz 17
```

ℹ️ Bu sayıları sabitleyen bir test **arandı ve bulunmadı** (tek eşleşme 5.6'nın CI adım kanaryası — o adımın **varlığına** bakıyor, sayıya değil). Yani güncellenecek bir iddia yoktu.

## 5. `rivalries` ölçümü — kapsam DARALDI

| Soru | Ölçüm |
|---|---|
| `name_key` sütunu var mı? | **Var** — ama `text('name_key')`, **nullable** |
| `competitions`ınkiyle aynı mı? | **Hayır** — orada `.notNull()` |
| `null` ne demek? | *"Rekabetin özel bir adı yok"* (`spec/01` nullable, `spec/12` §17.4: paketteki `rivals` girdisi `key`+`intensity`, **ad taşımıyor**) |
| Kaç satır? | **0** — `seed-sql.ts` `countries` · `competitions` · `people` · `players` dolduruyor, `rivalries` **hiç seed edilmiyor** |
| Faz 17'nin beş arama türünde var mı? | **YOK** — türler *oyuncu · kulüp · personel · **lig · turnuva*** ve son ikisi de `competitions` satırı |

⚠️ **Karar `rivalries`i KAPSIYOR ama arama sorunu kapsamıyor.** G-13'ün metni o sütunu *"aynı sorun"* diye anıyor ve bu **sınıf** olarak doğru (görünen ad bir i18n anahtarı) — ama bir **arama gereksinimi** olarak değil. Yani G-13'ün Faz 17'de kapatması gereken şey **tek bir tablo**: `competitions`.

⚠️ **Ve üçüncü bir olgu var, yalnızca orada yaşayabilir:** rekabetlerin çoğunun **hiç adı yok**. Bir gün rekabet araması istenirse sorun *"metin başka yerde"* değil ***"metin hiç yok"*** olacak — bir `display_name` sütunu onu **çözmez**, boşluğu taşır.

## 6. `rivalries.ts` yorum aldı — EVET, ve gerekçesi

ROADMAP yazım yeri olarak yalnızca `competitions.ts`i listeliyordu. **Fark bilinçli:**

- **Mimari karar** (`locales` tek kaynak) `rivalries.name_key`i de bağlıyor — yorumsuz bırakmak, o dosyanın bir sonraki okuyucusuna kararı yeniden türettirirdi.
- **Ama gerekçenin tamamı orada TEKRARLANMIYOR** — `competitions.ts`e atıf veriliyor (iki kopya bir gün ayrışır).
- **Ve yalnızca orada yazılabilecek olan** yazılıyor: `null` meşru, tablo boş, arama türlerinde yok.

Kriter `rivalries.name_key`i **adıyla** sayıyor; sessizce atlamak bir kayıt boşluğu olurdu.

## 7. Koşmayan kapılar — gerekçeleriyle

| Kapı | Neden koşmadı |
|---|---|
| `pnpm build` | Bu alt görev **derlenecek yeni kod üretmedi** — değişen üç dosyanın ikisi yorum, biri `.md`. Yorumlar `typecheck`ten geçti ve o koştu. Boş bir soğuk build ölçümü *"soğuktu"* der ama hiçbir şey kanıtlamaz |
| **Mutasyon** | **Bu alt görev nöbetçi üretmedi.** Mutasyon *"nöbetçi gerçekten nöbette mi"* sorusunu sorar; ortada test edilecek yeni bir kural, kapı ya da iddia yok. Kararın kendisini koruyan mekanizma `gaps:check` ve o **koştu** (§4) |

⚠️ **Bakacak bir şey bulamayan kapıya `✅` yazılmaz** (SAPMA-024) — bu yüzden yukarıdaki iki satır `[koşmadı]` olarak işaretlendi, `[x]` değil.

**Kapsam 364 / 451 = %80,70 — değişmesi beklenmiyordu ve değişmedi.** Üretilen şey yorum ve belge; hiçbir yeni fonksiyon ya da dosya paydaya girmedi. Marj: `364 / 0,70 = 520` → payda en fazla **520**, bugün **451**, **boşluk 69**.

## 8. Kriter 6'nın durumu ve Faz 5'in yeni sayısı

Kriter iki yarım taşıyor ve **ikisi de** sağlandı:

| Yarım | Nerede |
|---|---|
| *"çeviri kaynağının nerede yaşadığı **KARARA BAĞLANDI**"* | §1 — tek kaynak kararı, dört yerde yazılı |
| *"üç seçenekten hangilerinin **mümkün** olduğu **yazıldı**"* | §2 — üçü de mümkün, ikisi şartlı, şartlar adıyla |

→ **Kriter 6 `[x]`. Faz 5 artık 7/7.**

**Yazıldığı dört yer** (ROADMAP'in listesi + bir ekleme):
`docs/ROADMAP.md` Faz 5 (5.8 SONUÇ + kriter 6) · `docs/ROADMAP.md` **Faz 17** (kapsam bloğuna girdi olarak) · `docs/SPEC-COVERAGE-GAPS.md` (G-13 `Durum`) · `packages/db/src/schema/competitions.ts` **ve `rivalries.ts`** (yorum).

**Hedef fazın yapabildiği doğrulandı (NOT ⑦):** Faz 17'nin kapsamı üç seçeneği **zaten sayıyor** ve *"burada karara bağlanır"* diyor; ayrıca bir **kabul kriteri** taşıyor — *"Arama beş varlık türünün BEŞİNİ de kapsıyor — lig ve turnuva dahil; seçilen çözüm ve gerekçesi yazılı (G-13)"*. Yani faz bu boşluk sessizce açık kalarak **kapanamaz**.
