> **Alt görev:** 5.7 — `docs/glossary.md` (kabul kriteri 5)
> **Tarih:** 2026-09-03 · **Dal:** `feature/faz-05-i18n`
> **İçerik commit'i:** `da6bd17` · **CI koşusu:** yazım anında bilinmiyor — güncel durum ANLIK DURUM'da · **PR:** faz sonunda (5.9)
> **Bağlam yüzdesi:** ölçülemedi

## 🟢 TAMAM 5.7 — `docs/glossary.md` · KRİTER 5 KAPANDI

**Ne yapıldı:** 133 terimlik TR/EN sözlük yazıldı; sayı belgeye değil onu **ayrıştıran bir teste** kondu; ve bir toplama işleminin gizli varsayımı ölçülünce bir eksik çıktı.

**Kapılar:** typecheck [x] **SOĞUK** · lint [x] · format [x] ⚠️ · test [x] · build [x] **SOĞUK** · arch [x] · test:db [x] · gaps [x] · **i18n [x]** · mutasyon [x] **5/5**
**Commit:** `da6bd17` · push [ ] · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- typecheck `Cached: 0` 10/10, 10,38 s · build `Cached: 0` 8/8, 7,39 s · lint 0 (önbellek silinmiş).
- ⚠️ **format 0 ama BU ALT GÖREVİN ANA ÜRÜNÜNE HİÇ BAKMADI** — `.md` `.prettierignore`da (SAPMA-024). Denetlenen küme yalnızca iki `.mjs`.
- test → **1120 / 75 dosya** (1100/74 idi, **+20** sözlük testi) · test:db → **301/10** · arch 9 kural · gaps **20/3/17/0 ✗**.
- **i18n:check → temiz, 305 dosya** — 133 terimlik yeni Türkçe belgede gömülü görünmez karakter **0** (§7).
- **Kesişim ölçümü → 1** (§2), **mutasyon → 5/5** (§6), her birinin yerine oturduğu **ayrıca** doğrulandı.
- kapsam → **364 / 451 = %80,70**, hiç değişmedi (§8).

**KARARIN GEREKİYOR:** YOK

**Yeni kayıt:** **SAPMA-040** (`77 + 57 = 134` ayrıklık varsayımı — kesişim ölçüldü, gerçek sayı **133**). Çalışma günlüğü **#13**. Yeni BORÇ/SORUN yok.

**5.6'nın açık ucu kapandı:** CI koşusu `33786419740` adım adım okundu — `i18n:check` ve `gaps:check` **her iki mimaride de koştu ve success** (§1).

**Sıradaki:** 5.8 — G-13 kararı: `competitions.name_key` / `rivalries.name_key` için çeviri kaynağının nerede yaşadığı → kriter 6 (fazın **son** açık kriteri).

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. 5.6'nın açık ucu — artık ölçüm, çıkarım değil

5.6 kriter 2'nin ikinci yarısını (*"CI'da kırıyor"*) üç parçaya ayırmış ve
üçüncüsünü **ölçülemedi** diye işaretlemişti. Bugün koşunun **adım listesi**
okundu (`gh run view --json jobs`):

| Adım | amd64 | arm64 |
|---|---|---|
| `Çeviri kaynağı denetimi` (`pnpm i18n:check`) | **success** | **success** |
| `Spec boşluğu ↔ ROADMAP tutarlılığı` (`pnpm gaps:check`) | **success** | **success** |

⚠️ **Ne kanıtlıyor, ne kanıtlamıyor:** adımların CI'da **gerçekten koştuğu**
artık ölçüm (koşunun *"success"* olması bunu söylemiyordu — D3 sınıfı, adımı
tek tek görmek gerekiyordu). **Kanıtlanmayan** tek şey hâlâ aynı: başarısız
bir adımın işi kırdığı. O ancak gerçekten kırılan bir koşuda görülür.

## 2. `77 + 57 = 134` — bir toplama işleminin gizli varsayımı (SAPMA-040)

5.0 eşiği şöyle karşılamıştı: §14 **77** + nitelik envanteri **57** = **134**.
⚠️ **Bu toplam ancak iki küme AYRIK ise geçerli ve o gün kesişim sorulmadı.**

Programatik ölçüm (camelCase → kelime normalizasyonu, sonra §14 ile eşleme):

| Envanter | Üye | §14 ile kesişim |
|---|---|---|
| `VISIBLE_ATTRIBUTES` | 47 | **0** |
| `HIDDEN_ATTRIBUTES` | 10 | **1** → `injuryProneness` |
| `STAFF_ATTRIBUTES` | 16 | 0 |
| `MANAGER_ATTRIBUTES` | 8 | 0 |
| `POSITION_LEVELS` | 5 | 0 |

`injuryProneness` ↔ §14'ün `Injury Proneness | Sakatlığa Yatkınlık` satırı
**aynı terim**. Yani **134 değil 133**.

**Kriter etkilenmiyor** (133 ≥ 120) — ama düzeltilmezse bir sonraki okuyucu
134'ü yeniden türetir ve sözlükte olmayan bir terim arar. ⚠️ **Asıl kazanç
sayı değil, dedupe'un GÖRÜNÜR olması:** iki envanteri körü körüne birleştiren
bir sözlük `Injury Proneness`i **iki kez** taşırdı. Ölçüm önce yapıldığı için
terim çekirdek biçimiyle **bir kez** yaşıyor ve karar bir testte **adıyla**
sabit — sessiz bir dedupe değil.

🆕 **Aynı ölçüm 5.0'ın SAYMADIĞI üç envanteri de buldu.** Havuz aslında
77 + 47 + 9 + 16 + 8 + 5 = **162**. ⚠️ **Yazılmadılar (K12):** kriter 133 ile
sağlanıyor ve Türkçe karşılıkları o niteliklerin ekrana geldiği fazın işi.
Gözlem raporda bırakılmadı — sözlüğün **§6'sı** üçünü adıyla, kaynak
dosyasıyla ve sahibiyle sayıyor.

⚠️ **5.0'ın SONUÇ bloğu geriye dönük DEĞİŞTİRİLMEDİ** — o bir alt görev kaydı.
Düzeltme 5.7'nin SONUÇ'unda, kabul kriterinin metninde ve SAPMA-040'ta yaşıyor.

## 3. Kaynak KOD, spec değil — ve bu ROADMAP ile çelişmiyor

ROADMAP 5.7 *"`docs/spec/02/03/04/07`den toplanır"* diyor. `spec/02`'nin
nitelikleri **hücre içinde** duruyor (`| Kategori | Nitelikler (kod adı) |`),
yani elle çıkarmak bayat bir envanter üretirdi (F1).

Kullanılan kaynak `VISIBLE_ATTRIBUTES` / `HIDDEN_ATTRIBUTES` — ve **bu bir
sapma değil**, çünkü o sabitler `spec/02`ye karşı **kendi testleriyle zaten
çivilenmiş**:

| Test | İddiası |
|---|---|
| `player-attributes.test.ts` | `spec/02` §4.1'in dört kategorisi, adlar **tek tek**, toplam **47** |
| `player-hidden-attributes.test.ts` | `spec/02` §4.5, toplam **10** |

Yani koddan almak, spec'in **makine-okunur ve testle sabitlenmiş**
izdüşümünden almaktır.

## 4. Toplanan vs yazılan — iki yarım ayrı sayıldı

| | Sayı | Nereden |
|---|---|---|
| **Toplanan** (İngilizce sütun) | 77 + 47 + 9 = **133** | §14 ve kod envanterleri, programatik |
| **Yazılan** (Türkçe karşılık) | **56** | 47 görünür + 9 gizli nitelik |

⚠️ 77 çekirdek terimin Türkçesi **zaten vardı** (§14'ten birebir kopyalandı).
Yazılan yarı yalnızca niteliklerdir ve sebebi 5.0'da ölçülmüştü: `spec/02`
nitelikleri **yalnızca İngilizce** listeliyor, karşılıkların hiçbir yerde
önceden tanımlı hâli yoktu.

**Bu SAPMA-026'ya aykırı değil** (*"kimsenin belirlemediği alana değer
uydurma"*): bir sözlüğün tanımı gereği işi tam olarak o eşlemeyi tanımlamaktır.
Ama ayrım **saklanmıyor** — belgenin §3 başlığı *"Türkçe sütun bu belgede
doğdu"* diye yazıyor.

## 5. Sözlük ↔ locale kararı (NOT ④) ve ayrışmayı önleyen mekanizma

**Risk:** Faz 18/19 nitelik etiketlerini `t()` ile basacak, yani Türkçe bir gün
`locales/tr/squad.json`da da yaşayacak. İki liste bir gün ayrışır — bu deponun
en çok tekrarlanan hata sınıfı.

**Karar:**

| | `docs/glossary.md` | `locales/tr/*.json` |
|---|---|---|
| Ne | **Sözleşme** — hangi Türkçe kullanılacak | **Çalışma zamanı artefaktı** |
| Yön | **Yukarı akış** | Aşağı akış |

Yön belgenin başlığına yazıldı: bir terim ikisinde birden yaşadığında **çeviri
dosyası sözlüğe uyar**.

⚠️ **ZORLAYICI NÖBETÇİ BU FAZDA YAZILMADI — ve iki ölçülmüş sebep var:**
① Bugün çakışma **yok**: `squad.json` boş (`{}`), yani guard'ın bakacağı sıfır
satır olurdu ve *"bakacak bir şey bulamayan kapı bir onay değildir"*
(SAPMA-024). ② Anahtar adlandırma sözleşmesi (`attribute.<camelCase>` gibi)
**henüz yok**; bugün uydurmak Faz 18'in tasarımını **onun yerine sessizce**
vermek olurdu.

→ **Sahibi Faz 18 ve oraya KAPSAM MADDESİ olarak yazıldı** — kütüğe kayıt
yetmez, hedef fazın kapsamında görünmeli. Yeni bir BORÇ satırı açılmadı çünkü
hedef fazın kapsamı zaten o işin evi.

## 6. Sayının yeri ve mutasyon — 5/5

**Sayı belgede YAZMIYOR.** `docs/glossary.md` *"133 terim"* diye bir cümle
taşımıyor; sayı `tools/glossary-check/index.test.mjs`in **dosyayı
ayrıştırmasından** geliyor. Ödenmiş bedel: `world.md`nin programatik bloğu
güncel kalırken **prose dört ayrı yerde bayatladı**.

⚠️ **Ve uzunluk tek başına KÖR** (5.1'in dersi): 133 aynı satır da eşiği
geçerdi. Dağılım kaynak bazında **ayrı ayrı** sabit: çekirdek 77 · teknik 14 ·
zihinsel 14 · fiziksel 8 · kaleci 11 · gizli 9.

| # | Mutasyon | Kırılan / 20 |
|---|---|---|
| M1 | Sözlükten bir terim silindi | **3** |
| M2 | Yalnızca bir **karşılık** değiştirildi (terim aynı kaldı) | **1** |
| M3 | Ayrıştırıcı körletildi (`parseTerms` → `[]`) | **9** |
| M4 | `CLAUDE.md` §14 başlığı değiştirildi | **1** |
| M5 | Terim **bölüm değiştirdi** — toplam **korundu** | **1** |

⚠️ **M5 dağılım testinin gereksiz OLMADIĞINI kanıtlıyor:** toplam 133 kaldı,
sayım testi **geçti**, yalnızca dağılım testi ateşledi. *"Uzunluk kör bir
kontroldür"* somutlaştı.
⚠️ **M2, NOT ⑥'nın tam iddiası:** yalnızca Türkçe karşılık değişti; anahtar-only
bir eşleşme testi bunu **geçirirdi** ve iki liste o gün ayrışmış olurdu.
⚠️ **M4 SAPMA-024 sınıfını kapatıyor:** §14 bölümü okunamazsa eşleşme döngüsü
sıfır kez koşar ve test *hiçbir şey iddia etmeden* geçerdi; `toHaveLength(77)`
o boşluğu kapatıyor.

Her mutasyondan sonra **yerine oturduğu ayrıca basıldı**; geri alma **dosya
yedeğinden** yapıldı, `git checkout` kullanılmadı (günlük #9).

**Ayrıştırıcının kendi karşı kontrolü var (D2):** terim tablosu okunuyor ·
başka başlıklı tablo (`| Kullanma | Kullan |`) **okunmuyor** · ayraç ve başlık
satırı terim değil · tablo bitince duruyor. Tek yön yazılsaydı **her satırı
terim sayan** bozuk bir ayrıştırıcı da geçerdi — belgede terim taşımayan
**dört** tablo daha var.

## 7. `i18n:check` ve görünmez karakter (NOT ⑦)

5.6'nın kapısı `.md` dosyalarına bakıyor ve bu alt görev fazın **en büyük
Türkçe belgesini** üretti. Tuzak Faz 5 boyunca **sekiz kez** düşmüştü.

**Sonuç: 305 dosya tarandı, gömülü görünmez karakter 0.** Sayı 302 → 305
(glossary + iki araç dosyası), yani kapı **yeni dosyalara gerçekten baktı** —
*"0 bulundu"* ile *"hiçbir şeye bakmadı"* sayının artmasıyla ayrılıyor.

⚠️ **Bu, `format:check`in bakamadığı yer.** `.md` `.prettierignore`da; bu alt
görevde `pnpm format:check` **ana ürüne hiç bakmadı** ve raporda `✅` değil
uyarıyla yazıldı (SAPMA-024).

## 8. Kapsam ve belgenin okuyucusu

**Kapsam 364 / 451 = %80,70 — hiç değişmedi, ve ölçüldü:** üretilen ana ürün
bir `.md` (kod değil), `tools/glossary-check/` ise `.mjs` ve `src/` altında
değil → `coverage.include` (`tools/*/src/**/*.{ts,tsx,mts,cts}`) onu görmüyor.
`arch-check` · `i18n-check` · `bash-text-guard` · `eslint-local-rules` ile
**aynı statü**; bu araca özel hiçbir dışlama açılmadı.
Marj: `364 / 0,70 = 520` → payda en fazla **520**, bugün **451**, **boşluk 69**.

**Okuyucusu kuruldu (NOT ⑨).** Ölçüldü: `docs/glossary.md` `CLAUDE.md` belge
haritasında **yoktu**. Eklendi — okuyan fazlarla birlikte (6, 14, 17-21,
30-38, 44-45) ve otorite yönüyle. `SESSION-TEMPLATE` §15.1'in Faz 5 satırı da
güncellendi. 4.0'ın ① bulgusunun sınıfı: *"bir belgenin okuyucusu yoksa
yazılmamış gibidir"* ve atıf **hedef dosyada** aranır.

## 9. Kriter ↔ kapsam farkı (NOT ⑧)

Kriter tek yarım sayıyor (*"≥120 terim"*); ROADMAP kapsamı **iki** iş sayıyor:
*"TR/EN karşılıklar **+ kod içi isimlendirme standardı**"*.

**İkisi de yapıldı**, ama ikincisi **daraltılarak**: sözlüğün §1'i **dil**
kuralını taşıyor (hangi katman İngilizce, hangisi Türkçe), **biçim** tablosunu
(`kebab-case.ts`, `PascalCase.tsx`, `SCREAMING_SNAKE_CASE`, `snake_case`)
**taşımıyor** — onun tek yeri `CLAUDE.md` §1.3 ve sözlük ona **atıf veriyor**.
Aynı tabloyu ikinci bir yere kopyalamak, bu raporun §5'inde anlatılan
ayrışmanın ta kendisi olurdu.

## 10. Prompt ↔ kaynak

Bu turda promptun sayıları **tuttu**: §14 = 77 ✓ · BORÇ 10 satır / 3 ödenmiş /
7 açık ✓ · SAPMA 39 (şimdi 40) ✓ · G 20 ✓ · sorun 0 ✓ · kapsam 364/451 ✓ ·
test 1100/74 (5.7 öncesi) ✓. Düzeltilen tek sayı prompttan değil **ROADMAP'in
kendi 5.0 SONUÇ bloğundan** geldi: `134` → **133** (§2).
