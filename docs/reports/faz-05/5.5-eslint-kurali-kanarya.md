> **Alt görev:** 5.5 — ESLint kuralı + kanarya (kriter 1) · **§0.5 kontrol noktası**
> **Tarih:** 2026-09-03 · **Dal:** `feature/faz-05-i18n`
> **İçerik commit'i:** `e7d46ae` · **CI koşusu:** yazım anında bilinmiyor — güncel durum ANLIK DURUM'da · **PR:** faz sonunda (5.9)
> **Bağlam yüzdesi:** ölçülemedi

## 🟢 TAMAM 5.5 — `local/no-bare-jsx-text` + kanarya · KRİTER 1 KAPANDI

**Ne yapıldı:** K5'i zorlayan ESLint kuralı yazıldı ve **gerçek depoda kablolandığı kanıtlandı**; kuralın adı bir ölçüme dayanarak düzeltildi; 5.4'ün envanter aracı ikinci bir uygulama bırakmamak için **emekli edildi** (önce anlaştıkları ölçülerek); §0.5 kontrol noktası koşturuldu.

**Kapılar:** typecheck [x] **SOĞUK** · lint [x] **0** · format [x] · test [x] · build [x] **SOĞUK** · arch [x] · test:db [x] · gaps [x] · mutasyon [x] **4/4** · görünmez karakter [x] **0**
**Commit:** `e7d46ae` · push [ ] · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- lint **0**, önbellek silinmiş ve kural bağlandıktan **sonra** · typecheck `Cached: 0` 10/10, 8,23 s · build `Cached: 0` 8/8, 6,46 s.
- test → **1062 / 72 dosya** (1041 idi: **−11** emekli araç, **+32** yeni kural).
- test:db → **301 / 10** · arch `✓ temiz` 9 kural · gaps **20/3/17/0 ✗** · format 0 ⚠️ *(denetlenen küme TypeScript; `.md` `.prettierignore`da — SAPMA-024)*.
- kapsam → **364 / 451 = %80,70**, **değişmedi** (§6) · marj `364/0,70 = 520` → payda boşluğu **69**.
- **İki uygulama anlaşması → 33 = 33**, dosya dosya, uyuşmazlık yok (§2).
- **Mutasyon → 4 / 4** (§5) · **görünmez karakter → 0**, tarayıcının öz denetimi **9/9**.

**KARARIN GEREKİYOR:** YOK

**Yeni kayıt:** **SAPMA-039** (kural adı). Çalışma günlüğü **#9, #10** — ikisi de bu oturumda kırılan şeyler.

**§0.5:** `84a6d5f` → `e7d46ae` = **0,859 gün**, eşik **2** → **BÖLÜNME ATEŞLENMEDİ.** 5b ayrı dal açılmıyor.

**Sıradaki:** 5.6 — `tools/i18n-check/` + `pnpm i18n:check` + CI (aynı commit'te `gaps:check` de bağlanır) → kriter 2.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. Kural adı — `no-hardcoded-turkish` değil `no-bare-jsx-text` (SAPMA-039)

`index.js` Faz 1.4'ten beri ikinci kural için `no-hardcoded-turkish` adını
ayırmıştı ve fazın kapsam cümlesi *"JSX içinde çıplak **Türkçe** metin yasak"*
diyordu. **5.4 o sıfatı iki yönde birden çürütmüştü:**

| Yön | Ölçüm |
|---|---|
| Yanlış negatif | `Tekrar dene` gerçek bir K5 ihlali, **hiçbir Türkçe karakter yok** → `çğışöü` taraması **0** |
| Yanlış pozitif yönü | `App.tsx`in `base` / `api prefix` etiketleri **İngilizce** ve yine de ihlal |

ROADMAP'in 5.5 maddesi bunu **zaten yazıyordu** (*"«Türkçe metni yakala» diye
bir kural yazılamaz"*) — yani ölçüm bir yerde duruyor, ad başka bir şey
söylüyordu. **Bir ad bir sözleşmedir** ve kuralı örneklerinden geriye okuyan
birine yanlış öğretirdi (F3): 33 ihlalin **21'i** (`App.tsx` + `main.tsx`) tam
olarak *"benim etiketim İngilizce"* sınıfındandı.

Düzeltilen dört yer: kuralın kendi başlığı · `index.js`in ayırdığı satır ·
fazın kapsam cümlesi · ana dosyalar bloğu. **Kuralın davranışı değişmedi** —
değişen tek şey adının o davranışı doğru anlatması. Emsal **SAPMA-034**.

## 2. İki uygulama kararı — envanter EMEKLİ EDİLDİ (seçenek ②)

Karar **koda dokunmadan önce** verildi. Üç seçenek vardı (ortak çekirdek ·
emeklilik · ikisi kalır + fark yazılır).

**Neden ②, ölçümle:**

| Soru | Ölçüm |
|---|---|
| Aracın tüketicisi var mı? | **Yok** — hiçbir betikte, CI'da veya kodda geçmiyor; yalnızca kendi vitest projesi |
| Kuralın kapsamı aracınkini aşıyor mu? | **Evet** — `pnpm lint` deponun tamamına bakan bir **kapı**; araç elle çağrılan bir CLI |
| Depodaki `.tsx` yüzeyi | **9 dosya, hepsi `apps/web/src`** — yani dosya kümesi zaten aynı |
| ROADMAP ne diyordu? | Aracı *"kuralın **prototipi**"* diye adlandırıyordu |

⚠️ **Ortak çekirdek (①) mümkün değildi ve sebebi yapısal:** araç TypeScript'in
kendi ağacında (`ts.isJsxText`), ESLint kuralı **ESTree**'de çalışıyor. Düğüm
tipleri farklı; paylaşılabilecek olan yalnızca **politika**ydı, ve politikanın
tek bir eve ihtiyacı vardı — o ev kural oldu.

⚠️ **EMEKLİLİKTEN ÖNCE ANLAŞMA ÖLÇÜLDÜ.** *"Aynı işi yapıyorlar"* bir iddiadır;
`git show 5086b74^:` ile 5.4 öncesi dört dosya geri getirildi ve **ikisi de**
koşturuldu:

| Dosya | araç | kural |
|---|---|---|
| `App.tsx` | 19 | **19** |
| `main.tsx` | 2 | **2** |
| `components/ErrorBoundary.tsx` | 6 | **6** |
| `components/dev/DebugPanel.tsx` | 6 | **6** |
| **TOPLAM** | **33** | **33** |

Tek bir `only-tool` / `only-rule` satırı çıkmadı.

**Aracın 11 testinin sabitlediği NEGATİF anlamlar kuralın `valid[]` listesine
taşındı** (yorum · `logger` · `AppError.message` · teknik nitelik · JSX dışı
dize · sayı/noktalama). Emeklilik kanıtlanmış bir güvenceyi sessizce
düşürmemeli.

**Kör nokta kaydının yeni evi:** kuralın **kendi başlığı** (*"kural neyi
görmediğini kendi belgesinde söyler"* — ROADMAP 5.5 ②). Kayıt ayrıca
`DebugPanel.tsx`in yorumunda, ROADMAP'in 5.5 maddesinde ve ANLIK DURUM'da
duruyor; araçla birlikte gitmedi.

⚠️ **Araçtan İKİ BİLİNÇLİ SAPMA var ve ikisi de testle sabitlendi:**
① Nitelik konumundaki ifade kabına artık **nitelik politikası** uygulanıyor.
Araçta `data-testid={'x'}` ihlal sayılırken `data-testid="x"` sayılmıyordu —
aynı şeyin iki yazımı, iki farklı cevap.
② İfadesiz **şablon literalleri** de görülüyor (`` {`Metin`} ``) — emsal
`no-hardcoded-path`; aksi hâlde `{'x'}` yasak, `` {`x`} `` serbest olurdu.

## 3. Kablolama kanıtı — kanarya nerede, sökülünce ne kırılıyor

`RuleTester` kuralı **izole** koşturur. *"`pnpm lint` bunu gerçekten
koşturuyor mu"* bambaşka bir iddiadır ve bu depoda ölçülerek yaşandı: 2.3b'de
`arch:check`in `import-casing` kuralının kablolaması koparıldığında **43 test
birden geçti**.

**Kanarya deponun kendi yapılandırmasına bağlı:** ESLint Node API'si
`eslint.config.js`i **çözüyor** ve **var olan gerçek bir kaynak dosyanın
kimliği** altında lint ediyor.

⚠️ **Diske hiçbir şey yazılmıyor — ve bu üç ölçümün sonucu:**

| Denenen | Ölçüm |
|---|---|
| `lintText` + **sanal** yol | ✗ `projectService` ayrıştırma hatası — dosya diskte olmalı |
| `apps/web/src/` altına geçici dosya | ✗ `*.test.tsx` vitest'in `web` projesinin glob'una takılabilir (koşu ortasında dosya yaratmak bir yarış) |
| aynı dosya + `coverage.include` | ✗ desen onu da kapsıyor, paydayı rastgele oynatabilirdi |
| **var olan dosyanın kimliği** | ✓ yol gerçek (yapılandırma ondan çözülüyor), içerik bizim |

⚠️ **Mekanizmanın kendisi BU KURALDAN BAĞIMSIZ doğrulandı:** aynı yolla
bugünkü `local/no-hardcoded-path` da gerçekten ötüyor (`App.tsx` kimliğinde
sev 2, `App.test.tsx` kimliğinde sessiz).

**Kanarya neye bağlı:** kuralın `index.js`te kayıtlı olması · `error`
seviyesinde açık olması · yolun `ignores`a takılmaması · muafiyetin bu yolu
kapsamaması. Dördünden biri bozulursa kanarya **susar ve bir test kırılır**
(§5).

## 4. Kuralın KAPSAMI — ve NEYİ GÖRMEDİĞİ

**Gördüğü:** ① `JSXText` düğümleri ② kullanıcıya görünen JSX niteliklerindeki
dizeler (`title`, `aria-label`, `placeholder`, `alt`, `label`) ③ JSX **çocuk**
konumundaki ifade kaplarındaki sabit dizeler — koşullu (`? :`), mantıksal
(`&&`), şablon.

⚠️ **GÖRMEDİĞİ — KÖR NOKTA: MODÜL DÜZEYİ METİN SABİTLERİ.** Dosyanın tepesinde
duran `const TAB_LABELS = { logs: 'Kayıtlar' }` JSX'e render edilir, kullanıcı
görür, kural **görmez**.

⚠️ **DAR BİR HEURİSTİK DENENMEDİ ÇÜNKÜ ÖLÇÜM ONU ÇÜRÜTTÜ.** *"Modül düzeyi
dize haritası JSX'te kullanılıyorsa bildir"* kuralı bugün `DebugPanel.tsx`teki
**dört** haritanın **dördüne birden** öterdi:

| Sabit | Ne | Heuristik ne derdi |
|---|---|---|
| `TABS` | iç kimlikler | ihlal ✗ |
| `LEVEL_COLORS` | CSS renkleri | ihlal ✗ |
| `TAB_LABEL_KEYS` | **i18n anahtarları** | ihlal ✗ |
| `EMPTY_TAB_NOTE_KEYS` | **i18n anahtarları** | ihlal ✗ |

**4/4 yanlış pozitif — ve ikisi tam da sorunu ÇÖZEN tablo.** `looksLikeProse`
ayırmıyor: `'#ff6b6b'` dört harf taşıyor ve eşiği geçiyor. Meşru kodu
işaretleyen bir kural kapatılır ve bir daha hiçbir şey yakalamaz.

→ **Boşluğun sahibi 5.6.** `i18n:check` `locales/**` ile birlikte kaynağı da
tarıyor ve bir dizeye *"çeviri anahtarı mı, düz metin mi"* diye sorabiliyor —
bu kuralın elinde olmayan bilgi. Kapsam kuralın **kendi başlığında** yazılı;
sessiz bir sınır *"kısmi koruma D3 yanılsaması üretir"*.

**Ayrıca kapsam dışı ve bu bilinçli:** yorumlar · `logger.*()` argümanları ·
`AppError.message` (SAPMA-010) · teknik nitelikler · JSX dışı dizeler.

## 5. Muafiyet kanaryası ve mutasyon — 4/4

**Muafiyet AYRI BİR BLOK oldu.** Mevcut muafiyet listesi `base-path.ts` /
`tools/arch-check/**` gibi yol **tanım yerlerini** de taşıyor; oraya bir satır
eklemek yeni kuralı o dosyalarda da **sessizce** kapatırdı. Bugün etkisi
olmazdı (hiçbirinde JSX yok) — ama muafiyetin dar kalması kuralın
güvenilirliğinin şartı.

**Muafiyet iki yönlü iddia ediliyor:** bir birim testindeki ihlal **ötmüyor**
(ayrı vaka) · `*.spec.tsx` muaf **değil** (ayrı vaka, Faz 17 uçtan uca gerçek
arayüzü sürer).

| # | Mutasyon | Kırılan / 55 |
|---|---|---|
| M1 | Kural `eslint.config.js`te **`off`** | **3** |
| M2 | Muafiyet **`**/*.tsx`**e genişletildi | **3** |
| M3 | Kural **körletildi** (`looksLikeProse` → `false`) | **12** |
| M4 | Kural `index.js` **kaydından çıkarıldı** | **3** |

M2'de muafiyet kanaryası **doğru şekilde geçmeye devam etti** (genişlemiş bir
muafiyet test dosyalarını hâlâ muaf tutar) — yani iki yön birbirinden
**ayırt edilebiliyor**, ikisi aynı anda kırılan kör bir çift değil.

⚠️ **İKİ MUTASYON ÖLÇÜMÜ SESSİZCE GEÇERSİZ ÇIKTI VE İKİSİ DE YAKALANDI.**
Yakalayan şey sonuç değil, mutasyondan sonra basılan doğrulama çıktısıydı
(günlük **#9**, **#10**): biri `git checkout` ile "geri alma" yaparken
**commit edilmemiş kendi işimi sildi** (mutasyon kuralın hiç bulunmadığı bir
zeminde koştu ve sonuç **beklenen yönde** olduğu için inandırıcıydı), biri
`sed` deseni hiç eşleşmediği hâlde *"55 passed"* verdi. **Bir mutasyonun
yerine oturduğu ayrıca doğrulanır** — D2'nin en pahalı biçimi.

## 6. Kapsam ve `pnpm lint`

**`pnpm lint` = 0**, önbellek silinmiş hâlde ve kural bağlandıktan **sonra**.
5.4 envanteri 0'a indirdiği için `error` bugün hiçbir şeyi kırmıyor; `warn`
ile açılsaydı hiçbir şeyi **zorlamazdı** ve kriter 1 sağlanmazdı.

**Kapsam 364 / 451 = %80,70 — hiç değişmedi, ve sebebi ölçüldü (NOT ⑨):**
`coverage.include` `tools/*/src/**/*.{ts,tsx,mts,cts}` istiyor.
`tools/eslint-local-rules/` altında **`src/` yok** ve dosyalar `.js`/`.mjs` —
ne yeni kural ne testi paydaya giriyor. Emekli edilen araç da aynı sebeple
paydada değildi; silinmesi bu yüzden hiçbir şeyi oynatmadı. **Beklenti
tuttu ve artık varsayım değil ölçüm.**
Marj: `364 / 0,70 = 520` → payda en fazla **520**, bugün **451**, **boşluk 69**.
Eşik düşürülmedi, dosya dışlanmadı, import testi yazılmadı.

## 7. §0.5 kontrol noktası — koşturuldu, ateşlenmedi

| | |
|---|---|
| İlk commit | `84a6d5f` — `2026-09-02 23:12:53 +0300` |
| 5.5'in içerik commit'i | `e7d46ae` — `2026-09-03 19:50:13 +0300` |
| Fark | 74.240 sn = **0,859 gün** |
| Eşik | **2 gün** |
| **Sonuç** | **AŞILMADI** (marj 1,141 gün) → **bölünme ATEŞLENMEDİ** |
| Faz commit sayısı | **15** |

**5b ayrı dal AÇILMIYOR** — 5.6…5.9 aynı dalda ve aynı PR'da devam eder.
Ölçüm `git show -s --format=%ct` ile yapıldı, tahminle değil.
⚠️ **Eşik aşılmadığı hâlde yazıldı:** bir kuralın ateşlenmemesi de bir
sonuçtur; kaydedilmezse *"kontrol edildi mi?"* sorusu bir sonraki fazda
yeniden doğar. §0.5 kuralının **kendisi değiştirilmedi**.

## 8. Kütük sapması — prompt ile kaynak arasında

Oturum promptu teknik borç için *"8 satır, ikisi ÖDENDİ → 6 açık"* diyordu.
Kaynaktan sayım: **8 satır, ÜÇÜ ÖDENDİ** (003 ve 005 → 5.4, **008 → 4.11**)
→ **açık 5**. ANLIK DURUM'un eski hâli de *"7 satır, ikisi kapandı"* yazıyordu;
açık sayısı (5) doğruydu ama sayım değildi. Düzeltildi. *(D7: kendi yazdığın
plan bir kaynak değildir — bu prompt dahil.)*

## 9. Kapsam dışı bırakılanlar (K12)

- **`no-hardcoded-path` için kablolama kanaryası yok** ve bu 5.5'te ölçülerek
  görüldü (aynı mekanizma onu da kanıtlayabilirdi, bir `expect` uzağında).
  **Yazılmadı** — o kural Faz 1.4'ün işi ve 5.5'in kapsamı değil. Buraya
  kaydedildi ki bir sonraki okuyucu için görünür olsun.
- Kuralın kör noktası için heuristik **denenmedi** (§4) — gerekçe ölçüm.
