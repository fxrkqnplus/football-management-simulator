> **Alt görev:** 5.9 — Faz 5 kapanışı
> **Tarih:** 2026-09-04 · **Dal:** `feature/faz-05-i18n`
> **İçerik commit'i:** `f552ea0` · **CI koşusu:** yazım anında bilinmiyor · **PR:** yazım anında bilinmiyor — kapanışta açılıyor, güncel durum ANLIK DURUM'da
> **Bağlam yüzdesi:** ölçülemedi

## 🟢 TAMAM 5.9 — FAZ 5 KAPANDI · KABUL KRİTERLERİ 7 / 7

**Ne yapıldı:** Faz kaydı (11 başlık) yazıldı, günlük boşaltıldı, süre iki eşikle ölçüldü — ve bir kapsam taşıması hatası bir faz sonra **yine** bulundu.

**Kapılar:** typecheck [x] **SOĞUK** · build [x] **SOĞUK** · lint [x] · format [x] ⚠️ · test [x] · arch [x] · test:db [x] · **gaps [x]** · **i18n [x]**
**Commit:** `f552ea0` · push [ ] · **PR [ ]** · ANLIK DURUM güncellendi [x]

**Kanıtlar:**
- typecheck `Cached: 0` 10/10, 8,45 s · build `Cached: 0` 8/8, 6,71 s · lint 0 (önbellek silinmiş).
- ⚠️ **format 0 ama fazın en büyük ürünlerine HİÇ BAKMADI** — `.md` `.prettierignore`da (SAPMA-024).
- test **1120 / 75** (faz başında 977/67 → **+143 / +8**) · test:db **301 / 10** (değişmedi) · arch 9 kural.
- **gaps 20 · 3 · 17 · 0 ✗** · **i18n:check temiz, 307 dosya** · kapsam **364/451 = %80,70**.
- **§0.5 → 1,260 gün**, iki eşik de aşılmadı (§3).
- **Prose taraması → 6 aday, 2 bayat** (§5).

**KARARIN GEREKİYOR:** YOK — ama **PR merge edilmedi**, o kullanıcının işi.

**Yeni kayıt:** SAPMA/BORÇ/SORUN **yok**. Çalışma günlüğü **13 → 0** (başlık kaldı), hepsi faz kaydının §5'ine indirgendi.

**Sıradaki:** **FAZ 6 — Tasarım Sistemi ve Bileşen Kütüphanesi.** Açılışta üç iş: `spec/05-design-system.md` **+ `docs/glossary.md`** okunur · günlük başlığı güncellenir · **BORÇ-009 yeniden ölçülür**.

───────────────── DETAY (okumak zorunda değilsin) ─────────────────

## 1. Kabul kriterleri — 7 / 7, her biri hangi ölçümle

| # | Kriter | Alt görev | Ölçüm |
|---|---|---|---|
| 1 | Sabit metin eklenince ESLint hata veriyor | **5.5** | `local/no-bare-jsx-text` `error`; **kanarya gerçek depoda** (gerçek `eslint.config.js`, var olan bir dosyanın kimliği altında). **Mutasyon 4/4** |
| 2 | `i18n-check` eksik anahtar buluyor, CI'da kırıyor | **5.6** | Sahte depoda `missing` ötüyor + CLI **alt süreç** çıkışı **1** (karşı kontrol **0**); adım `ci.yml`de ve **maskelenmemiş**. **Mutasyon 7/7** |
| 3 | Ek motoru 50 vakayı geçiyor | **5.1 + 5.3** | **57 vaka**; dört ünlü sınıfı, sekiz bileşim, dağılım **tek tek sabit**. **Mutasyon 3/3** |
| 4 | Tarih / para biçimi | **5.2** | İki yarısı da; `U+00A0` **kod noktalarıyla**, zaman dilimi **varsayılmıyor**. **Mutasyon 3/3** |
| 5 | Sözlükte ≥120 terim | **5.7** | **133**, sayı **prose'da değil** ayrıştıran bir testte; §14 eşleşmesi **anahtar ve değer**. **Mutasyon 5/5** |
| 6 | G-13 karara bağlandı | **5.8** | İki yarısı da; karar + üç seçeneğin şartları. Şema `drizzle-kit generate` ile **değişmemiş** kanıtlandı |
| 7 | BORÇ-003 + BORÇ-005 ödendi | **5.4** | İki yarısı da; `titleKey` tip seviyesinde zorluyor, `MESSAGE_BY_KIND` silindi. **D5 10/10 TAM** |

⚠️ **Yedisinin de "iki yarısı" ayrı ayrı soruldu** — *"bir kriteri kısmen sağlamak sağlamamaktır"* bu fazda üç kez uygulandı (5.2 · 5.4 · 5.8).

## 2. BORÇ-009 / BORÇ-010 Faz 6'ya taşındı mı — ve Faz 6 bunu YAPABİLİYOR mu

**Ölçüm önce:** ROADMAP'te `BORÇ-009` ve `BORÇ-010` **yalnızca 2768–2769**'da geçiyordu — yani **Faz 5'in içinde** (5.6'nın SONUÇ bloğu). Faz 6 bölümünde (2933–2974) **0 eşleşme**.

⚠️ **Bu, 4.11'in BORÇ-003/005 için bulduğunun birebir aynısı, bir faz sonra:** *"kütükte «Faz 5» yazıyordu ama ROADMAP'te yalnızca Faz 2 bölümünde geçiyorlardı."* Kütüğe kayıt, kapsam taşımasını **bitirmiyor**.

**Yapabildiği gösterildi — yoksa atama yanlış olurdu** (G-18'in dersi: yanlış bir atama, atamamaktan tehlikelidir):

| Borç | Faz 6 neden ödeyebilir |
|---|---|
| **009** (modül düzeyi metin sabitleri) | Faz 6 kapsamı **18 temel + 10 alan-özel bileşen** üretiyor ve hepsi etiket taşıyor. 5.6'da heuristik **3 yanlış pozitif / 0 gerçek pozitif** vermişti — **ölçülecek vaka yoktu**. Faz 6'da olacak. Bir `FormIndicator`ın `const LABELS = { W: 'G' }` yazması en doğal şey |
| **010** (`no-hardcoded-path` kanaryası) | İş, 5.5'in `no-bare-jsx-text.test.mjs`teki kanaryasının **birebir kopyası** (kimlik altında `lintText` + `calculateConfigForFile`); ve bu faz `apps/web`e yüzlerce dosya getirdiği için `basePath()` disiplini tam orada sınanacak |

→ **İkisi de Faz 6'nın kapsamına, gerekçeleriyle yazıldı.** BORÇ-009 için ayrıca bir **koşul** yazıldı: *"İlk iş yeniden ölçmek; heuristik hâlâ 0 gerçek pozitif veriyorsa çözüm bir kapı değil bir bileşen inceleme disiplinidir"* (SAPMA-026).

## 3. §0.5 — iki eşik, ikisi de ölçüldü

| | |
|---|---|
| Fazın ilk commit'i | `84a6d5f` — `2026-09-02 23:12:53 +0300` |
| Fazın son commit'i | `fb7feff` — `2026-09-04 05:27:50 +0300` |
| Fark | 108.897 sn = **1,260 gün** |
| **Eşik A** — bölünme kontrol noktası | **2 gün** → **aşılmadı** (5.5'te de ölçülmüştü: 0,859) |
| **Eşik B** — §0.5'in asıl sınırı | **3 gün** → **aşılmadı**, marj **1,740 gün** |
| Faz commit sayısı | **22** (`84a6d5f..fb7feff`) |

⚠️ **Bölünme çizgisi (5a / 5b) çizilmişti ama KULLANILMADI** — ve bu kaydedildi, çünkü *"bir kuralın ateşlenmemesi de bir sonuçtur"*. Kaydedilmezse *"kontrol edildi mi?"* sorusu bir sonraki fazda yeniden doğar.
ℹ️ Karşılaştırma: Faz 4 **4,562 gün** sürmüştü ve eşik B'yi aşmıştı. Faz 5 onun **dörtte biri**.
⚠️ **§0.5 kuralının kendisi kullanıcının açık kararı — değiştirilmedi**, yalnızca ölçüldü.

## 4. `glossary.md` kararı

**Ölçüm:** 5.7 sözlüğü `CLAUDE.md` belge haritasına ekledi ve okuyucular arasında **Faz 6**'yı saydı. Ama Faz 6'nın **kendi kapsamında** `glossary` için **0 eşleşme**.

**Karar: fazın kapsamına da yazıldı.** Gerekçe mekanik: bir oturum faz başında `SESSION-TEMPLATE` gereği **ROADMAP'in o faz bölümünü** okuyor. Belge haritası **global** bir dizin; fazın kapsamı ise o oturumun gerçekten okuduğu yer. İkisi farklı işler yapıyor ve bu bir kopya değil — harita *"ne zaman okunur"*, kapsam *"bu fazda neden gerekiyor"* diyor (bileşen etiketleri için **56 nitelik karşılığı** orada).

## 5. Prose taraması — 6 aday, 2 bayat

4.11'in dersi: *"bir belge, koşan bir testin koruduğu bloğun DIŞINDA sessizce bayatlar."*

| # | Aday | Sonuç |
|---|---|---|
| 1 | `CLAUDE.md` §2.2 `tools/` ağacı | ❌ **BAYAT** — altı dizinden **ikisi** listeliydi. Eksikler: `arch-check` · `bash-text-guard` · `eslint-local-rules` (üçü Faz 5'ten **önce** de eksikti) · `glossary-check`. **Düzeltildi, altısı da yazıldı** |
| 2 | `CLAUDE.md` §2.1 yığın | ❌ **BAYAT** — `i18next-browser-languagedetector` **hiç geçmiyordu**, oysa tablo başka satırlarda eşlik paketlerini sayıyor (`react-hook-form 7 + @hookform/resolvers 5`). **Düzeltildi** |
| 3 | ROADMAP Faz 5 "Ana dosyalar" | ⚠️ eksik (`glossary-check` · `i18next.d.ts` · `i18n-dynamic-keys.ts` yok) — **BİLEREK GÜNCELLENMEDİ**, §6'ya bak |
| 4 | `README.md` | ✅ güncel — i18next'i sürümsüz anıyor, bayatlayacak bir sayı yok |
| 5 | `.env.example` | ✅ güncel — Faz 5 hiçbir ortam değişkeni eklemedi |
| 6 | `SESSION-TEMPLATE` §15.1 Faz 5 satırı | ✅ güncel — 5.0-ön'de düzeltilmişti, 5.7'de glossary eklendi |

ℹ️ **Ve bir tuzak arandı ama bulunmadı (NOT ③):** 4.11'de kapanış talimatının **kendisi** bayattı (*"bugün G-01…G-16"*, gerçek 20). ROADMAP'in **5.9 maddesi** taşıdığı tek sayı *"faz kaydı (11 başlık)"* ve `docs/spec/11` §12.1 **aynı sayıyı** veriyor — **bayat değil**.

## 6. "Ana dosyalar" bloğu neden güncellenmedi

O blok fazın **planı**, kaydı değil. Sonradan tamamlamak, planın **öngörmediği** şeyleri öngörmüş gibi gösterirdi — bir eksiklikten daha kötü bir çarpıtma. Gerçekte üretilenlerin yeri faz kaydının **§9**'u ve orası **tam** (18 satır, `[YENİ]`/`[DEĞİŞTİ]`/`[SİLİNDİ]` etiketleriyle).

ℹ️ Bloğa 5.5'te bir düzeltme yapılmıştı (`no-bare-jsx-text.js` + SAPMA-039 notu) — ama o bir **yanlışın** düzeltilmesiydi (yanlış dosya adı), bir **eksiğin** tamamlanması değil. Ayrım kasıtlı.

## 7. CI — dalın tam sayımı

**Liste sorgusuyla, `event` + `status` + `conclusion` tek tek okundu:**

**Dalda 10 push koşusu, 10'u da `completed success`.** Son: `33829616049` (`fb7feff`, 5.8).

ℹ️ 5.7'de bir adım daha atılmıştı: koşunun *"success"* olması **adımların koştuğunu söylemiyor** (D3). `gh run view --json jobs` ile okundu ve `Çeviri kaynağı denetimi` + `Spec boşluğu ↔ ROADMAP tutarlılığı` adımlarının **her iki mimaride de** success olduğu görüldü.

⚠️ **FAZ 4'ÜN KIRMIZI KOŞUSU HÂLÂ AÇIK.** Faz 4'ün kaydı seriyi `33419337117`'de sonlandırmıştı (İmaj / amd64, Docker Hub auth zaman aşımı — **kod dışı**) ve koşu **yeniden denenmedi**. Bu **kullanıcının kararı** ve hâlâ bekliyor; Faz 5 ona dokunmadı.

⚠️ **Hâlâ kanıtlanmayan bir şey var:** *"başarısız bir adım işi kırıyor mu?"* — 5.6 onu *"ölçülemedi, çıkarım"* diye işaretledi ve öyle kaldı. Ancak gerçekten kırılan bir koşuda görülür.

## 8. Faz kaydı — 11 başlığın tamamı

`docs/spec/11-project-memory.md` §12.5 şablonunun **on bir başlığı da** dolduruldu: konu · planlananlar · gerçekte yapılanlar · **plandan sapmalar (6 satır)** · **giderilen hatalar (13 günlük satırı → 8 desen)** · kontroller · performans · **kabul kriterleri (7/7, tek tek)** · önemli dosyalar (18 satır) · yeni kayıtlar · **devir teslim**.

⚠️ **§6 ve §7'nin rakamları 5.9'da YENİDEN ölçüldü**, ara ölçümlerden kopyalanmadı — şablonun kendi uyarısı bunu istiyor ve Faz 1'de kapsam rakamı tam bu yüzden **17 puan** sapmıştı.
⚠️ **`docs/reports/faz-05/` OTORİTE DEĞİL** (D7): kayıttaki hiçbir sayı rapordan alınmadı; hepsi ya koşan bir komuttan ya `git`ten ya kaynak dosyadan geldi.

## 9. ANLIK DURUM — ne silindi, nereye gitti

**10 satır → 10 satır** (sözleşme korundu), ama **içerik faz geçişine çekildi**. Silinen her şeyin yeni evi ölçüldü:

| Silinen | Nereye gitti |
|---|---|
| 5.8'in ayrıntılı G-13 anlatımı | Faz kaydı §4 + §8, ve `docs/reports/faz-05/5.8-*.md` |
| Alt görev bazlı kapı geçmişi (1041→1062→1100→1120) | Faz kaydı §6 (faz başı → faz sonu) |
| i18next kurulum ayrıntısı (lockfile +5, peer kapısı) | Faz kaydı §3 + `DEPENDENCY-WATCH` |
| Günlük #9'un `git checkout` uyarısı | Faz kaydı §5 satır 3 |
| Paket boyutu geçmişi (321,49 → 373,22 → 380.908) | Faz kaydı §7 |

→ Hiçbir bilgi **sadece** ANLIK DURUM'da yaşamıyordu; her satır için *"bu faz kaydında ya da arşivde var mı?"* soruldu.

## 10. Faz 6'ya devir ve BORÇ-009 öngörüsü

Faz 5'in Faz 6'ya devrettiği: **i18n katmanı** (zorlayıcı — `no-bare-jsx-text` `error`) · **`docs/glossary.md`** (bağlayıcı sözleşme, 56 nitelik karşılığı) · **`i18n:check`** (her yeni anahtarı denetliyor) · **tipli anahtarlar** · **BORÇ-009 ve BORÇ-010**.

⚠️ **ÖLÇÜLMÜŞ ÖNGÖRÜ — ve 5.6 onu adıyla tahmin etmişti:** *"ilk vakalar tasarım sistemi bileşenleriyle gelecek ve o gün heuristik ölçülebilir olacak."* Faz 6 **28 bileşen** üretiyor; modül düzeyi bir etiket tablosu (`const LABELS = { W: 'G', D: 'B', L: 'M' }`) orada en doğal yazım. Yani BORÇ-009'un **bugün 0 olan gerçek pozitif sayısı** ilk kez sıfırdan büyük olabilir.
→ Devir bölümü bunu yazıyor **ve bir koşul koyuyor**: hâlâ 0 çıkarsa çözüm bir kapı değil bir **bileşen inceleme disiplinidir**.

**Ayrıca devredilen üç uyarı:** Faz 6 kendi risk notunda *"3 günü aşabilir"* diyor ve bölünme çizgisi (6a/6b) hazır · `main.test.tsx` jsdom yıkım yarışı **kapanmış sayılmıyor** ve gerçek sınavı orası · Faz 4'ün kırmızı koşusu kullanıcının kararını bekliyor.

## 11. Koşan ve koşmayan kapılar

**Koşanlar (ölçümüyle):** typecheck **10/10 `Cached: 0`** · build **8/8 `Cached: 0`** · lint **0** (önbellek silinerek) · format **0** · arch **9 kural** · test **1120/75** · test:db **301/10** · gaps **20/3/17/0 ✗** · i18n:check **307 dosya temiz** · kapsam **364/451**.

**Koşmayan:** yok — 5.9'da **hepsi** koşturuldu. ⚠️ Ama bir kapı **baktığı kümeyle** yazıldı: `pnpm format:check` `.md`ye bakmıyor, yani bu alt görevin ürettiği faz kaydına, CHANGELOG girdisine ve bu rapora **hiç bakmadı** (SAPMA-024). Onların görünmez karakter denetimi `i18n:check`ten geçti.

ℹ️ **Bir ölçüm daha yapıldı ve temiz çıktı:** kayıt yazılırken `PROJECT_MEMORY.md`de satır sonu uyarısı geldi. Ölçüldü — çalışma kopyası CRLF, git deposu LF (autocrlf, **her iki commit'te de aynı**); diff **227 ekleme / 30 silme = net +197** ve dosya **2583 → 2780** satır, birebir tutuyor. **Gizli bir satır sonu hasarı yok.**
