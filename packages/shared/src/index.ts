/**
 * @fms/shared — Paylaşılan tipler, Zod şemaları, sabitler, logger, basePath — hiçbir şeye bağımlı değildir
 *
 * Faz 1.2 iskeleti. Gerçek içerik ilgili fazda gelir.
 */
export const PACKAGE_NAME = '@fms/shared' as const;
